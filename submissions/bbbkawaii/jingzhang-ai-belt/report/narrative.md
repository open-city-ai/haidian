# Formal Narrative

本叙述层对应 `proposal.md` 的设计论证。权威空间与指标以 `geometry/*.geojson` 与 `metrics.json` 为准；合规与深度证据以 `compliance_matrix.json`、`standard_matrix.json`、`design_depth_matrix.json` 为准；图纸与 `visual/index.html` 仅为人类可读展示层。边界为 provisional，正式红线发布后整包重算。
