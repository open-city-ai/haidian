# 版权与许可声明 / Copyright and License Statement

## 一、总体声明

本方案包（含正文 `proposal.md`、英文译文 `proposal.en.md`、空间数据 `geometry/*.geojson`、指标 `metrics.json`、矩阵、图件 `assets/figures/*.png`、图纸 `drawings/*.pdf` 与离线展示页 `visual/index.html`）由声明作者 `xuhong992` 及其 AI agent（Claude Code）生成。方案仅使用三类材料：公开资料（official_public）、任务书 site package（user_provided_cleared）与 agent 生成内容（agent_generated_design）；不使用个人隐私、非公开规划资料或未获授权数据。

All submitted text, geometry, diagrams, PDFs, and static HTML assets are generated by the declared AI agent (`xuhong992` / Claude Code) or use cleared public/user-provided sources listed in `sources.json`. No third-party copyrighted assets are embedded unless explicitly cleared.

## 二、逐资产版权台账

逐资产作者、来源、许可与再使用范围以 `sources.json` 的 `asset_copyrights` 注册表为准。摘要如下：

| 资产 | 生成方式 | 来源依据 | 许可 | 再使用边界 |
| --- | --- | --- | --- | --- |
| `geometry/*.geojson`（9 图层） | agent / shapely 剖分 | provisional_boundaries.geojson（provisional_only） | COMMUNITY-DISPLAY-ONLY | 官方边界发布后须重算；不得作为官方红线或法定结论 |
| `assets/figures/*.png`（5 图） | agent / matplotlib | 本包 geometry 与 metrics（同源） | COMMUNITY-DISPLAY-ONLY | 仅作方案展示，不替代 JSON/GeoJSON 权威数据 |
| `drawings/*.pdf`（A3/A0） | agent / matplotlib PdfPages | 本包图件排版 | COMMUNITY-DISPLAY-ONLY | 仅供评审展示，非施工/审批图纸 |
| `visual/index.html`、`report/proposal.html` | agent / 静态 HTML/SVG | geometry、metrics、矩阵、proposal | COMMUNITY-DISPLAY-ONLY | 离线使用，不加载远程资源 |
| 命名与 Logo 方向 | agent / 概念建议 | 自创概念 | COMMUNITY-DISPLAY-ONLY | 未注册商标；正式使用前须另行清权 |
| 正文/矩阵/指标文本 | agent + 公开引用 | sources.json 所列来源 | COMMUNITY-DISPLAY-ONLY | 引用的公开内容保留原权利人权利 |

## 三、评审点名的逐项说明

- **字体（Fonts）**：本包不内嵌、不引用任何第三方字体文件；HTML 使用系统字体栈（`-apple-system, "PingFang SC", "Microsoft YaHei"`），PDF 使用系统渲染字体，均不随包分发受版权字体。`proposal.en.md` 与正文不含外部字体资源。
- **图像（Images）**：`assets/figures/*.png` 全部由 agent 依据本包自身 GeoJSON 与 metrics 用 matplotlib/Pillow 绘制，不使用外部图片、照片、插画或截图。
- **地图与数据（Maps/Data）**：空间数据仅来自 `brief/site-package/geometry/provisional_boundaries.geojson`（provisional_only）与 agent 生成图层；不含商业地图瓦片、OSM 派生、内部测绘或非公开空间数据。
- **Logo / 铁路意象（Logo/Railway imagery）**：「智脉京张」与「ZHANG-ZHI SMART BELT」为 agent 提出的概念性命名与 Logo 方向，不包含詹天佑/京张铁路的受版权图像；"铁路文脉"仅作文字概念引用，不以未授权照片或受版权图形呈现。
- **企业标识（Company logos）**：正文提到的 8 个国际案例（斯坦福、肯德尔广场、纬壹科技城等）均为公开行业案例的文字描述，不包含企业 logo、商标图样或受版权标志。
- **代码与依赖（Code/dependencies）**：生成脚本（`scripts/gen_submission_data.py`、`gen_figures.py`、`gen_pdfs.py`）由 agent 编写，依赖 shapely/pyproj/Pillow/matplotlib 均为开源许可；脚本不作为提交包正式成果，仅为可复现性说明。
- **AI 生成方式（AI generation）**：正文、几何、指标、图件、HTML 均由 Claude Code 依据任务书与公开资料生成；生成方法在 `agent.json` 的 `generation_note` 中披露。生成过程不采集个人数据。

## 四、COMMUNITY-DISPLAY-ONLY 条款

`COMMUNITY-DISPLAY-ONLY` 许可含义：本方案包**仅**供本开源征集活动的展示、评审、横向比较与社区讨论使用。具体地：

- 允许：本征集主办方与评审在 GitHub、项目展示站（haidian.open-city.ai）及相关评审材料中展示本包。
- 允许：第三方为学习、研究、评审目的引用本包并注明出处（作者 xuhong992）。
- 禁止：未经作者与各资产权利人书面授权，将本包或其衍生作品用于商业开发、工程实施、政府审批申报、品牌注册或任何正式落地用途。
- 再使用前须重新核对：商标/版权检索、「智脉京张」命名、Logo 元素、引用的公开内容权利，以及官方边界发布后的数据重算。

## 五、边界与责任

- 空间边界：`geometry/site_boundary.geojson` 与 `geometry/key_areas.geojson` 为 `provisional_constraint`（`official_boundary=false`、`boundary_precision=provisional_rough`），仅用于生成、展示与临时自检，不作为官方红线、审批依据、精确面积依据或法定控制结论。
- 本方案不声称官方批准、审定控规、最终土地权属、最终建设规模或保证实施；AI agent 对事实、来源、版权、空间数据、指标与表达负责。
- 本声明与 `sources.json` 的 `asset_copyrights` 注册表共同构成逐资产权利证据；任何资产的作者、许可或授权状态发生变化，须同步更新本声明并重新提交核验。

*本声明为 COMMUNITY-DISPLAY-ONLY 许可的说明文件，不构成法律意见；正式使用前请咨询专业法律顾问。*
