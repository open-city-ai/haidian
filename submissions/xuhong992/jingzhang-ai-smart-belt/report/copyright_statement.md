# 版权声明 / Copyright Statement

本方案包（含正文 `proposal.md`、空间数据 `geometry/*.geojson`、指标 `metrics.json`、矩阵、图件 `assets/figures/*.png`、图纸 `drawings/*.pdf` 与离线展示页 `visual/index.html`）由声明作者 `xuhong992` 及其 AI agent（Claude Code）生成，仅使用公开资料、任务书 site package 与 agent 生成内容，不使用个人隐私、非公开规划资料或未获授权数据。

All submitted text, geometry, diagrams, PDFs, and static HTML assets are generated by the declared AI agent (`xuhong992` / Claude Code) or use cleared public/user-provided sources listed in `sources.json`.

具体声明：

- **来源**：`data/source_registry.json` 与 `sources.json` 登记全部公开、清权与临时资料的来源、许可与用途边界；正文 `proposal.md` 以 `[source:]`、`[standard:]`、`[depth:]`、`[data:]`、`[metric:]` 显式引用证据。
- **边界**：`geometry/site_boundary.geojson` 与 `geometry/key_areas.geojson` 为 `provisional_constraint`（`official_boundary=false`），仅用于生成、展示与临时自检，不作为官方红线、审批依据、精确面积依据或法定控制结论。
- **图片与图纸**：`assets/figures/*.png` 与 `drawings/*.pdf` 均由 agent 依据本包 GeoJSON/metrics 派生，无外部授权图片、字体、图标、肖像或企业标识。
- **命名与视觉**：「智脉京张」命名与 Logo 方向为概念建议；字体、图标、商标、人物与企业标识在正式使用前须另行清权。
- **离线属性**：`visual/index.html` 与 `report/proposal.html` 均为离线静态 HTML，不加载 CDN、远程地图瓦片、外部脚本、外部字体、iframe、表单或外部 API。
- **许可**：本方案包按 `COMMUNITY-DISPLAY-ONLY` 提供，供开放征集展示与评审使用；正式落地与商用前需另行授权。
