# Copyright Statement

## Authorship and License

This submission package — including all narrative text (proposal.md, proposal.en.md), structured data (metrics.json, geometry GeoJSON, matrices), diagrams (assets/figures/*.png and *.en.png), PDF drawings (A3 booklet and A0 boards, bilingual), and static HTML (report/, visual/) — is generated by the declared AI agent **Kimi K3 (Moonshot AI)** as the submission author.

The entire package is released under the license declared in the proposal front matter: **CC-BY-SA-4.0** (Creative Commons Attribution-ShareAlike 4.0 International). Reuse requires attribution to the submission author and share-alike redistribution under the same license.

## Third-Party Sources

All external facts, figures, and planning-baseline claims are traceable to the 26 registered entries in `sources.json`, each carrying its source level (official / official_public / media / reference), an accessed date, and a usability grade (`yes` / `background_only` / `provisional_only`). Entries graded `background_only` are used solely for case-study and background context and are not load-bearing for compliance claims. Where official red-line or regulatory data was unavailable, the affected metrics are explicitly declared `unknown` in metrics.json and the affected boundaries are declared `provisional` in the geometry metadata, rather than substituted with unverifiable figures.

## Asset Provenance

- Geometry files (geometry/*.geojson) are constructed by the agent from publicly announced scope descriptions; boundary status is declared per feature.
- Figures and PDF drawings are rendered by the agent from the package's own structured data (metrics.json, matrices, geometry); no third-party copyrighted maps or images are embedded.
- visual/index.html and its bilingual counterparts are fully offline: no remote assets, fonts, scripts, iframes, or forms.

## AI-Generation Disclosure

This is an AI-generated submission as required by the call for proposals. Human review flags, known blockers, and assumptions are disclosed in assumptions.json and in Chapter 12 of the proposal (Compliance, Risk, and Governance Boundaries).
