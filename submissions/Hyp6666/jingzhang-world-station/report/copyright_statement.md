# 版权与生成说明 / Copyright and Generation Statement

## 作者与视觉系统 / Authorship and Visual System

本投稿的正文、结构化几何、指标、原创图表、双语网页、A3 文册与 A0 展板由投稿身份 Hyp6666 与 **OpenAI Codex 智能体（实际运行模型：GPT-5.6 Sol；模型标识：`gpt-5.6-sol`）** 在本仓库任务范围内共同生成。本次方案生成未调用其他子 Agent；ReportLab、Pillow、pypdf、pdfplumber、Poppler 与 Playwright 仅作为构建或质检工具。视觉系统 **Railway Evidence**（铁路证据）及其标志、色彩、版式、信息图和图件构成为本投稿的原创表达。

All submitted narrative, structured geometry, metrics, original diagrams, bilingual HTML, A3 booklet, and A0 boards were generated by the submitting identity Hyp6666 with the **OpenAI Codex agent running GPT-5.6 Sol (model identifier: `gpt-5.6-sol`)**. No other sub-agent was invoked for this proposal; ReportLab, Pillow, pypdf, pdfplumber, Poppler, and Playwright served only as build or QA tools. **Railway Evidence**, including its mark, palette, layouts, infographics, and figure compositions, is an original visual system for this submission.

## 数据与图像 / Data and Images

图件仅使用包内 `geometry/*.geojson`、`metrics.json`、矩阵文件、`visual/assets/design-data.json` 与 `sources.json` 中声明的公开/仓库资料生成。未使用外部地图截图、远程地图瓦片、遥感底图、未清权照片、远程图片或第三方品牌素材。临时坐标始终以 provisional / 非法定状态表达。

All figures derive only from local `geometry/*.geojson`, `metrics.json`, matrix files, `visual/assets/design-data.json`, and public/repository material declared in `sources.json`. No external map screenshot, remote map tile, satellite basemap, uncleared photograph, remote image, or third-party brand asset is used. Provisional coordinates are always presented as non-statutory.

## 字体与工具链 / Fonts and Toolchain

- PDF 中文排版使用 macOS 系统内的 **Arial Unicode.ttf**，通过 ReportLab 进行子集嵌入；字体文件未复制进投稿包，也未作为独立资产再分发。
- 英文 PDF 使用 ReportLab 内建 Helvetica 字体；离线 HTML 使用本机可用系统字体回退，不加载远程字体。
- PDF 由 **ReportLab** 生成，并使用 Pillow、pypdf、pdfplumber 与 Poppler 进行尺寸、页数、文本、渲染和视觉复核。
- 图件通过本地 Python/SVG/Raster deterministic build pipeline 生成；生成脚本保留在投稿包外的本地构建工具目录中。

The Chinese PDFs use the macOS system **Arial Unicode.ttf** with ReportLab subset embedding; the font file is not copied into the submission or redistributed as a standalone asset. English PDFs use ReportLab's built-in Helvetica. Offline HTML uses local system fallbacks and loads no remote font. ReportLab generates the PDFs; Pillow, pypdf, pdfplumber, and Poppler support dimensional, pagination, textual, rendering, and visual QA.

## 限制 / Limitations

本文件不改变 `sources.json` 中各资料原有权利与限制，也不把新闻示意、临时几何或国际案例转化为官方背书、合作关系或法定规划依据。正式实施前仍须由主办方、规划建筑、交通市政、文保结构、无障碍、法律与数据治理专业人员复核。

This statement does not alter the rights or limitations declared in `sources.json`, and it does not convert public descriptions, provisional geometry, or international precedents into official endorsement, partnership, or statutory planning evidence. Implementation still requires review by the organiser and relevant planning, architecture, mobility, utilities, heritage, structural, accessibility, legal, and data-governance professionals.
