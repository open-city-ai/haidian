# 方案迭代记录

## v0.42 - 2026-08-30

- **推算层第一条由「给不出」升级为「明示假设的参考推导」。** 区分被说清：此前否定的是**冒充已知**（把系数当已知去算），本轮给出的是**明示情景下的代入**——系数摆明是假设、算式全给、主体确定后整表替换。三样落点各归其位：完整推导住进新文件 `visual/assets/ops-ledger.json`（7,248 字节，中英双值，含 honesty_contract 直白句「52 与 0.5–1.0 系包外情景设定、无实测依据，不因写入本包而升格为已知」）；假设登记 `A-CARE-COEFF-001` 入 `assumptions.json`（第 19 条）；正文只留一句指路（段末，脱出「输入全部取自包内」前言辖域）。
- **算式与数**（脚本现算，不抄评审工作流报告）：计入点 59＝46 座应答门＋3 处广场＋主脊每公里 1 处（ceil(9,710.125/1000)＝10）；59×52×(0.5–1.0)＝**年 1,534–3,068 人时**；敏感性 26 次/年→767–1,534、104 次/年→3,068–6,136。道钉与标准组件并入宿主点不重复计点。**系数三分法**：频次是运营强度的决策变量（谁运营谁定）、单次工时是现场测出来的属性（此处仅为无依据宽区间情景设定）、周期是产品属性（选型未定无从设起）——把 0.5–1.0 也叫「决策变量」曾被三镜验证判为过度声明，已按三分法改。
- **两个情景人时键以 unknown+reason 入 metrics.json，不以 known。** known 形态被对抗验证判为 fatal：metrics 词表只有 known/unknown/not_applicable，把情景乘积盖 known 戳，正面撞上推算层自己那句「把这两列当作已知去算，算出来的是一个看着像概算的编造」——键名带 scenario、confidence=low 都压不住 status 字段的字面。数字改由 reason 全文携带（unknown+reason 载数有 floor_area_ratio 等 10 个先例），一个数都不丢。`care_point_count=59`（纯包内算术）与门表三计数 `external_gate_hold_count=11 / external_gate_released_count=0 / authorized_site_action_count=0`（对既有门表的自描述，assumption 只引门表前言原句）以 known 入表。metrics 共 **99 键＝87 known＋12 unknown**。
- **第三条（维护周期）的理由同批改写**，否则第一条改后它就塌了——原文「按通用工程经验填入即引入包外数据」对情景系数同样成立。改为「也不设明示系数：周期由尚未选定的产品决定」，中英同宽。**第二条（场地容量）一字未动**：它的理由是人身安全与正文承诺「不预设人数」，与系数无关——结尾句明写这一点，拦住将来任何「统一三条口径」的顺手改。
- **A-METRICS-001 同批修订**：原文「metrics.json 全部数值由 build_metrics.py 一次生成，不手写」在手写加键后即自我证伪，已登记六键例外清单。**「并入 A-RECALC-001 触发清单」的宣告一处都没写**——经核该触发器管官方红线边界、不管运营系数，写了就是跨文件自相矛盾；替换条款一律挂真实触发源 PRE-09／A-OPERATOR-001。
- **实施章重排（纯移动，零新增主张）**：最小可验证包整句提为章首第一句（原「其三」，编号头删除，其四→其三、其五→其四，计数句按实改为「除这个最小可验证包外…还有四类」——写「四类」会数错自己：第五类只是换了位置，仍是做法之一）；三处重点区的实施三步段各自上移至该区开篇段之后（大钟寺段先核过：第一步内容无一项落在文物协商范围内）。**两项被对抗验证砍掉的未做**：「-29 重复免责」删除（两路独立扫描该句不存在，其依据 out.txt 实为他人投稿普查、不是字节审计）；「本章先说能建的」胶水句（在全门 HOLD 旁读作开工许可口气，正撞评审表扬点）。
- **六载体同步 + 两处结构自纠**：md 搬移后三处插入段与目标段之间缺空行（markdown 会并段渲染），已补；随手清掉三处多余双空行。visual 两页整表渲染全部指标与假设，新增 6 指标行（known 4 行不带 data-metric——visual_review 只对五个核心指标要求声明，不带即不入其核值范围）＋2 unknown 行＋A-CARE-COEFF-001 行，页面计数 93→99、83→87、10→12、18→19 逐处改（含中英「包体事实」汇总表）。`metrics_snapshot.js` 以同构变换重嵌（对象级与 metrics.json 一致、A-METRICS-001 冻结五项逐值未变、sha256 重算）；页面生成器不存在的事实再次成立，全部为手工镜像。
- **manifest 登记** `visual/assets/ops-ledger.json`（role=evidence_data，五个同角色先例；包根新建 JSON 会被目录白名单硬拒，落点经验证器源码两路独立核对）。
- 字节：`proposal.en.md` 终值 **262,095**（距 256 KiB 上限 −49）；每步实测，含一次三版连错后回到「量确切字符串」的老实做法。

## v0.41 - 2026-08-30

- **空间不变量：给既有句子分类，不新增段落。** 甲方 2026-08-30 评审给满分那家的关键词是「空间明确性」，而给我们的扣分句是「当前证据只支持概念转化性」。差别不在有没有资料，在**空间定义建立在什么之上**：我们每处重点区的数值都建在临时边界上，资料一到就要重算；但**我们其实早就写着不随资料改变的规则，只是三处各说各的、没有共同的名字，读者读不出这是一套方法**。
- **三处就地标记（不动原句）**：钟声广场「关键构造纪律只有一条**，且不随任何官方资料改变**：同心环之间不得出现高差」；应答原点「两条不依赖该图纸也能成立的规则**（同样不依赖红线、权属与文保范围）**」；人字之跃「对专项研究提一条形态底线**，它同样不随资料改变**：无论如何分跨，桥面纵向必须保持平直」。英文侧的对应句本来就存在（"two rules that hold without those drawings"、"one critical construction discipline"、"one formal bottom line"），同样就地标记。
- **章首一句总述**说明两类陈述如何区分，并写明「**凡带尺寸的数字都属于重绑定量**」——这样重绑定量不必逐条列举，**既有数值一个未删**（100 米见方、700/3,100/6,200 ㎡、66×9、636 ㎡ 全部原样保留），只是被标明了性质。
- **两种做法的成本实测对比**（先算后写）：写成新段落需 1,431 字节、英文缺口 1,127；就地标记只需 373 字节、缺口 69。**差近 4 倍**，原因是后者不复述任何既有句子。缺口 69 已由门表第三轮无损压缩（10 处措辞，−65 字节）覆盖。
- **`proposal.en.md` 至此距 256 KiB 上限仅 19 字节。** 英文正文已实质到顶：此后任何新增英文内容，都必须先删掉等量的既有英文内容，且必须先算后写。

## v0.40 - 2026-08-30

- **甲方 2026-08-30 评审（PR #4296，93.0/100）表达完整度的唯一扣分点已改**：「HTML视觉首屏的灰色标题叠加深色夜景，对比度明显偏弱」。改的是封面副标题 `#ovx .cover .en` 三项：字号上限 13→15px、字距 .44→.30em、颜色由 `--ov-on-dim` 改为 `--ov-on`。中英两页同一条规则，各净减 3 字节。
- **对比度自算，未采信转述的数值**：`--ov-on-dim` 在包内定义三次——`#C3C4BC` 属浅色主题、`#ABAFA8` 属深色主题（`:root:not([data-theme="light"])` 与 `[data-theme="dark"]`）。**收到的 11.34:1 算的是浅色主题那个值，而封面照片在两种主题下都是深色夜景**，深色主题读者看到的是更暗的 `#ABAFA8`，实测对纯黑约 9.43:1。改后浅色 18.58:1、深色 18.09:1。两种主题下改前均已过 AA，扣分点不在数值而在小字号加宽字距压深底。
- **同类问题全页扫过，只此一处**：15 条使用 dim 色的规则中，`.hint` 形态相同但 `class="hint"` 在 HTML 里零使用（死 CSS，未动）；`.plate .txt p` 虽压在照片上，但图片 `::after` 有 `rgba(5,8,7,.66)` 遮罩覆盖文字所在的左侧 34%，对比度受控；其余均在纸色底上。**未为求统一而提亮其他 dim 色**——那会破坏层次。
- 未动内容、门表、可逆段与推算层。

## v0.39 - 2026-08-30

- **新增「推算层」一节，落在 PRE 门表之后。** 整章顺序成为：可逆立场（条件明确前就能开始）→ JZ 项目表（做什么）→ PRE 门表（缺什么、谁去补）→ 推算层（真转起来要多少）。节尾点明三者关系，未新造表。
- **原拟四条推算，实做一条半，另两条写明为什么不做。** 动手前先查输入，包内 `巡检 / 频次 / 养护频次 / 人日 / 工日 / 更换周期 / 保养` **各 0 次**——做推算所需的系数一个都没有。
  - **①运维工作量：只做可数的那一半。**设施量包内全有（主脊 9,710.1 米、46 座应答门、三处广场 10,000＋6,000＋8,000＝24,000 平方米、10 件标准组件、道钉起步 122 颗），算式给出 `年人时 ≈ Σ（数量 × 年频次 × 单次工时）`；**频次与工时两列包内没有，明写给不出**，并指向 PRE-09（运营与养护主体）。把这两列当已知去算，算出来的是「看着像概算的编造」。
  - **②维护周期：不做。**包内无任何周期口径，而「按通用工程经验」填入即引入包外数据，与本节前提冲突。
  - **③场地容量：能算而不算。**净面积除以人均安全站立面积即可得限流人数，算式并不难；但正文已两次写死「本方案不预设人数」「由专业团队按现行场所疏散要求核定」。**这条涉及人身安全，一个标着「推断」的区间仍然会被当成可用的数，档位标注挡不住这种误用。**故只给面积、不给人数。
  - **④造价跳档变量：做。**沿用正文既有依据（是否触及既有结构与市政管线），读出三个变量：触及既有结构 / 需管线迁改 / 需交通导改；并解释第一步为何落最低档——三项都不触及。仍不给绝对值。
- **③与④都是「不给数」，但理由不同，分开写明**：③是能算而不该算，④是没有依据可算。混为一谈会让读者以为本方案只是一律回避数字。
- **算式里原有一个包外系数，自查时改掉了**：初稿写 `年人日 ≈ Σ(…) ÷ 8`，那个 8（每人日 8 小时）是包外的换算惯例。改为输出**人时**，并注明由人时折人日需要一个工作日时长口径、同样不是包内数据。改后段内全部数字均为包内值。
- **门禁包装脚本此前可以一边显示红灯一边返回绿灯，已修并经故意破坏验证。** `build-staging/refresh_gates.py` 的返回值只反映 `check_drift` 与 `check_numbers`，**四门的 `ok` 只被打印、从不进入判定**。本轮四门第一次报 `ok=False`，而脚本照旧打印「已收敛」并返回 0——**此前十余轮把「退出码=0」当作四门通过的证据，是侥幸而不是判据在起作用**。已改为四门不过一律失败并打印「上面的 DRIFT/NUMBER 结果不能替代它」；故意破坏（把 `proposal.en.md` 撑过上限）实测返回 1，还原后返回 0。
- **本轮真实失败原因：`proposal.en.md` 撞了单文件 Markdown 上限。** `MAX_MARKDOWN_BYTES = 256 KiB`（262,144 字节），加入推算层后实测 265,213，超 3,069。中文正文只有 190,495——同样的命题英文词量大得多，**先撞这条上限的永远是英文版**。
- **压缩过程如实记录**：先扫全文找可删的重复，**英文没有可删的重复长句，最高频长短语总共才 235 字节**——这个文件是密的、不是灌水的。因此只压本轮自己新增的三段：推算层英文由 4,128 压到 2,496 字节、可逆立场段压 372、门表英文两轮共压 612；又删掉一处**我自己造成的重复**（三节关系在可逆段与推算层各说了一遍，删可逆段那处，中英同步）。压到距上限 6 字节仍不够，最后按预授权砍掉**造价跳档变量**一项（三档量级正文已有），得到 261,840、低于上限 304 字节，条目重编号为二、三。
- **压缩差点造成新的图文不符**：起初只压了 `.md`，两个英文 HTML 载体仍是长版本，而它们本应渲染同一内容。改为**整块以 `.md` 为准重写**（两段落 + 门表 11 行），三个英文载体现已完全一致（`report/proposal.en.html` −3,425、`visual/index.en.html` −3,385 字节）。

- 净增 22,685 字节 = 22.2 KiB，中英六个载体同步；未新增表，`make_visual_page.py` 的表数断言不受影响；`metrics.json` 的 unknown 一项未动。

## v0.38 - 2026-08-30

- **`render-ren-bridge.jpg` 换为板梁版人视透视，并改掉三处已失真的声明。** 旧图（2026-08-28 近正俯视航拍）**实为走不到地面的桥**：两侧各一组 U 形坡道，180 度折返弯位于贴近主跨的一端，两条腿都背向桥面延伸并在远端凌空截断——四个端头无一落地，图上不存在从桥面通往地面的路径。而当时的声明称它「经逐条比对与正文一致……两端均落在环路两侧绿地内」。**那句话是假的**，此处据实更正并记载替换原因（前版判为不采用由两版增至三版）。
- **新图逐条核过（自行开图、放大核端头，不是看整图凭印象）**。相符：一跨平直主跨、桥面之上除栏板与照明外无构筑物、桥墩落在中央分隔带、每侧一处折返且两段并排、两段标高不同、中夹绿带、坡道与梯段顶端和主跨交于同一桥头平台且标高一致、坡道低端与地面平接、梯段落地、坡道全程在道路一侧绿地内无一段跨越车道、图上无可读企业名称。结构形式为**铆接钢板梁**、**块石砌筑桥墩**、铁路式钢格栅栏板，与京张短跨桥的做法一致；里程桩面为不可读笔画，不构成编造内容。
- **三项不符或无法核实，全部如实声明**：①**梯段在图中为一跑到顶**，未表现正文与断面 ZZY-2 的 14＋13＋13 三段与两处休息平台；②铜道钉在图中位于步道中部，正文口径为步道边缘（与短片同一处已声明差异）；③**画面只含近端**，故「两端均落地」无法由本图核实。**其中 ①③ 是对端核对清单里没有的，由本轮开图自查发现。**
- **`b3d-poster.jpg` 同类问题一并更正**：它两端的折返坡道构成悬空闭合环道、没有任何一段落到地面，而声明同样称「逐条一致」。它的角色是三维场景加载前的兜底图，理应与 `visual/assets/bridge3d.js` 一致，而三维几何本身是对的（6.0→3.0→0，逐值复算过）——**不一致的是兜底图，不是三维场景**。重新截取三维场景需要仓库外没有的截图工具链，本轮不做，改为如实声明并注明仅作加载占位。
- **顺着换图扫出四处沿用旧图特征的文字**：manifest 的中英标题与描述开头仍称「片区鸟瞰 / key-area aerial」；版权声明的图名标签同；展示页画廊图注写「本版为近正俯视航拍，平面形态可直接读出」。**更糟的是英文画廊的一致性说明还停在两版之前**——它描述的是「a helical ramp」，而螺旋那版早已被换掉两次。中英两侧的这段说明已各自重写，内容对齐。
- **引线锚点全部重定位并逐一验证**：`place()` 用 `data-ix/data-iy` 覆写行内 `left/top`，真正定位的是 data 属性。三条引线改到主跨桥面（78%,28%）、两段坡道之间的绿带（36%,42%）、坡道落地处（40%,76%），**把锚点画到新图上开图核过**，三处都落在预期物体上。英文页的 alt 文本原为中文，一并改为英文。
- 净减 50,176 字节（旧图 535,355 → 新图 485,179），尺寸与格式不变（1672×941 / JPEG），仅替换该文件路径，未新增文件。
- **把散在四处的可逆做法提炼成一段明确的实施立场，落在实施章节开头**（在 JZ 项目表与 PRE 门表之前）。一句话：**在条件明确之前就能开始，条件明确之后不用重来。**它与门表咬合——门表说「缺什么、谁去补、卡在第几步」，这一段说「门关上之前照样能开始，门关上之后不必推倒」。
- **不新增任何做法，每条都指回原处**，逐条核到行号：不做基础开挖／不浇混凝土基础／不接永久地下管线／配重基座／拆除后铺装可复原（176 行）；临时门扇试运行一个完整学年、**未作决定的自动恢复原状**（198 行）；最小可验证包三件互不依赖、任一可**单独撤回**（233 行）；布点按实测读数调整、不按规划图一次固定（233 行）；地面预埋可拆卸旗杆基座、封盖与铺装齐平（106 行）；北跃段第一步不依赖工程条件（300 行）；造价三档（247 行）。
- **三条自我约束照做**：造价三档的「**推断**」档位标注原样保留、未升格为结论；无「确保／高效／领先」这类词（零命中）；结尾保留正文原有的直白姿态——「本段最大的风险是把全部价值押在一座桥上，桥不落地这一段就什么都没有，那是设计的失败」，并写明可逆做法**不能**让第二步、第三步的内容凭空成立。
- 净增 16,834 字节 = 16.4 KiB，中英六个载体同步；未新增表，`make_visual_page.py` 的表数断言不受影响。

- **引线 B 指错了对象，已改指并列梯段。** 它原标「折返坡道 · 两段并排中夹绿带」，而该锚点落在两段坡道围合出的草地里——那里可见的是带铁轨的草地，宽度明显大于 1.0 米；**那条 1 米中夹绿带在这个机位被坡道遮挡、根本看不见**。指一个看不见的东西，就是又制造一次「说的和指的不是一个东西」。改指并列梯段（图上清晰、且是本轮写进正文的内容），锚点移至 (55.5, 45.0)，画到图上核过确实落在梯段上。
- **没有采纳"改标铁轨"的建议**：正文全篇只有一处提到铁轨，写的是「**铁轨让位给公园**」（老线腾退），**包内没有保留轨道遗存的主张**。用图注去标它等于靠注记引入正文没有的内容。
- **同一条自查也纠出我自己写的一句**：`copyright_statement.md` 里我原写「两段坡道之间的绿带内表现了既有铁轨遗存」——「既有…遗存」同样是正文没有的断言。改为「绿地内画有铁轨；**正文并未主张保留轨道遗存**，此处仅为场地语境的意象表达，不构成任何保留或复建主张」。
- 引线 C 由 (40.0, 76.0) 微调至 (34.0, 74.0)，落在钢梁结束、坡道到达地面的那一点；开图复核过。

- **未做**：`visual/index.en.html` 里 `render-dazhongsi-aerial.jpg` 的 alt 文本同样是中文（与本轮修的是同一类缺陷），但那是另一张图，超出本轮范围，记为已知项。

## v0.37 - 2026-08-30

- **把「主要依赖」重组成 11 道可追踪的前置条件门（PRE-01…PRE-11）。** JZ-01~08 的依赖列原本是一份被动清单——「文物部门协商」「权属梳理」「运营主体确定」——读者只知道我们缺什么，不知道谁去补、补到什么算补上、现在卡的是哪一步。现按门写：把关对象／开启触发／责任方／关闭条件／状态／阻断哪一步。
- **覆盖面是穷举映射出来的，不是挑出来的**：JZ-01~08 的 8 条依赖与北跃段 5 项前置逐条都有归属，没有一条落空，也没有一道门凭空长出来。门的内容全部取自本文既有材料——依赖列、302 行「缺一项不进入第二步」、94 行与 112 行的「补齐后能推进到的深度」与「核查未完成前不得定桩」、`assumptions.json` 的 A-* 条目——**不新增任何设计主张**。
- **最后一列写的是「阻断哪一步」而不是「是否阻断本轮投稿」。** 实情是十一道门无一阻断本轮投稿，若照后者写就是十一个「否」，等于自己给自己盖章。写清它卡在各自后面的哪一步（选点定桩／进入第二步／测试环线上线／数据链闭合），证明的是「知道每道门卡在哪」而不只是「现在没卡住」。分步依据是正文既有的三步实施与北跃段前置条件。
- **十一道门当前全部为 HOLD**，这是概念阶段的正确状态；正文同时写明本方案自始至终不把概念深化写成实施许可。
- **PRE-07 原本准备砍掉**——起初判断包内没有依据说明「轨道与市政管线资料由谁提供、拿到什么算拿到」，硬写关闭条件就是编。实查后发现依据比预期强：正文 112 行明写「基础埋深与位置须先完成站域地下轨道结构与市政管线的避让核查，**核查未完成前不得定桩**」，94 行给出关闭后的深度（钟架基础可进入结构选型）。故保留，11 道门成立。
- **交付前自查两项**：① 每道门的「关闭条件」单独读一遍，主语必须是资料或凭证（取得／核定／完成／建立），承诺性主语（「我们将」「本方案负责」）零命中；② 门表与 `risk.json` 的 8 维度不重复——风险是风险，前置条件是前置条件。
- 落正文第十章 JZ 表之后，不新建文件（新建 JSON 会进 manifest 哈希链，还要多维护一套一致性判据）。实测净增 39,609 字节 = 38.7 KiB，六个载体中英同步，未动 `narrative.mp4` 与任何 render。

## v0.36 - 2026-08-30

- **`manifest.json` 说「三处」却只列两条**，漏的正是 v0.35 新加的并列梯段那条。这是同一文件、同一类错误**连续第二次**（v0.34 那次是「仅存一处」没跟成两处）。
- **根因不在这次漏抄，在结构**：同一份差异清单在 `narrative-transcript.md` 与 `manifest.json` 各写一遍，而 manifest 那份是叙述式的（用「以及」把条目串起来），加一条就要重构句子，于是必然漏。**已把 manifest 改为不再重复列举、也不再重复计数，只指向权威载体**：「已声明差异逐条见 `assets/media/narrative-transcript.md`，该文件是这份清单的唯一权威来源」。清单只存一份，计数只存一处。
- **新增判据 4c**：从权威清单里数出实际条数（总条数减去标注「已重绘消除」的），与文字稿中英标题、两个展示页四处写出的计数逐一比对；同时禁止 manifest 重新长出自己的清单。三个方向故意破坏均报警：改小展示页计数、往清单加一条而不改计数、让 manifest 重新列举。
- **判据里的一处措辞缺陷也一并修了**：中文数字表和正则都漏了「两」，把「两处」读成「读不出条数」——仍会 FAIL，但报错指错了方向。补进去之后报的是「写的是 2 处，而权威清单实际现存 3 条」。
- **设计说明进图纸**：`关沟／Guangou` 此前在四份 PDF 里 0 命中——那句最有力的设计说明只在正文与网页上，而评审 pipeline 只截 PDF。已加到 A3 第 12 页（人字之跃专页）。**没有塞进 A0 第 1 页总体结构板**——一座桥的设计说明放在总体结构板上会失衡。**A0 第 5 页的人字之跃卡试加后又撤回**：判据是「不挤占既有内容才加」，而实测它确实挤——该卡原为 3 条 6 行，加进说明后首条变 5 行、总 9 行，条间距被压到与同板另两张卡明显不齐（并进已有条目、两次缩写都没能解决，根因是卡高固定而这一列内容最多）。按同一条判据，A0 维持原状。
- **版面审计漏报了一次真碰撞，阈值已修。** 加了这条要点后英文 A3 第 12 页末行压在断面图上 7.7pt，而审计报「无越界／无碰撞」。**原因是重叠阈值按整条要点的高度取 35%——一条 6 行的要点只有末行压图时，7.7pt/28pt=18% 被稀释到门槛以下。** 文字框已先按 15% 收过边，残留重叠超过约 1.7pt 就是真碰撞，改用绝对阈值 0.0020。改完后判据当场抓住了它先前放过的那次，随后修版面（图纸条目精简、要点间距 0.040→0.035）至净空 13.3pt、审计通过。
- **「同层并排」这个措辞在最自然的读法下是假的，已改。** 效果图与短片的形式核对里写着「两段坡道**同层并排**」，英文写作 "two runs sit side by side **on the same level**"。而 `bridge3d.js` 的标高链是：上行段 6.0→3.0、折返平台 3.0、回折段 3.0→0——**两段坡道在任一横断面上都不同高**。`on the same level` 的首选读法就是「同一标高」，那句声明因此说的与实物不符。正文自己的口径本来就是精确的「在平面上并排展开、不上下叠置」，7 处（中文 5 / 英文 2）统一到正文口径。这是「声明与实物不符」那一类，不是措辞偏好。
- 顺带：图纸条目里的「梯段供一般步行、坡道供轮椅与低速机器人」精简掉了，这条分工留在正文，图纸只给尺寸。中英同步。

## v0.35 - 2026-08-29

- **天桥补设梯段。** 原竖向链条只有折返坡道与电梯：`楼梯／踏步／梯段／stair` 全包出现 0 次。而方案自己在钟声广场界面立过「设轮椅坡道…**并同步设台阶**，不允许出现只有台阶的入口」——**天桥把这条原则只贯彻了一半**。补梯段是贯彻既有原则，不是新增设计意图。
  参数：踢面 0.15／踏面 0.30 量级，40 级分 14＋13＋13、2 处进深 1.5 米休息平台，净宽 3.0 米。**踏面数是 37 级不是 40**（每段最后一级踏在平台上），故水平投影 37×0.30＋2×1.5＝**14.1 米**，单侧另占 14.1×3.0＝**42 平方米**。踏步取值一律写「按现行规范由专业团队核定」，**不标条号**——没查证的条号编出去就是新的硬伤。
- **占地口径同步改准**：原文只写「单侧占地约66米×9米」，加梯段后若不改，这个数就在冒充全部占地。现写作坡道 594 ㎡＋梯段 42 ㎡＝**单侧约 636 ㎡**，图纸与断面同步。
- **一个被提议但算死不成立的做法**：曾有意见主张把梯段塞进「U 形折返围出的空当」、零额外占地。**包里没有这个空当**——断面是坡道 4.0｜分隔绿带 1.0｜坡道 4.0＝9.0 米，两段之间那 1.0 米就是绿带本身，3.0 米梯段塞不进 1.0 米；挤掉绿带仍差 2.0 米（要 4.0＋3.0＋4.0＝11.0 米，即单侧多占 132 ㎡）；压窄坡道到 3.0 米则违反「扶手内侧净宽不小于3.6米」，而 4.0 米是由三个数倒推的。故仍按「桥头平台一侧、与折返坡道并列」布置。
- **设计说明断链已接上。** 正文第 21 行讲了「人」字的来历（关沟陡坡、折返换向、两段缓坡替代一段陡坡），第 257 行讲天桥却从不说这个折返从哪来，两处相隔 236 行，读者自己连不起来，「人字之跃」于是像个硬安的名字。补一句把两处接住：**同一个动作，一次是为了让火车上山，一次是为了让轮椅上桥。**补的是「为什么这么做」，不含赞美性形容词，也守住 257 行自己立的「这个「人」字自始至终是几何事实而不是治理隐喻」。
- **补梯段的连带后果：既有的桥效果图与短片里都没有梯段**，而 `report/copyright_statement.md` 写着 `b3d-poster.jpg` 与 `render-ren-bridge.jpg`「经逐条比对与正文一致」——**加了梯段之后这句话就不成立了**。两处形式核对已注明所核对的是补设梯段之前的正文口径；短片差异清单加第 3 条，四个载体（文字稿中英段、两个展示页、manifest）同步为「现存三处、另一条已重绘消除」。
- **`bridge3d.js:108` 的注释里躺着已撤回表述**「俯视合读为「人」」。它是 manifest 声明的包内文件，而黑名单判据只扫 md/html/json——**「包内文件」不等于「被扫过的文件」**。已改写注释，并把扫描面扩到 `.js/.css/.svg/.vtt/.csv`，故意破坏验过。
- **manifest.json 还写着短片「仅存一处已声明差异」**，是 v0.34 我把敲钟资格那条加进文字稿和展示页时漏的一处。已改。
- **不写进正文的东西**：加梯段的决策依据里有一套绕行系数算术（120＋60＋120＝300 米、系数 6.7）。**三个跨度数字互不一致**——`bridge3d.js` 里 `ROAD_W = 60`（路幅）、`SPAN = ROAD_W + 16 = 76`（桥面跨度），另有口头估的 45 米路幅；而正文第 304 行明写主跨分跨要等横断面资料补齐后才可推算、第 261 行明写中间支承「本方案不作结论」。**方案本来就没有主跨长度这个数，也不该有**，故绕行系数一个字不写进交付物。

## v0.34 - 2026-08-29

- **敲钟资格在正文里原有两套口径，已统一。** 窄口径穷举「备案或IPO」两项，宽口径写在可审计叙事指标那节——「钟声数 = 可公开核验的企业里程碑事件数」，且冬季活动那节明写「全年常态受理」。窄口径与方案自身三处冲突：方案说「备案即钉」，钉了却不许响；发布新模型要钉一颗钉却不能敲钟；IPO 是极低频事件，照窄口径这口钟可能一年响不了一次，与「全年常态受理」直接矛盾。**统一到方案自己已经写在指标那节的宽口径**，不新增机制、不改判据：敲钟资格与可公开核验的企业里程碑事件绑定，事件类型开放，但须有政府、交易所或其他第三方的公开可查记录。三条铁律（只刻事实不刻排名、只记事件不背书主体、与主体存续脱钩）与可核验判据一字未动。
- **实际改动 29 处、7 个文件**，中英对称：`proposal.md`(3)、`proposal.en.md`(4)、`report/proposal.html`(3)、`report/proposal.en.html`(4)、`visual/index.html`(7)、`visual/index.en.html`(7)、`assets/media/answer-bell-transcript.md`(1)。其中包含一处**音频小节标题**「应答钟：备案或上市，敲一响」——标题也是口径。人才转化路径末端的「鸣钟（IPO与里程碑）」同步改为「鸣钟（可公开核验的里程碑事件）」；同一句里「学生—创业者—备案企业—上市公司」的成长曲线保留，那里的「上市公司」是职业路径终点、不是敲钟资格限定。
- **短片字幕三份载体刻意未改，并加了据实声明。** `narrative-transcript.md` 自己写着「字幕已烧录进画面，`narrative.vtt` 与烧录字幕逐字一致」，`narrative.mp4` 本轮不重新渲染。**改文字稿会制造一个新的不一致（文字稿↔画面）去掩盖旧的（字幕↔正文），缺陷数量不变而可核验性变差。**故在文字稿既有的「已知形式差异」清单加第 3 条：片中字幕举的是最常见两类事件，正文口径为开放事件类型，一律以正文为准；该清单的计数由「两处」同步改为「三处」。
- **改完后对这 29 处做了一轮五视角对抗核查（每条发现另派人证伪），抓出 10 条确证问题，其中 4 条 P0 是我自己的漏。**
- **P0：四份 `required: true` 的正式图纸 PDF 里全是窄口径**，本轮差点原样交出去——
  `drawings/a3-booklet.pdf` p10「敲钟机制：带内企业完成备案或IPO」、`a3-booklet.en.pdf` p10、
  `a0-boards.pdf` p5「新铸应答钟：备案/IPO敲一响」、`a0-boards.en.pdf` p5。
  **漏掉的原因是判据本身的盲区：PDF 文字在压缩流里，纯文本 grep 看不见**；A0 那处又写成斜杠式「备案/IPO」，
  即便能搜 PDF，按「备案或IPO / 备案与IPO」这类变体清单去搜也照样漏。已改 `build-staging/make_drawings.py`
  四处源字符串并重渲染，版面审计仍「无越界 / 无碰撞」，压缩后四份净减 16,819 字节。
- **展示页的「已知形式差异」块不止计数不对，内容本身已过期**：中英两版都还在声明「片中人字之跃表现为下承式桁架并带端部门架」，
  而短片早已按断面 ZZY-1／ZZY-2 重绘、该差异已消除（文字稿里写着，页面没跟）。两版一并改写为当前真实的两条
  （道钉位置 + 敲钟资格表述），并注明桥型差异已重绘消除。
- **短片文字稿的英文 `Declared divergence` 段此前只声明了道钉一条**，中文侧加了敲钟资格那条、英文侧没加——
  英文读者读到窄口径字幕却拿不到任何提示。已补齐，两侧等义。
- **中英摘要口径不对称**：英文本轮改成 "every **publicly verifiable** milestone"，中文摘要一直写「每个里程碑」，
  改后中文反而比英文松。已补为「每个可公开核验的里程碑」。
- **本轮新口径引爆了一处既有的潜伏矛盾**：三类可审计叙事指标承诺「全部锚定政府既有公开口径」，
  但钟声数在本轮之前就已经是「可公开核验的企业里程碑事件数」，而 IPO 是交易所事件、不是政府事件——
  **矛盾一直在，只是写明「交易所或其他第三方」之后才显形**。把那个不准确的限定词改为「全部锚定既有的外部公开口径」，
  载重的半句「不依赖本方案自建的统计体系」原样保留；中英合计 9 处同步。
- 另有 3 条候选被证伪、未改，其中包括「上钉→鸣钟的箭头先后关系不再成立」。

## v0.33 - 2026-08-29

- **`report/proposal.html` 与 `proposal.en.html` 的首屏标题重复已消除。** 根因：front-matter 的 `title:` 与正文首个 `# ` 同文，渲染器把两者都输出，于是甲方 1440×1600 的评审首屏里同一个标题占了两次黄金位置（同批 PR #4152 正因「大标题=小标题同串重复」被扣表达分）。**渲染器 `scripts/render_proposal_html.py` 属甲方仓库工具、投稿 PR 明令不得修改**（PR 模板：本 PR 不修改 `.github/`、`brief/`、`schema/`、`scripts/`），故改我们自己的四个文件。
  - `proposal.md` / `proposal.en.md` 各删一行（与 title 同文的正文 H1）；`report/proposal.html` / `.en.html` 各删 hero 之后那一个 `<h1>`。**是手改产物、不是重新生成**——重新生成会冲掉为修复方框而加进 HTML 的字体 `<link>`；源与产物改的是同一处，两边不脱节。
  - 自查：两个 md 各只有 1 行改动且就是那行 H1；`##` 章节数 13→13、13 项必需章节缺 0；中英各 −1 行、位置对应；五类证据锚点 `[source:]/[standard:]/[depth:]/[data:]/[metric:]` 改前改后均为 **237 个、分布完全相同**（87/29/47/59/15）。渲染后两页 `<h1>` 各剩 1 个。
- **`visual/index.en.html` 首屏引言的孤字换行已消除。** 实测各行墨迹宽 `[617, 74, 629, 106]`——第 2、4 行只有一个词和两个词。**先试 `text-wrap:balance`，无效**：`.q` 内有 `<br>` 强制换行，balance 跨不过去（该死代码未留在包里）。改为只把英文页 `.q` 的 `max-width` 由 30em 调到 36em，令两句各占一行；**中文页保持 30em**（实测 `[576, 479]` 本就均衡，加宽反会拆坏）。现英文 `[697, 741]`，与中文版同为两行。
- **甲方 2026-08-29 12:48 评审的唯一阻断项已修：`design_depth_matrix.json` 的「公共空间约3.9%」。** metrics.json 的 `public_space_ratio = 0.0193`，公式 `round(public_space_area_sqm / site_area_sqm, 6)`，实算 220,269.135 / 11,412,825.386 = 0.0193；metrics 里只有这一个公共空间比例指标，正文出现 1.93 三次、3.9% 零次——**是孤例错误，不是另一口径**，故按评审要求改为 1.93%，未另设新指标。同句的「绿地占提交范围约25%」一并写准为 25.04%（metrics `green_ratio = 0.250356`）。该句在展示页中英两版各有一份渲染副本，同步改正，共 3 处。
- **顺着这条自查，又找出三处同类问题——都是「只改了 JSON、没同步展示页」**：
  - 展示页中英两版的 `metrics_recalculation` 证据摘要仍写「公共空间比例约**3.93%**」。`design_depth_matrix.json` 里这一处早在 v0.15 就已改成 1.93%（changelog 有记录），**但页面那份渲染副本一直没跟上**，于是 JSON 说 1.93%、页面说 3.93%。已同步。
  - **`design_depth_matrix.json`（13 处）与 `standard_matrix.json`（7 处）的 `self_check_ids` 仍是那批悬空 ID**（`BOUNDARY_TRUST` / `KEY_AREAS_TRUST` / `VISUAL_STATIC` / `LAND_USE_TOPOLOGY`）。v0.28 只修了 `compliance_matrix.json`，**当时的判据也只扫了 compliance_matrix**，于是另外两个矩阵原样留着且全绿。已按同一映射改指真实检查。
  - 展示页中英两版各有 **41 处**旧 ID 的渲染副本（已核实全部落在「自检」块内），同步改正 82 处。
- **两处判据的覆盖面按「手上那个例子」划，已扩**：
  - `check_drift.py` 第 6b 项由只扫 `compliance_matrix.json` 扩到三个矩阵；新增第 6d 项——展示页「自检」块里渲染的 ID 也必须能在 `self_check.json` 解析到（**JSON 与页面各说各话就是这么留下的**）。
  - `check_numbers.py` 的 `DOCS` 此前**根本不含三个矩阵与两个 report HTML**，甲方抓到的 3.9% 正是从这个缺口过去的；现已纳入，并新增「比例指标各产物数值必须等于 metrics.json」判据。为避免把矩阵拉进「必须出现该数字」类判据造成误报（矩阵本就不报场地面积与 122），另切出 `NARRATIVE_DOCS` 供存在性判据使用。判据放过两类合法写法：以整数作上界的行文（正文「公共空间率却不到2%」）与 changelog 的历史记述。
  - 四条新判据全部以故意破坏验证过会 FAIL、还原后 PASS。
- **全包复扫**：`public_space_ratio` 与 `green_ratio` 在 32 个文本文件中的全部出现均已一致，未解释的不符 0 处。
- **本轮未做**（推迟）：A0 右下角缩略图 72 DPI 不可读、A0 中部留白、A3 底部小字——三项都要重跑 `make_drawings.py` 并重过整套版面审计，包体余量也不足，风险不成比例。
- **复验**：四门 PASS · 漂移 PASS · 跨产物数字 PASS。

## v0.32 - 2026-08-29

- **修复「声明与实况不符」：版权声明与 sources.json 里写的字体文件在包内不存在。** 上一轮因包规则不允许字体扩展名，字体交付形态从独立的 `.woff` 改成了「WOFF2 经 `data:` URI 内嵌于 `.css`」，但两处声明还停在旧方案上，写的是 `visual/assets/fonts/NotoSansSC-subset.woff`（约 210 KB）——**大小写、扩展名、形态三者都与包内实况不同，且该文件在包里根本不存在**。甲方对同批投稿的修复要求原话即「使可见成果与包内声明一致」，这类不符打的是风险合规与表达完整度两维。
  - 两处均已改准：路径与文件名 `visual/assets/fonts/notosanssc-subset.css`；形态「WOFF2 子集经 `data:` URI 内嵌于该 CSS，因 `visual/assets/**` 仅收 .css/.js/.json/.svg/图片、`assets/**` 仅收图片」；体积「CSS 223,958 字节（约 218.7 KB），其中内嵌 WOFF2 解码后 167,488 字节（约 163.6 KB）」。其余（OFL 1.1、版权取自 name 表、无 Reserved Font Name、1551 码位、族名改名理由、四个 HTML 的用途）本就准确，未动。
  - **`sources.json` 那一处是本轮 grep 出来的，此前未被报告** —— 只修被点名的那一处就会漏掉它。
- **固化为判据（`check_drift.py` 第 6c 项，位于包外的构建目录、不进投稿 diff）**：`report/copyright_statement.md`、`sources.json`、`manifest.json` 里点名的包内路径必须真实存在。
  - **这条判据自身出过两次错，都是靠故意破坏验出来的**：第一版正则末尾混进了一个真的退格字符（0x08），导致它对任何输入都不匹配——**一个永远不会失败的检查比没有检查更糟**；第二版又把 `brief/site-package/geometry/…` 这类甲方侧长路径的尾段误当成本包文件报缺失，已加前置约束修正。现已验证：正常态 PASS、把声明改成不存在的文件名即 FAIL、还原后再 PASS。
- **本轮只动了这些**：`report/copyright_statement.md`、`sources.json`、`changelog.md`，以及门禁自动刷新的 `manifest.json`、`self_check.json`。**排版类改动（proposal 首屏标题重复、英文孤字换行）已完成并验证，但按用户「先提交再做 B」的顺序，另置于后一个提交，不混入本次。**
- **复验**：四门 PASS · 漂移 PASS · 跨产物数字 PASS。

## v0.30 - 2026-08-29

- **修复甲方评审 PR #4151 的唯一阻断项：四个必需 HTML 的中文在无中文字体环境下显示为方框。** 评审在离线容器里渲染截图，标题与正文中文大量成豆腐块，表达完整度因此扣到 3/5。**根因是包里根本没带字体**——实测四个 HTML 的 `@font-face` 数为 0、包内字体文件为 0，字体栈全部只指向系统字体（`PingFang SC` / `Microsoft YaHei` / `Noto Sans SC` 等）。本机 Windows 装着雅黑所以永远正常，这正是「只在会成功的环境里验证过」。
- **随包字体：Noto Sans SC 子集，SIL OFL 1.1。** 取自本机既有的开源字体文件（**未联网下载**），版权与许可字段直接读自字体自身的 name 表（版权 © 2014, 2015 Adobe Systems Incorporated；nameID 13/14 载明 OFL 1.1 与 http://scripts.sil.org/OFL ）。该字体**未声明保留字体名**，故子集化与改名为许可允许；按四个 HTML 的实际用字子集化为 **1551 个码位**（1433 个 CJK／全角 ＋ 90 个 ASCII ＋ 28 个其它，源字体覆盖率 1551/1551、无缺字），族名改为「NotoSansSC Subset」以便与系统同名字体区分、并可验证页面用的确实是随包字体。
- **交付形态被包规则逼出一个转折**：字体最初以 `.woff` 放在 `visual/assets/fonts/`，**确定性校验直接拒绝**——`visual/assets/**` 只允许 `.css/.js/.json/.svg/.png/.jpg/.jpeg/.webp`，`assets/**` 只允许图片，**包内任何目录都不允许字体扩展名**。故改为 WOFF2 以 `data:` URI 内嵌进 `visual/assets/fonts/notosanssc-subset.css`（`.css` 在白名单内），四个 HTML 以 `<link>` 引用同一份，字体数据只存一份而非四份。
- **字体栈按用途分别处理，而不是一律前置**：sans 栈前置我方字体（保证各环境渲染一致）；**mono 栈追加在末尾 `monospace` 之前**——西文保持等宽，只有系统字体供不出的中文才落到我方字体；`--serif` 同理追加。共改写 16 处声明与 3 个 CSS 变量。
- **验证（甲方明确要求在无中文字体环境下复核，本机无 Docker／WSL 发行版，故用等价的强制回退法）**：把四个页面的所有字体栈改成**只剩我方字体、不留任何后备**，此时每个字都必须由随包字体供给，供不出就会现原形。结果：**726 个含中文的元素，非我方字体供给的字形 0 个**，四页字体状态均为 `loaded`，截图目视无方框。真实页面（保留系统字体后备）四页亦为 `loaded`。
- **登记**：`sources.json` 新增 `FONT-NOTOSANSSC-OFL`；`report/copyright_statement.md` 中并入「第三方开源组件声明」与 three.js 并列（**不是**放进「AI 生成内容声明」——字体不是 AI 生成物），并把原「除此之外本包不含任何其他第三方代码库」改为「除 three.js 与上述字体外」；`manifest.json` 补登该 CSS（生成器未自动收集，需手工补，否则是磁盘有、声明无）。
- **本轮不动内容**：几何、指标、provisional 声明、`investment_estimate_cny=unknown` 全部保持——评审对这些给的是满分，且原文标注「当前阻断：否」。
- **复验**：四门 PASS · 漂移 PASS · 跨产物数字 PASS · 展示页体检全部通过 · manifest 98 条哈希 0 不符 · 包体 39.88 MiB / 40。

## v0.29 - 2026-08-28

- **修好两处我自己在上一轮改出来的毛病**——都是「插入一段之后没有回头把整句读一遍」造成的，与本轮一直在查的那类缺陷同族：
  - `report/copyright_statement.md` 第 44 行原句结构是「Lib Image 两张：A（说明）、B（说明）。」，上一轮把 A 的括注换成一整段署名缺口说明后，**B 被挂到了那段的句尾**，读起来成了「……此项需重新确认参考图来源后方可继续使用、`render-spike-belt.jpg`（道钉带日景）。」。已改为两张先并列成句，署名缺口另起一段。
  - 同一文件里「**两张**前版已判为不采用并整条替换」的小标题，在补记 `render-ren-bridge` 第二次替换之后就与正文对不上了——正文实际描述的是三处（同一文件先后两版 ＋ `render-answer-gate` 一版）。已改为「前版判为不采用共三处（两处为同一文件的先后两版、已整条替换且不随包提交；一处仍随包但仅作留档）」。
- **顺带把该文件所有数量词与实际对了一遍**：Lib Image 声明两张、枚举 2 个；GPT image 2.0 声明九张、枚举 9 个；合计 11，与 `visual/assets/renders/` 磁盘实际 11 个文件**集合完全相同**（不只是数目相同）。两处「十一张」的总述亦与之一致。
- **复验**：四门 PASS、漂移 PASS、跨产物数字 PASS。

## v0.28 - 2026-08-28

- **展示页四组引线的圆点全部改为按图像坐标定位，并修好两个此前无法根治的成因**。用户看到的「黄色的线漂移了、和圆点对不上」不是单纯的坐标标错，底下有两件事：
  - **① 圆点不跟着入场动画走。** `#ovx .plate .ph img` 进场时 `transform` 从 `scale(1.05)` 动画到 `scale(1)`，历时 2.6 秒，而圆点用的是容器百分比、原地不动——那 2.6 秒里图在圆点底下滑，**这就是字面意义上的「漂移」**。
  - **② 容器百分比在不同视口指向图上不同位置。** `.ph img` 是 `object-fit:cover`，裁掉多少随视口比例变；在一个视口上量准的百分比，换个视口就偏了。
  - 现给每个圆点加 `data-ix` / `data-iy`（**原图百分比**），由脚本用 `img.getBoundingClientRect()`（已含 transform）反算 cover 后的内容框再映射，逐帧跟随。行内百分比保留为脚本失效时的兜底。
- **自愈式循环替换掉了会卡死的记账式循环**。第一版用 IntersectionObserver 维护一个 `live` 数组决定更新谁；实测大钟寺那块的圆点**只被改写 3 帧就停住**，停在 `scale(1.05)` 时算出的位置上，留下约 1% 的固定错位——记账一旦与实际可见状态脱节，循环就再也起不来。改为每帧自己按 `getBoundingClientRect()` 判断该更新哪块、由 scroll/resize 唤起，没有这个失效模式。
- **12 个圆点逐点重定位并逐点验证落在实物上**。旧位置里「折返坡道 · 两段并排」落在机动车道、「落在环路两侧绿地内」落在中央隔离带、「13 号线高架」落在天空、「觉生寺 · 一砖不动」与「钟声广场」压在标题与正文上。新位置在原图网格上逐点读取后，关掉引线截纯图、每点裁 280×230 放大确认。
- **判据从「离文字块外接矩形」升级为「离真实字形」**。用外接矩形判会把段落右侧的参差空白也算成文字，导致误判；改为对 `.txt` 内文本节点取 `Range.getClientRects()` 逐行求最近距离。据此在 5 个视口 × 中英两版共 8-10 组组合下为「觉生寺」逐点比选，最终取寺院东院（图像坐标 38%,34%）——**唯一在所有组合下都不压字的位置**，最小净距 13.8px。
- **复验**：中英两版 × 1280×800 / 1366×768 / 1440×900 / 1680×1050 / 1920×1080 共 10 组，每组 12 条引线，**映射偏差 0、压字 0、出图 0、JS 错误 0**。

- **版权声明张数自相矛盾已改**：正文两处写「七张」，而枚举是 Lib Image 两张 + GPT image 2.0 九张 = 11 张，目录里也正好 11 张。两处均改为「十一张」。
- **`render-bell-plaza.jpg` 的 CC 参考图署名缺口据实写明**：该图以一张 Wikimedia Commons 的公开授权大钟寺照片为参考图生成，但**具体文件名、作者与许可版本在生成当时未被记录，包内亦无留档**，因此无法给出 CC 系列要求的完整署名。已在版权声明中写明这一缺口、说明产出物为文本提示词生成而非该照片的复制/裁切/改编，并注明若主办方要求完整署名则此项需重新确认来源。**未编造署名。**
- **`compliance_matrix.json` 的 21 处悬空引用已改指真实检查 ID**：`self_check_ids` 引用的 5 个 ID 里有 4 个在 `self_check.json` 中解析不到（`BOUNDARY_TRUST` / `KEY_AREAS_TRUST` / `LAND_USE_TOPOLOGY` / `VISUAL_STATIC`），波及 23 条 requirement 中的 18 条。逐条核对来源后改指：临时边界警告实际由 `DETERMINISTIC_VALIDATION` 发出；`KEY_AREA_PROVISIONAL` 这个 ID 真实存在于 `spatial_issue_ids`；几何/拓扑归 `SPATIAL_REVIEW`；`visual_review.py` 那道门的 ID 是 `VISUAL_PACKAGING`。**没有往 `self_check.json` 里手工添加条目**——那是自检工具生成的文件，手工加进去等于声称跑过一个没跑过的检查。
- **新增判据：跨文件 ID 引用必须解析得到**（`check_drift.py` 第 6b 项）。甲方的 `validate_compliance_matrix_file` 只校验这些数组「非空」、不校验里面的 ID 能否解析，是典型的「机器门看形状、不看内容」；四门全绿而 18 条 requirement 的自检引用是悬空的，就是这么来的。判据一并覆盖 `source_ids` / `assumption_ids` / `standard_ids`（这三支实测干净），并以改名验证过会报警。

## v0.27 - 2026-08-28

- **`render-ren-bridge.jpg` 画的不是本方案那座桥，且画廊说明声称它已核对过**。这是北跃整节的主视觉。旧图两端画的是**封闭矩形环道**——绕圈上升，**没有「折返」这个动作**（折返＝走到端头转 180° 沿并排的另一段走回来）；环道尺度也远超正文的「单侧占地 66×9 米」。正文对此有明确排除：「每侧只设一处折返，两段坡道在平面上并排展开、不上下叠置，因而不产生坡道之间的净空冲突，**也不出现多层盘旋**」。
  - **比未声明的差异更糟的是**：该图的画廊说明写着「本版已按断面 ZZY-1／ZZY-2 重做：……两段并排展开、端头折返、不上下叠置」——**声称核对过、实际没有**。同屏正文还写着「折为并排两段各 62 米……单侧占地 66×9 米」，文字全对、图全错。
  - 现换为近正俯视航拍版（AI 生成，2026-08-28），逐条复核：两端各一处折返、两段同层并排且中夹绿带、方向相反并在远端相接、**无环无盘旋**、一跨平直、桥面之上除栏板与照明外无构筑物、两端均落在环路两侧绿地内、尺度回到与跨路段相当。这个角度顺带把平面形态摊开，「俯视不构成人字轮廓」直接可读。
  - 版权声明中记录了该图**先后两次整条替换**及各自的不符原因（初版下承式桁架桥；第二版矩形环道），两个旧版文件均不随包提交。AI 生成图计数不变——该文件本就在 image 2.0 清单内，替换内容不新增文件。
- **三条引线圆点全部标错，已按渲染实测重标**。圆点是手填的百分比，且 `.plate .ph img` 为 `object-fit:cover`，**不能在原图上量、必须在渲染出来的页面上量**。旧位置：「折返坡道·两段并排」落在**机动车道**上，「落在环路两侧绿地内」落在**中央隔离带**上。现按 1440×900 实测重标，并关掉引线后逐点裁图放大核对落点：桥面那点在桥面上（两侧仅栏板）、折返那点正落在**两段并排之间的绿带**上、落绿地那点在坡道落地旁的绿化里。
- **顺着这条线把 11 张效果图对着正文的形态主张过了一遍**（这类矛盾今天已证明不止一处，而且判据一旦只盯被点名的那一张，同类对象就自动豁免）：
  - `render-answer-origin` —— **判为一致**，早先设计评审怀疑「零公里桩与应答簿做成了带基座的纪念碑与石书，与『以钉代碑、不立高碑』打架」，开图核后不成立：广场确实空旷（无花坛、无树阵、无雕塑群），画面上正好只有两件东西（一根桩、一册簿），两者都低矮，不构成高碑，与页面标题「站前只放两件东西」对得上。
  - `render-bell-plaza` —— **判为一致**：同心声环铺装在；钟架为独立铆接钢架、不依附文物；新铸应答钟与背景的觉生寺钟楼是两个分开的对象，「绝不敲击 1733 年文物古钟」成立。
  - 其余各张的 `alt` 形态主张（机器人停在门槛之外／课门旁听席在墙外／公示墙的实体关闭键等）与画面一致，未见与正文冲突。
- **复验**：四门 PASS、漂移 PASS、跨产物数字 PASS、展示页体检全部通过、包体 39.65 MiB。

## v0.26 - 2026-08-28

- **效果图换为 v3，上一版的两处形式不符已消除，相应的差异声明同步撤回**。v0.25 进包的是 v1，我核出两处与正文不符（折返坡道上下叠置；右端不止一处折返），当时按包内先例把差异声明在中英 `alt`、`manifest.json`、`report/copyright_statement.md` 三处。现换为 v3 并逐条复核：
  - **两段坡道同层并排、中夹绿带、行进方向相反并在远端相接** ✅ 与正文「两段坡道在平面上并排展开、不上下叠置」一致；局部放大可见两段之间确有绿化带，与三维模型的 `GAP = 1` 中夹绿带对应。
  - **每端恰好一处折返，无多层盘旋** ✅ 与正文一致。
  - 一跨平直主跨、桥面之上除栏板与照明外无构筑物、两端均落绿地、桥墩 V 形支撑、跨越对象只有环路 ✅ 与 v1 同等。
  - **三处差异声明已全部撤回并改写为「形式核对一致」**——图既已与正文相符，留着一条「已知差异」就变成一处新的产物与自述不符。AI 生成声明与「非实景照片／非工程效果图／非官方审定成果」边界保留不变。
- **压缩档位自测后选 q90**（523 KB）：对源 PNG 平均差 3.02/255、PSNR 35.5 dB，并在栏杆（全图最细密的线条）处 2× 放大目视无块状伪影。相对 v1 的 q92 **净减 76 KB**，包体 39.74 → 39.67 MiB，余量回到 343 KB。
- **复验**：四门 PASS、漂移 PASS、跨产物数字 PASS、展示页体检全部通过。

## v0.25 - 2026-08-28

- **三维静态图换为 AI 生成的概念意象效果图**（GPT image 2.0，2026-08-28）。上一版是确定性脚本绘制的正投影技术图；本轮按「效果优先」替换为写实概念意象，用作展示页三维场景加载前的静态图与浏览器不支持 WebGL 时的兜底图。图上无文字，**中英两版共用同一文件**（`language: "neutral"`），原 `b3d-poster.en.jpg` 已随之删除，manifest 由 98 条回到 97 条。
  - **经逐项开图核对与正文口径相符的部分**：一跨平直主跨、不拱起不折线；桥面之上除栏板与照明外无构筑物；两端以折返坡道落回道路两侧绿地；桥墩落地、V 形支撑；耐候钢色与道钉带的铜相呼应；跨越对象只有环路。
  - **已知形式差异（据实声明，三处产物同时标注）**：图中两端折返坡道为**上下叠置**，而正文口径是「两段坡道在平面上并排展开、不上下叠置，每侧只设一处折返」；正文对这两条是给了理由的（避免坡道间净空冲突、不做多层盘旋）。差异已写入图片 `alt`（中英）、`manifest.json` 描述与 `report/copyright_statement.md`，写法沿用包内既有先例——短片字幕轨 `narrative.vtt` 对「铜道钉位置」与「人字之跃桥型」两处差异就是这么声明的。**形式一律以断面 ZZY-1／ZZY-2 与正文为准。图本身的调整排在提交之后。**
  - 版权声明中该图由「非 AI 生成的随包图像」移入「GPT image 2.0 八张」并计为九张，附生成日期、用途与「非实景照片／非工程效果图／非官方审定成果」边界。
- **补入一个此前任何产物都没写出来过的数**：按三维场景的构造几何反算，坡道纵坡约 **1:20.7**（3.0 米 / 62 米），满足正文「不陡于 1:20」的控制意向。这个数是上一轮为画正投影图而反算几何时得出的副产品，本身就是一次跨产物一致性核验——展示页 `.dims3` 标注的五个数、`bridge3d.js` 的实际几何、正文的控制意向三者闭合。已写入展示页两语言正文。
- **判据补一处盲区**：`check_drift.py` 第 6 项原先只比对 `render-*` 命名的图，而本轮进包的 AI 图叫 `b3d-poster.jpg`，旧判据对它完全不设防；现改为覆盖 `visual/assets/renders/` 下所有 jpg。反向一支（声明列出但包内不存在）改为对全包比对——声明里也会提到 `assets/media/` 下的 `cover.jpg` 等，拿 renders 的清单去判它们「不存在」是判据用错了地方。两支均以改名验证过会报警。
- **舞台比例**由 1742/1108 改为 16/9 以匹配新图；两条路径复验：无 WebGL 时两语言均显示该图、`role=img`、操作提示与 `tabindex` 已移除；有 WebGL 时画布起来、静态图隐藏。
- **复验**：四门 PASS、漂移 PASS、跨产物数字 PASS、包体 39.74 MiB。

## v0.24 - 2026-08-28

- **三维静态图改为平面／立面／纵剖三格正投影**。上一版把它套进图签版式是对的方向，但没解决根本问题：**一张斜俯视的体块透视图说明不了形态**。透视擅长的是「站在那儿什么感觉」；而这张图要传达的四件事各有各该用的投影——两段坡道并排（平面）、6.0／1.4／9 三个尺寸（立面）、竖向链条六步（纵剖）、一跨平直且桥面之上只有栏板（立面）。上一版是**用透视图＋一圈文字去解释画面里看不出来的东西**，这一版让画面自己说。图签外框、尺度校核与免责整套保留，返工的只是画面。
  - **几何取自 `visual/assets/bridge3d.js` 的构造参数，不从文字重画**，因此本图、三维场景、展示页 `.dims3` 三者同源；脚本末尾以断言锁住单侧占地 66×9、主跨 76、全长 208 三项，参数改动而未同步会直接报错。
  - **两条制图取舍都写在图上**：① 平面取一端坡道局部（1:1）——全长 208 米、坡道宽仅 9 米，画全长时两段坡道会挤成一根线，「并排而非叠置」这件最要紧的事恰恰看不见；② 立面与纵剖竖向放大 4×——208 米对 6 米抬升，等比画出来是一条平线，三个尺寸标不出来。
  - **纠正一处自己画错的**：纵剖首版把平面长度当成了展开长度，回折段因此画成「一段平的＋一个垂直跌落」。两段坡道在平面上并排，沿路径展开的长度是 62+4+62+4 ＋76＋ 镜像 ＝ 340，而非平面长度 208。已改为展开剖面，并在图上注明横轴口径，避免读者把两者混用。
- **相应的自我描述同步改**：`manifest.json` 两条描述、`report/copyright_statement.md` 此前都写「Three.js 场景截图套版式」，现均改为「确定性脚本绘制的正投影技术图，几何与三维场景同源」；展示页两处 `alt` 同步。**未使用任何图像模型。**
- **复验**：无 WebGL 时两语言均显示对应语种图、`role=img`、操作提示与 `tabindex` 均已移除；有 WebGL 时画布起来、静态图隐藏、`tabindex=0` 保留。漂移与跨产物数字均 PASS，包体 39.41 MiB。

## v0.23 - 2026-08-28

- **展示页对「人」字的说法与正文相反，共四处**。正文原话：「"人"字落在两端折返坡道上，不落在桥面上——**桥面一旦做成人字形平面造型，展线的技术内核就退化成图案**」「每侧只设一处折返，**两段坡道在平面上并排展开**、不上下叠置」。也就是说方案是**主动拒绝**人字形平面造型的，「人」指的是折返这一动作（京张展线的技术内核）。而展示页说的是反的：中英文正文各一句「「人」字要从空中俯视两端并排折返的坡道才能读出来」、静态图 `alt` 两处、`manifest.json` 的图件描述里还有「俯视合读为「人」字」。**已开图核实：把三维场景转到接近正俯视，确实读不出人字轮廓**——页面让评委去做一件做不到的事，而且做完会发现与正文互相矛盾。四处已全部按正文口径改正。
  - **固化为判据**：`check_drift.py` 的已撤回表述黑名单新增「俯视合读为」与两条英文对应写法；**并把 `manifest.json` 纳入黑名单的检查对象——它此前不在检查名单里**，这也是这条能在 manifest 里存活至今的原因。用故意改回旧说法验证过会报警。
- **三维静态兜底图改为带图签的技术示意**。原图是 Three.js 场景的裸截图（树是球体、地面纯色、桥是几块板），而它是 WebGL 加载前第一个被看到的画面，同包另有 11 张照片级写实效果图，落差刺眼。**问题不在"画得糙"**——包里 `streetview-*.png` 同样是简笔几何却完全合格，因为它有标题、视点说明、比例与免责，读者一眼知道该用图解的标准看它。现套用 `sec_lib` 同一套图签版式（1742×1108，与断面图件同族）：标题「人字之跃步行桥 · 形态说明（概念示意）」、几何依据副题、尺度校核五项（**全部取自展示页 `.dims3` 既有口径，未新造任何数字**）、竖向链条与人字说明（**逐字取自正文**）、以及体量图自己的免责——不套断面那句「宽度为分配意向而非道路红线」，那对三维体量图是错的自我描述。中英两版，按 `site-overview` 的惯例进 manifest（`language` + `translation_of`）。
- **三维场景本身同步改**（静态图就是这个场景的截图，改场景等于同时改好交互与静态图）：地面与树冠降饱和（实测地面平均饱和度 89.4 → 61.4）、球形树冠改扁冠、新增一道方向光阴影使桥面投影落在路面上、雾距由 260–560 推到 620–1400（原来那段雾会把「退到能看全整条链」的距离整片洗白）。**未使用任何图像模型**：这张图的职责是替代那个交互场景，用写实图会让它与随后加载出来的东西对不上，属于另一种自述不符。
- **舞台高度改为随内容切换**：图签图有页眉页脚、必须整幅可读，故舞台默认按图解幅面定高（`aspect-ratio:1742/1108`）、`object-fit` 由 `cover` 改 `contain`；三维成功启动后由脚本加 `.is-live`，改回按画布定高。两条路径均实跑验证。
- **复验**：四门 PASS、漂移 PASS、跨产物数字 PASS、manifest 98 条哈希 0 不符、声明缺失 0、包体 39.36 MiB。

## v0.22 - 2026-08-28

- **模型披露此前只报了两个模型中的一个**。`agent.json` 的 `model_detail` 写的是 `claude-fable-5`，而本仓库自身的提交记录显示：本投稿目录下 43 次提交里，**26 次署 Claude Fable 5、17 次署 Claude Opus 5**，两个模型都做了实质工作（Opus 5 的 17 次含 8-27 与 8-28 两天的全部整改）。这是一次开源 AI 智能体征集，作者模型是会被直接读取的事实字段；只报其中一个属于披露不完整。已改为 `claude-fable-5, claude-opus-5`（校验器对 `model_detail` 只要求非空且非脚手架占位符，`model_family` 仍为枚举内的 `claude`）。`agent_id` 与 `agent_name` 未动。
  - **依据可独立复核**：`git log --format="%b" -- submissions/wanghao198822/jingzhang-century-answer | grep -i co-authored-by | sort | uniq -c`。
  - 这项改动属于事实字段的更正，不涉及任何设计主张；若参赛人对披露口径另有安排，此处应以参赛人的决定为准。
- **全包「登记了却没人看得见」的普查已做完**：36 张图件、11 张效果图、8 个媒体文件、9 个几何图层——**全部至少被一件产物引用**；`design_depth_matrix.json`（219 个可检字段）与 `compliance_matrix.json`（380 个）在人能读到的产物里零缺席。`standard_matrix.json` 有 3 个是分组名（AI共创与运营／AI治理合规／无障碍与包容），其下的具体标准均已露面；`spatial.json` 的缺席项为机读键与带括号全称，同名要素在正文中均有出现。

## v0.21 - 2026-08-28

- **登记了却没人看得见：风险框架此前只活在一个供机器读的 JSON 里**。`risk.json` 登记了 8 个风险维度，每项带评分、成因、缓解措施与人工复核触发条件——是相当扎实的一套东西；但**它的 8 个标签在正文、展示页、两本图册里一次都没出现过**。也就是说，评委能读到的任何一件产物里都看不到这套框架，而「风险管理」恰恰是会被单独评分的一项。现在 A3 第 14 页「风险与待补资料」与 A0 板 07「合规与风险」各以一条列出八个维度及其分值，并注明成因／缓解／人工复核触发条件均登记在 `risk.json`——细节仍以该文件为准，不在图纸上截断转述。复测：八个维度在人能读到的产物里均已出现。
- **同一类问题固化成判据**：`check_drift.py` 新增第 9 项——`risk.json` 的每个维度标签必须出现在正文、展示页或图册之一（比对前先去掉空白，因为图纸折行会把标签拆开）。已用插入一个伪维度验证它会报警、撤回后归零。这类「包里有、但人看不到」的缺陷本轮已连续出现三次（九张断面在两本 PDF 零引用、`render-disclosure-wall.jpg` 无处使用、风险登记无处露面），故立判据。
- **两处排版由审计器兜住**：首版把风险登记做成 p14 的独立区块，审计器当场报出中英文共 5 处碰撞——该页在上一版加图后已排满；改为并入该页原有的风险要点列表后归零。

## v0.20 - 2026-08-28

- **图册末页补上收尾的分量**。全册翻阅节奏是「封面 31% → 中段 15–22% → 重点区三页 30/30/29% → 末两页 12/11%」，以两页纯文字收场，末页右下还空着约三分之一。同时 `render-disclosure-wall.jpg` 在两本 PDF 里此前一次都没用过。这一页的副标题正是「把不确定性摆在台面上」，而算法公示墙就是那句话的画面——把它收在这里，既补上收尾，也让这张图有了去处。末页着墨率 11.0% → 21.7%。
- **英文版「9月交接姿态」行宽同步按栏宽重设**（58 → 92 半角）并把该块整体上移。又是版面审计器逼出来的：加图后它立刻报「正文压在公示墙图上」，且第一次只放宽行宽还不够，直到把块上移才归零。
- **两条外部评审意见经实测后决定不改，测量值一并记录**：① 「导览层与证据层之间有超过 1000px 的洞、评委会以为文档结束了」——首次测得 677px（中）/974px（英），但那是我只扫描了 `#ovx` 内部的结果；实际打开看，这段里排的是首屏大标题、三段总览面板与免责声明，不是洞。② 「末两页着墨率 5.7%/3.9%，近乎空白」——那是 v0.11 给 p14 补入演练读数之前的旧数，本轮改前实测为 12.0%/11.0%，与 p2（11.7%）同档；不过「以最轻的一页收尾」的判断成立，故按上一条处理。

## v0.19 - 2026-08-28

- **A0 板 02—06 补远观层，同一块地方顺手解决重复问题**。A0 是贴墙看的，评审第一动作是三米外扫一眼决定要不要走近；而这五块板最大的字只有 40pt（14.1mm），三米外整块板退化成灰纹理（板 01 有 150pt 主标题，是唯一例外）。同时那条六格数字带在六块板顶部原样重复、各占约 5% 版面，序列毫无推进感。两件事占的是同一块地方：撤掉板 02—06 的重复数字带，换成该板自己的一句结论（96pt 铜色）——「轻介入 · 可逆化 · 复用优先」／「先通后留」／「把答案变成日子」／「南问 · 中答 · 北跃」／「测试先于推广」。**用词全部取自该板原有副标题**，只是把最有力的半句提到远观层、把限定条件留在副标题，不新提任何主张。数字带在板 07「指标与合规」保留一次，122 与 ~70/45万 因此仍在 A0 组里出现，未丢信息。做完用 1/9 缩放的三米视距模拟逐板复看，五块板现在都能在远处读出一句话。
- **新增 `check_numbers.py` 并挂进收口流程**：同一个事实在正文、指标、图册、图板、展示页里必须是同一个数。原有四门只比对得了页面 `data-metric` 与 metrics.json；而图纸里的数字是在 `make_drawings.py` 里**手打的字符串**，此前任何一门都没有查过。现固定核查重点片区面积口径、应答门数 46、海淀备案大模型 122、场景卡张数、场地面积的跨产物等值写法，以及中英文关键数字的对齐。已用故意写入「12 张场景卡」验证它会报警、撤回后归零。
- **本轮跨产物核查未发现事实性错误，一并如实记录**（首轮 7 条告警全部是我自己判据写错，不是包的问题）：① 中文里 `\b` 不是数字边界（汉字与数字都算 `\w`），`\b46\b` 在「46座」上一律匹配不到；② 「备案大模型 N 款」本就有多个带出处的口径——海淀区政府 122、北京日报 128、首都之窗 130、全市统计公报 209，图册里专门有一段讲它们的差异，见到非 122 就报警是错的，判据已收窄为「只禁把海淀说成非 122」；③ 中文「1141.28 万平方米」与英文「11.4128 million square metres」是同一个数，只是各自语言的习惯写法，判据改为从 metrics.json 现算出全部等值形式再比对。

## v0.18 - 2026-08-28

- **A3 第 10／11／12 页此前共用同一张三联区位图**，摆在约 210px 宽的小框里：三段全画，恰恰指示不出「现在讲的是哪一段」，读者连翻三页会以为印错；那个尺寸下图内标注也全糊。现三页各配本段自己的断面——DZS-1（声波径穿越城市道路）／ORI-1（校园围墙界面「透而不破」）／ZZY-1（人字天桥跨环路主跨），放在原本空着的左下半页，图注**逐字取自展示页已登记的同名图件说明**，不另行编写。这也让包里九张断面的引用数从 2 张增加到 5 张。
- **英文版行长同步按栏宽重设**（86 → 110 半角）。这一步是版面审计器逼出来的：英文条目更长，第一版改完它立刻报出中英文共 11 处「正文压在断面图上」，改完归零。
- **A3 图册的 JPEG 质量单独降一档（70 → 64）**：新增六张断面（中英各三张）后包体升到 39.48 MiB、只剩 545 KB 余量。A0 是贴墙近看的、质量不动；A3 是拿在手里或屏幕上看的，同样观感下可以再压。已在 5 倍放大下逐字核对断面图内的中文注记，无可见压缩痕迹。包体回到 **38.99 MiB**（余约 1.0 MiB）。
- **一条经实测后决定不改的**：外部评审指「每张图下重复 9 次的三栏说明带，其中一整栏是常驻空白，约 1500px 版面被样板话占掉」。实测该三句样板话合计仅约 875px（虚线口径 10 处 208px、比例尺口径 9 处 505px、悬停占位 9 处 162px），占全页 56,000px 的约 1.5%，且悬停占位每处仅 18px 高——与「1500px、整栏常驻空白」的描述不符。更要紧的是，「每张图自带口径」本身是本包的一项主张，合并成全文只讲一次会削弱它。故保留，如实记录测量值与理由。

## v0.17 - 2026-08-28

- **三维兜底图没做到它唯一要做的事**：`b3d-poster.jpg` 存在的理由是「俯视合读为人字」，而画面里左下与右侧两组折返坡道都跑出了画外，什么也没解释成——它还是 WebGL 加载前第一个被看到的画面。改了取景（把环路转到斜贯画面、相机后退并抬高俯角），两组坡道现在完整入画，桥面—坡道的「人」字形一眼可读。
- **顺带修掉画面里的「世界的边」**：地面原为 560×500 米，相机稍一后退就能看到地块被切断的直边。雾在 260 米起、560 米止，已把地面放大到 1500×1180 米（雾外），边缘再也进不了画面；车道虚线与树木随之铺满。
- **三维起不来时页面在让人做办不到的事**：WebGL 不可用时保留静态兜底图是对的，但右下角「拖拽或方向键旋转 · 滚轮或 +/− 缩放」的操作提示仍在，`aria-label` 也还在说可用方向键，而此时容器已无任何交互。现在这条分支会移除操作提示与 `tabindex`、把 `aria-label` 改写为静态图说明。**两条路径都实跑验证过**：禁用 WebGL 时提示消失、`role=img`、aria 改写；启用 WebGL 时画布、提示、`tabindex=0` 与可交互 aria 均在。

## v0.16 - 2026-08-28

- **全包主色统一到印刷件的藏青 + 铜**。此前印刷图册是藏青 `#1F3A5F` + 铜 `#B87333`，展示页却是青绿 `#2F6B4F` + 另一个铜，藏青一次都没出现——评委拿着藏青图册扫码进青绿网页，第一反应会是「这几件东西不是一个人做的」。核查用色时还发现更实质的一条：**青绿在页面上身兼二职**，既是品牌与结构色（品牌字、章节号、栏目标题、按钮、语言切换），又是「通过 / 进度」的状态色（`pass` 标、核验通过徽章、长卷进度条、道钉带生长条）。现在把两件事拆开——结构与品牌统一到藏青，青绿降级为**状态色专用**（新增 `--ok` 记号）。改的是记号，不动任何数值、`data-metric`、图文对应或证据引用。
- **未一并改的一处，如实记录**：叙事层结尾的链接色仍用青绿系 `--ov-patina`（全站仅此一处，且它在深色主题下的对应值是浅青，换藏青会在深底上看不见）。断面图件自带的绿色图签栏也未动——那要重出九张图，本轮不做。
- **复验**：对比度改动后重跑，中英文仍为 0 处不达标（藏青比青绿更深，对比度只增不减）；四门 PASS、漂移检测 PASS、展示页体检全部通过；并逐屏目视核对首屏、总览面板、各章节头与附录 E，状态绿只出现在 `pass` 与进度条上。

## v0.15 - 2026-08-28

- **英文页的「可当场核验」面板印出 31 行中文**（12 条不重复）。这一节是全包最硬的卖点——「任何人可独立核对」——却恰恰是英文评委最看不懂的一块：拓扑主表的 5 个行标题、几何复算里 23 行跳过原因（4 种），全部来自 `verify_topology.js` 与 `verify_geometry.js` 里写死的中文串，而两个页面共用这两个脚本。现给这两个脚本补上 `label_en` / `reason_en`，页面按 `LANG` 取值。复测英文页只剩 3 行中文，且都是标了「raw script output」的脚本原样输出——那三行**故意不翻译**，读者据此可与自己跑出来的结果逐字对照。
- **导语里的「页上 165 处声明」是旧数**，脚本实际报 171（差的正是新增的 6 项演练指标）。在一段正推销「可当场核验」的话里对不上自己的工具，格外难看。两语言已改正，并**把这条加进 `check_drift.py`**（导语数字必须等于页面 `data-value` 的实际条数），用故意改回 165 验证过它确实会报警。
- **两处自述经复核成立，未改**：① 三维场景的键盘可达（`aria-label` 与 v0.11 声称的方向键旋转、加减号缩放）——首轮用 `canvas.toDataURL()` 测得「无变化」，但 WebGL 画布在未开 `preserveDrawingBuffer` 时 `toDataURL` 本就返回空白，读数无效；改用合成器截图比对后确认方向键与加减号都真的改变画面，纯 Tab 第 139 次到达舞台。② 短片自述「1920×1080 · 55 s · 无音轨」，实测 1920×1080 / 54.92 s，成立。
- **复验**：四门 PASS、漂移检测 PASS（含新增第 8 项）、展示页体检全部通过、核验面板三个脚本在两语言下均 PASS（几何 70/70、页面数值 171/171、拓扑 5/5），远程请求 0。

## v0.14 - 2026-08-28

- **短片字幕在本地打开时一条都不显示**（本轮最严重的一处，此前未知）。Chromium 把每个 `file:` URL 当作独立源，`<track src="...">` 因此必然加载失败——实测两语言页面的默认字幕轨 `readyState=3`（错误）、`cues=0`。而这是一部**静音短片，字幕即全部叙述内容**，且本包就是给人双击打开的离线包。现把两条字幕轨的正文内联进页面，用 `blob:` 重新挂上：不发任何请求、不受文件源策略限制。复测两语言默认轨各 7 条、切换到另一轨也是 7 条，远程请求仍为 0。
- **WCAG AA 补到真的为零**。上一轮报「零不达标」是背景色取法偏松所致：改为逐元素向上找到第一层不透明祖先的实际背景色后复测，中英文各有 25 处不达标。分三类：① `.kick` 小标签的铜色 `#A4632A`，上一轮只加深了 `.b3d/.trio/.day` 三处，漏了 `.nums` 与 `.duo`（浅底 3.68:1），深底 `.plate` 那 4 处则相反——那里该用更亮的铜，正好取印刷件的 `#B87333`（4.40 → 5.54:1），**顺带让两套并行的铜色在深底这一处对上了同一个值**；② `.b3d .dims3 b` 的 16px 铜色数字；③ **「未知」的破折号用的是发丝线颜色，只有 1.37–1.42:1，几乎看不见**——「unknown 如实标注」是本方案的主张之一，看不见的标注等于没标，已压暗到过 AA 且仍弱于正文。现两语言均为 0 处。
- **窄屏溢出补到 768px 档**：靠左的道钉悬浮卡向左顶出视口。页面本就有 `.w`（卡片左锚）规则，但脚本从来只加 `e`、不加 `w`；宽度夹取也只写在窄屏 media 里。两处都按既有机制补齐。现两语言 × 360/375/414/768/1440 越界元素全为 0。
- **首屏引文**：栏宽 23em（实测 483px）一行只放得下约 23 个汉字，把「中国人能不能自／己修铁路」拆词、第二行只剩 5 字孤行；高亮用 inset 阴影，行内元素跨行时默认 `slice`，于是断成上下两块错位方块。改为 30em 并补 `box-decoration-break:clone`。
- **「一天」五格时间轴把图注点名的主角裁出画外**：格子被 `1fr` 撑成 243×371 的竖框，而源图是 16:9，`object-fit:cover` 只留下 **36.9%** 的横向条带——10:00「机器人敲门」的配送机器人被挤成左下角一条碎片，14:00「课门开讲」的课门只剩一道边框。改为固定 16:10 比例并让格子按内容收拢，横向保留率回到 90%。
- **断面与人视图册在页面上只有 178×112**：1742×1108 的专业断面（有图签栏、引注、比例尺、指北针、材料图例）在这个尺寸下全糊，评委不点开灯箱就永远不知道画了真断面。放宽到每格约 405×258，并按图件比例排；同时**删掉每张缩略图下重复排两遍的标题**（中英各 24 处）。
- **被判「不采用」的那张图是画廊里最抢眼的一张**：`render-answer-gate.jpg` 彩度与人物密度都是九张里最高的，而「不采用」只由一枚小红标承担。现降到 55% 不透明度并去色 85%，让否定先于图像被读到。
- **右侧长卷尺的当前节标签压住正文右缘**：`.km.cur .lab` 常驻显示、最宽 91px，实测盖住用地表最右列的登记面积尾数。正文右槽由 104px 让到 160px。首屏「向下滚动」由 10.5px 提到 13px（原规则里写了两次 `font-size`，后者覆盖前者）。
- **新增 `verify_web.py` 作为展示页的常设体检**（窄屏溢出 / 对比度 / 零远程请求 / 字幕实际条数），此前这几项都是临时脚本临场写的、跑没跑全靠记。其中窄屏一项固化了 2026-08-27 的教训：入场动画未触发时元素处于塌缩态、包围盒返回 0 会漏判，所以先滚完整页再测。
- **复验**：四门 PASS、漂移检测 PASS、展示页体检全部通过（两语言 × 五档宽度溢出全 0、对比度 0 处不达标、远程请求 0、默认字幕轨各 7 条）。
- **已知未决**：印刷件（藏青 `#1F3A5F` + 铜 `#B87333`）与网页（青绿 `#2C5C50` + 铜 `#A4632A`）仍是两套主色，本轮只在深底 `.kick` 一处对齐了铜色；断面图件自带第三套绿。全面统一需重跑对比度与整页复看，未做。

## v0.13 - 2026-08-28

- **两本 PDF 的嵌入位图历来卡在 100 dpi**。根因不在压缩后处理，而在 matplotlib 的 `figure.dpi` 默认值 100：`imshow` 会把每张图重采样到「摆放尺寸×dpi」，所以四本 PDF 里每一张嵌入位图恰好都是 100 dpi。代价落在证据图上——`site-overview.png` 本身有 2100×3308，摆到 A0 上只存了 943px，标注文字因此糊掉。现改为**每张图取到它自己源图的真实像素为止、上限 200 dpi**：证据图落到 115–200 dpi；效果图源文件本就只有 1672–2048px，回到自身上限（58–141 dpi），不再存插值出来的假像素——单纯提 dpi 会把包体推到 55.7 MiB，而这一步在提升证据图的同时把它压回 **38.90 MiB**（上限 40）。
- **开图查出三处版面事故**（程序化文字扫描此前覆盖不到）：A0 板 07 右栏标题与指标表硬碰撞，渲染出「unknown合规与风险」连字（中英文均有）；板 05 中间卡的末条要点掉到米色框外；板 03 图注被页脚铜线从字腰横穿，等于给整行画了删除线（英文版三处）。
- **两条根因，改在机制而非逐页搬字**：① 要点行高用的是写死的 `0.0135×(size/9.5)`，那是按 A3（842pt）定的，搬到 A0（3370pt）就变成真实行高的 3.3 倍，于是留白被平均摊进每一条要点的缝隙里，版面看着又空又散——改为按图幅高度实算行高，并新增「把要点均匀铺满到指定高度」的排法。② A0 右栏净宽 1049pt、中文 24pt 可排约 43 字，而行宽参数写的是 20 字，行数直接翻倍——英文版板 04 因此冲进页脚。现按栏宽重设各栏行宽。
- **通栏装饰线被内容切断，两端留孤儿线头**：A3 封面铜条画在 x=0→1，而照片压在其上，只剩左右两截；A0 板 01 的藏青时间轴线从区位图卡背后穿过再冒头。封面铜条收到照片正下方并对齐照片版心，板 01 的线收在图卡之前。
- **图注**：由居中改为左对齐到图像左边线，并贴住**图像可见下沿**而非 axes 矩形下沿——`imshow` 等比留白会让两者相差很远（实测 68–170mm 不等），图注因此忽远忽近、远到不再「属于」那张图。同时立页脚禁排线，越线的图整体上移。
- **包里 9 张断面图在两本 PDF 中零引用**（`grep -c "section-" ` 结果为 0），而 A3 第 8 页页题就叫「交通与机器人通道断面」，占主图位的却是一排没有竖向尺度、没有比例、没有人的色块。现 A0 板 03 与 A3 第 8 页各放入断面 DZS-3（桥下空间由无序停车改为公共使用），色带图降为功能带索引；色带中「步道+道钉带」与「步道+断点客厅」原本写死同一色值 `#B87333`，已区分。
- **A0 板 05 重排**：底图是 2100×3308 的竖图，原先摆在 0.88 宽的横框里，等比留白后实际只占版面 36% 宽度，三张卡横排在下方又填不满。改为竖图占左栏、三卡竖排右栏，卡高跟最长一列的内容走。
- **封面标题加压暗层**：白色主标原本直接压在夜景的高光带上（高架灯带正从「应答」两字上沿穿过），加一道自上而下渐隐、且严格对齐照片可见范围的压暗层。
- **KPI 条的孤立括号**：行宽不足使「（官方口径）」被拆到两行、末行只剩一个「）」，在 8 个版面上重复出现。改用间隔号，一处改动同时修好全部版面。
- **新增版面审计器并做了故意破坏验证**：每页出图前实测所有文字（含画在 axes 里的）的渲染包围盒，检查越页脚禁排线、出血、文字重叠、贴脸、以及正文压在内容图上五类。为确认它抓得住假货，把 A3 第 8 页的色带故意放回碰撞位置重跑——中英文两版各被报出，改回后归零；此前它只看图层级文字，中文版那次碰撞漏报，据此补上了 axes 层。
- **复验**：四门 PASS、漂移检测 PASS、版面审计 42 页 0 项、包体 38.90 MiB。
- **已知未决**：全包并行三套主色（印刷藏青 `#1F3A5F`＋铜 `#B87333` / 网页青绿 `#2C5C50`＋铜 `#A4632A` / 断面图的绿），本轮未统一。

## v0.12 - 2026-08-28

- **展示页长期未随正文更新，本轮系统清理**。根因：收口流程只把门禁面板*拼接*进 `visual/index.html`，从不整页重生成；而页面生成器里并不包含后续加入的人本导览层、三维场景与短片（页面 157 处 `ovx`、生成器 0 处），因此历来无法重生成。后果是正文与矩阵多次改正的内容没有传导到页面，具体清理如下：
  - **13 处「官方场景ID」**——changelog 早已记载该表述为不实并宣布改为「甲方场景类别」，但当时只改了正文与矩阵，页面原样保留；现两语言全部改正，另一并清理「全部场景挂接甲方标准场景ID」等同源表述。
  - **场景卡道钉带只有 12 枚、计数器写 0/12**，而全包已是 19 张；两页各补 7 枚至 19，计数器同步。
  - **已撤回的法律主张仍在页面**（「这条判断有明确的法定依据…」）；同步为正文现行口径「法定依据止于其自身适用范围…扩展到 AI 服务界面属本方案自愿设定的更高标准」。
  - **三处声明「87 项指标：77 已知 + 10 未知」**，而 metrics.json 为 93（83+10），附录 A 只渲染 77、六个演练指标在两页出现 0 次；现附录 A 新增「09 · 离线场景演练读数」6 项并改正三处总数，页面 `data-metric` 由 77 增至 83、与 metrics.json 零不符。
- **矩阵与假设登记的过期口径**：`compliance_matrix.json` 的八要素仍为旧「…场景—政策」（已对齐 agent.2 的「…产业…场景」）；`standard_matrix.json` 两处「12张场景卡」；`design_depth_matrix.json` 把公共空间率写成 **3.93%**（正文与 metrics.json 均为 1.93%）。
- **来源表一处错链**：`HD-BRIDGE-SPACE-2025`（自述北京日报客户端）与 `DONGSHENG-2026` 共用同一 URL，且该 URL 属新京报域名，系复制粘贴残留。已换为实测可达的对应报道《新增2090个车位！海淀194处桥下空间功能提升》（北京日报），标题与正文内容逐项核对。
- **`metrics.json` 的 `site_area_sqm` 假设自相矛盾**：首句「场地包中存在可信的官方边界」与次句「基于临时边界、非官方红线」直接冲突，且挂在 confidence=high 的分母指标上；已删除该模板残句。
- **「130 款」一条的方向修正**：外部审查报「图纸引用了本包自禁数字」，核查后**图纸无误**——`sources.json` 的 `JZ-BELT-OFFICIAL-2026`（首都之窗）正是其出处，经实际打开该页面确认载有「备案大模型130款」等数据；过期的是 `assumptions.json` 的挂账与正文「未在本包登记来源中逐字核到」一句，两处已改正（中英同步）。
- **版权声明与实际文件对齐**：声明称两张作废图「不再随包提交」，但 `render-answer-gate.jpg` 仍随包作为「判为不采用」的过程留档并在画廊展示；AI 生成图清单漏列 `render-answer-origin`／`render-dazhongsi-aerial`／`render-robot-knocks` 三张。现逐张核对磁盘实况后改正，并注明 `b3d-poster.jpg` 为三维场景截图、非图像模型生成物。
- **测试卡计数与免责范围统一**：总表标注属性为「测试」的是 6 张（S9—S12、S17、S18），而正文写「含4张测试验证卡」、免责只覆盖 D 组四卡——S17（机器人进社区）与 S18（带边界交接）恰恰最需要该免责。现统一为六张（中英同步）。
- **复验**：四门 PASS、97 条哈希 0 不符、窄屏两语言 × 360/375/414 溢出全 0、WCAG AA 中文 1413／英文 2113 个文本元素零不达标、42 页四类缺陷 0、包体 36.2 MiB。

## v0.11 - 2026-08-27

- **补齐三处评审点名但正文此前零命中的内容**：市级“三区两翼”的完整对位（并显式区分本方案自用的“一脊两翼十地块”与市级布局，避免读作混淆），其中**中关村科技服务翼**给出要素服务驿站第二类窗口的三件可核验事项、**小月河场景赋能翼**对接 19 张场景卡的公共体验路径与 S9—S12 测试环线；**五大功能**逐条给出空间落点（道钉带／要素服务驿站／场景卡／应答门与断点客厅／公示墙与年度报告三张清单）；**区域协同**写到具体对象——与北纬社区、未来科学城、怀柔科学城、北京经济技术开发区以“场景互开＋测试互认”为最小单元，京津冀层面建议 3·25 赶考日联办。均标注为概念建议，不构成机构安排或政府承诺。
- **渲染稿与正文对齐**：`report/proposal.html` 与 `report/proposal.en.html` 此前是不含本轮新增章节的旧渲染，已用官方 `scripts/render_proposal_html.py` 重渲染，双语同步。
- **英文图件引用修正**：英文正文中 site-overview、key-areas、land-use-structure、metrics-evidence、mobility-bluegreen 五张图此前引用的是中文版（英文版早已随包提供且确认为不同文件），已改指 `.en.png`；另修正一处交叉引用（掠射照明规则在断面三，原误写断面一）。
- **三维场景补齐无障碍三项**（对应提交指南「WebGL 需静态兜底图＋键盘操作＋加载/错误状态」）：新增静态兜底图 `visual/assets/renders/b3d-poster.jpg`（取自该三维场景自身的截图，非另行生成的效果图），加载前作占位、浏览器不支持 WebGL 时作兜底并改写状态提示；场景容器可获焦点（`tabindex=0`、`role`、`aria-label`），方向键旋转、`+`/`−` 缩放，不依赖鼠标即可操作。已用 Playwright 实跑两语言页面验证：兜底图先显示、成功后隐藏，方向键与加号确实改变渲染画面，零远程请求、零页面错误。
- **短片补英文字幕轨**：新增 `assets/media/narrative.en.vtt`（7 条，时间码与中文轨逐条一致、末条 00:00:54.900），两语言页面各挂两条 caption 轨并按语言设默认；片中烧录字幕为中文，英文轨为叠加显示的译稿，已在 manifest 描述中如实区分。文字稿补「制作与权利边界」与英文全文段（生成工具、图生视频模型、拼装链路、无第三方素材声明）。
- **窄屏横向溢出修复（共四处成因）**：实测 375px 下中文页横向溢出 94px、英文页 106px（甲方要求响应式适配移动端）。前三处：`.tw` 表格包裹层缺横向滚动；`.plate/.cover .ph` 未裁剪，视差 `scale(1.05)` 使图片渲染盒宽出容器 5%；`.mmb .mh` 英文长标题串不换行。第四处 `.spike .card`（道钉悬浮卡固定 268px，居中锚定在靠边道钉上顶出视口）**是首轮测量漏掉的**——首轮未滚动全页，道钉带入场动画未触发、元素仍处塌缩态，`getBoundingClientRect` 因而返回 0，导致误判为已清零；改为先滚完整页触发全部入场动画后复测，读数与外部独立复测一致（360→22px / 375→18px / 414→7px）。现补 `.belt{overflow-x:clip}` 与窄屏 `width:min(268px,calc(100vw - 56px))`（不隐藏卡片，因其文字是道钉按钮的可访问名称）。**最终实测两语言 × 360/375/390/414/768/1440 共 12 个组合，且在触发全部入场动画后，横向溢出全部为 0、未被裁剪的溢出元素 0 个，道钉带动画仍正常。**
- **对比度补到 WCAG 2.1 AA**：实测中文页 765 个文本元素中 207 个、英文页 1128 个中 212 个低于 4.5:1。先测清色号用法再动手——`--brass #A8763E` 与硬编码灰 `#8A938E` 在浅底共 1149 处、深底 0 处，故可安全整体加深为 `#835C30` / `#5D6460`；`--ov-copper #A4632A` 深底另有 4 处在用，因此不动全局，只在浅底卡片的三处 `.kick` 标签限定加深为 `#8F5625`；三维提示片 `#7A7F76` 加深为 `#666A62`。加深幅度按各自实际背景色算到刚过阈值、留 0.05 余量，不做过度加深以免破坏视觉。复测：中文 1379、英文 2074 个文本元素**零不达标**，并核对首屏观感未受损。
- **逐页开图核查 42 页图纸，查出并修复两处维护者门槛级缺陷**（程序化文字扫描覆盖不到、只有开图才看得见）：① 英文 A0 第 3 张的「数据边界」整段压在照片上、白字压深色底基本不可读（英文条目更长、列宽 40 字符导致行数溢出到图片区），现将英文列宽放宽至 52 字符，文字回到文字栏；② **`Jingzhang` 错写共 26 处**（英文 A3 图册 18、英文 A0 图板 8）——官方术语表明确「京张＝Jing-Zhang，避免 Jingzhang」，另在 `visual/index.en.html` 6 处、五张英文图件的图例与时间轴中亦有，已连同生成器一并统一。正文 `proposal.en.md` 本就为 0 处。
- **法条引用边界收紧**：自查确认全文无任何"第X条"式精确条款引用（0 处），因此不存在错引条款序号的问题；生成式AI办法的四处引用均落在通用义务上且明写"不预设任何豁免、以取得管理许可为前提"。但两处措辞把"本方案自愿扩展"写得像"法律既有要求"，已收紧：《无障碍环境建设法》一处改为"法定依据止于其自身适用范围……把同一原则扩展到 AI 服务界面是本方案自愿设定的更高标准、属概念建议"（中英同步）；《适老化实施方案》一处改为"作为**政策参照**而非本项目的法定控制条件"。
- **场景卡对齐官方六要素**：官方要求每卡说明服务对象／使用情境／所需数据／公共价值／风险点／人工复核。结构化核验三张场景表各 19 行、**零空单元格**，其中五项已有专列（空间载体／数据档位／验证对象／退出条件／人工复核＋独立核验两列）。唯"服务对象"无专列，现补一段六要素对位说明并**逐卡列明服务对象**（S1–S19，中英同步）。
- **演练读数进入图纸可见面**：台账 `simulation.json` 本身不进评审输入，读数与不利结论此前只在正文。现加入 A3 图册 p14「风险与待补资料」左栏（两语言）——该页无图且下半部为空，主题上也正属"把不确定性摆在台面上"。**先试放 A0 图板 07「指标与合规」右栏，实测撞出中文 2 处、英文 10 处重叠（该栏合规条目在英文下换行更多、实际已满），按防死循环闸不在同一位置硬试第二次，改判位置后一次通过。**重出后 42 页四类缺陷仍为 0，并目视确认 p14 排版干净。
- **修复离线数据镜像过期**：`visual/assets/data/metrics_snapshot.js` 是 `metrics.json` 的逐字内嵌副本（离线页在 `file://` 下无法读同级 .json，故内嵌），文件头自带 `source_sha256` 声明来源。本轮往 metrics.json 新增 6 个演练指标后**未同步该镜像**：其记录的源哈希仍是旧版 `73399e70…`、内嵌指标只有 87 个，**即该文件对自身的"逐字副本"声明已不成立**。生成器自带的 `make_visual_data.py --check` 独立复现该判定（报 `DRIFT metrics_snapshot.js`）。已重生成：源哈希与当前 metrics.json 一致、93 个指标齐全、共有值零不一致、六个演练指标已入镜像，`--check` 转为 `OK`。（四门自检不覆盖该文件，属人工核验发现。）
- **A0 图板字号补齐站姿阅读下限**：先前记为"已知未决"的页脚小字（4.59mm 中／3.70mm 英）已解决。**根因不在页脚而在 board 2**——其条目块中英共用 34 字符列宽，英文行数暴涨、内容下探到距页底 4.0mm，页脚一加大就与之相撞。实测七张板两语言的最低正文位置后定位到只有该处越界（其余 ≥23mm），将其列宽字号按语言区分（34/56 字符、25/21pt）后，英文内容退到距页底 400mm，页脚随即安全加大到 17pt（6.00mm）。另把一处 `cap_size=15` 的图注改为 19（其余所有图注本就是 19，属不一致），board 6 英文表由 15pt 提至 17pt 并同步放宽列宽。**现 A0 最小字号 17pt = 6.00mm，全部达到站姿阅读下限**（原最差 3.70mm）。
- **A0 board 6 场景卡表补齐 S13–S19**：该表副标题写「19 张场景卡」，表格却硬编码只到 S12——是场景卡由 12 张扩到 19 张时该表未同步，**属"声称与展示不符"**。已补入 7 行（内容取自正文表 A，中英同步），行高压至 0.027 以容纳 19 行。首次补入后英文因单元格换行撞出 4 处重叠，**未压缩行高硬塞，改为把 6 处超列宽的英文单元格压到列宽内**（如「Materials deleted at closure; human review before issue」→「Deleted at closure; human review first」），一次通过。重出后 42 页四类缺陷仍为 0，两语言均覆盖 S1–S19。
- **全量嵌图反查**：用感知哈希把 42 页的每张嵌图反查回源文件（最大汉明距离 9/256），逐页与页面主题核对——封面配夜景、现状配大钟寺街景、三处重点区各配自己的鸟瞰＋共用区位小图、指标页配证据图，英文页对应 `.en` 变体，**零嵌错图**。这是布局检测覆盖不到的一类缺陷。
- **复验与体积**：重出后 42 页四类门槛缺陷（文字压图／文字重叠／越界裁切／缺字豆腐块）**全部为 0**。重出的基础图件因保存为 RGBA 而由 9.3MB 涨到 18.9MB、包体一度到 47MB 超限，已按原版口径量化回 256 色调色板（P 模式），图件回到 8.6MB、包体 36.1MB；量化后逐张目视确认文字清晰、无抖动伪影。
- **一处优化尝试后撤回并记录**：曾把 A0 页脚免责声明由 13/10.5pt 加大到 17pt（6.0mm，A0 站姿阅读下限），但 board 2 的指标块本就下探到页脚区，加大后与之相撞（实测 355×12.8pt 交叠）。两害相权取无重叠，维持原字号，**如实记录 A0 页脚字号 4.59mm（中）/3.70mm（英）低于站姿 6mm 下限为已知次要项**——该行为免责声明、非主读内容，且 A0 亦以可缩放 PDF 形式评阅。
- **prefers-reduced-motion 实测有效**：在 reduce 偏好下运行中动画数为 0。
- **灯箱放大图补替代文本**：原 `openLb()` 只设 `src` 不设 `alt`，读屏用户放大后拿到空图；现从触发元素或其内 `<img>` 取 `alt` 传入，关闭时一并清空。已用 Playwright 实测：中文页放大后 alt 23 字、英文页 70 字，零页面错误。
- **补两处评审点名词**：`南北贯通`（agent.4 必答的「东西缝合与南北贯通」，此前 0 命中——正文实质已讲透南北已贯通，现用官方术语点明并说清该方向的设计动作是把已贯通的九公里做成读得下去的长卷，而非再修一条路）；`经开区`简称与全称并列。

- **新增可选成果 `simulation.json`（离线场景演练台账）**：16 条任务覆盖四类场景（步行可达性、低速配送机器人、文化导览、企业服务副驾），固定 seed 20260828、纯离线合成轨迹，用例语义为人工设计的确定性清单，仅能耗与重规划耗时由 seed 生成，可按同一 seed 重放并逐条反算。六项读数接入 metrics.json（`simulation_task_count` 16、`simulation_success_rate` 0.6875、`tool_schema_pass_rate` 0.9375、`audit_completeness` 0.9375、`energy_budget_violations` 1、`replan_p95_seconds` 12.4），`baselines.urban_llm_harness` 与之逐键镜像，`high_risk_intercept_rate` 按不适用记空值加说明，不填 0 或 1.0。
- **台账保留不利读数，并把不利结论写进正文**：完成率 0.6875 中的 5 条未完成里，3 条是机制按设计工作的证据（敲门被社区拒绝、口述史未过三审、四字段核不到即停止铸钉），2 条是真失败（路径建议与实际不符、派单结构非法被拦）；另有 1 条满载爬坡能耗超预算、1 条夜间影像留存缺口。两处不利读数直接改写设计判断：坡道纵坡与休息平台间距须在试点实测复核；道钉带夜间可读性与可审计性分别验收。评审输入不含 simulation.json，故方法、读数与边界均在正文叙述并以 `[metric:...]` 回指。
- **六项智能体任务对位表进入正文**：`agent.1`–`agent.6` 逐条给出一句话回应与章节落点（两语言），此前该对位只存在于 compliance_matrix.json 与网页；创新生态八要素口径由“…场景—政策”对齐任务书 agent.2 的“土地—空间—产业—资金—人才—算力—数据—场景”，政策工具改为贯穿八项、不单列。

## v0.10 - 2026-08-25

- **人本导览新增两节**：甲方点名的“一天中的使用体验”（07:20–21:00 五个时刻：晨跑数钉、机器人敲门、课门上课、一响钟声、数灯回家，含“阶段变化”“人机协作”的字面回应）；「人字之跃」本地离线三维示意（three.js r160 随包分发并入第三方 MIT 声明，拖拽旋转/滚轮缩放，几何按断面 ZZY-1/ZZY-2 设计目标值搭建，零网络请求）。
- **竞品增量复检响应（119 个增量包）**：“道钉”符号与“作答/双答”近名已在其他投稿出现，区隔因此押到证据链上——四字段（模型名称·备案单位·备案编号·备案时间）与“逐颗可对国家公告核验”前置到摘要、两语言首屏导语与 A3 速览页；摘要补 3·25 公益纪念开放日与“应答的主语是可核对的物证和年年具名认领它的人”；人字桥加“几何事实、非治理隐喻”定位句；荣誉层补“断网断电仍可读”；分期第一步补最小可验证包（1 座已认领应答门＋1 段 100 米道钉试段＋1 次 3·25 开放日，互不依赖、可单独撤回）。
- **口径统一**：钉面刻字旧写法“备案事件+日期”在 A3 速览页、设计公理页与正文荣誉章的残留（中英共 6 处）全部统一为四字段；A3 速览页两处悬挂孤字与一处行首标点清理。

## v0.9 - 2026-08-20

- **短片桥段纠偏**：人字之跃段原用早期桁架桥素材（与全包「不做塔拱门架」口径相悖，此前以字幕声明兜底），
  现换为按断面 ZZY-1/ZZY-2 绘制的正确渲染推镜（桥面平直、无上部构筑物、两端并排折返坡道），
  桥型差异从「已声明」变为「已消除」；仅存的已声明差异为道钉位置（片中步道中部 / 正文步道边缘）。
- **A3 图册逐页一图**：三处重点区页改为各自鸟瞰主位＋区位小图；概念页配"蹲下数道钉"、蓝绿页配钟声广场；
  封面加入 16:9 夜景主视觉；修正重点区文案中"俯瞰为人字形的人行桥"（"人"字在坡道、桥面平直）与
  封面场景行的自造轨道名。
- **英文导览层**：九屏人本导览全文译入 index.en.html（58 串、术语沿用包内既定英文）；两语言页的
  证据层导航（顶栏与 K0–K9 章节尺）在导览层内淡出、过分隔条恢复。
- **合规矩阵 agent.3 证据摘要**：修正过期且不实的"12张卡挂接甲方六个标准场景ID"表述（矩阵与两页共 3 处）。

## v0.8 - 2026-08-20

- **第一印象层整体重做（封面 / A0 展板 / 人本导览页）**：封面改为 AI 生成的大钟寺片区鸟瞰打底、程序化排版信息，
  文字对比度按最不利像素实测全部≥6.9:1；A0 第一板改为"一张大图＋一句主张＋三个数字"（主图占版面约 30%、
  标题字高 53mm），七板正文全面提级至站看可读（字高<6mm 的文本由 73–87% 降至 0–4%），并以脚本对四份 PDF
  逐对检测文字/图元碰撞至全部归零（中英同检）；人本导览层（9 屏、四种版式、9 张效果图、引线标注）并入 `visual/index.html` 首屏，密集证据在同页其后。
- **展板配图纠偏**：B3/B4 原共用同一张交通蓝绿图，B4 改为钟声广场与道钉带两张效果图；B6 原纯表格无图，
  补入课门效果图；A0 上 2.5mm 高的断面小标注（站看不可读）撤下，断面仍保留于 A3 图册与 assets/figures/。
- **不实表述再清零**：图册与展板中残留的「场景ID全部取自甲方6个标准场景」「官方场景ID」等 8 处，
  与正文同口径改为"场景卡覆盖甲方点名的八类场景与三个概念；轨道名称为本方案自定，不冒充官方编码"。
- **体积**：matplotlib 无损嵌图导致 A0 一度 13.6MB，后处理将 57 张嵌入位图重编码为 JPEG，
  四份 PDF 合计 33.7→8.3MB（画质经 120dpi 放大目检无损）。
- 新登记：`render-answer-origin.jpg`（清华园车站站前素广场，AI 生成概念意象）；封面转为 JPEG。

## v0.7 - 2026-08-20

- **补齐甲方点名却缺失的 AI+ 场景**：场景卡由 12 张增至 19 张。此前 AI+教育、AI+法律、自动驾驶三类完全没有内容，
  「AI 文化／AI 社会／AI 城市」三个概念在正文中各出现 0 次；现八类场景与三个概念全部有卡、且使用任务书原词。
  新增七件可指认的空间要素：课门与道钉微课、算法公示墙与算法应答窗口、机器人敲门、道钉作为机器人定位锚点、
  退役刻痕、交接厅、慢速街。三张表（名录／验证协议／退出条件）同规格填满，每张新卡均给出可证伪假设、
  量化判据与「降级为无 AI 等价服务」的退出路径。
- **改正两处不实表述**：场景卡表头「官方场景ID」与章首「全部场景挂接甲方标准场景ID」——该列 ID 系本方案自造，
  在甲方场地包五个枚举文件中零出现。现改为「甲方场景类别」并注明轨道名称为本方案自定、不冒充官方编码。
- **统一 32 处残留命名**：正文早已将「长卷主轴」统一为「长卷主脊」以避开法定的中关村大街创新发展轴，
  但合规矩阵、深度矩阵、假设清单与中英展示页仍留有「主轴／一轴两翼／一轴一带」，评审对表时看到的恰是方案
  自称要避开的撞名。已全部统一，法定轴原样保留。
- **效果图整体重做，两张作废**：旧人字之跃渲染为下承式桁架桥，与断面 ZZY-1／ZZY-2 的五项口径不符
  （桥面之上不叠加构筑物、不做塔拱门架、单侧坡道 128 米、两段并排折返、两端均落地），整条作废；
  旧应答门渲染为青铜卷轴纪念门，与「46 座社区出入口」的定义不符，降为生成过程留档并标注不采用。
  新增六张由 GPT image 2.0 生成的意象图：大钟寺片区鸟瞰、人字之跃片区鸟瞰、应答门机器人敲门、道钉带夜景、
  算法公示墙、无墙学期课门。其中大钟寺一张的空间关系取自 OSM 实测（觉生寺与地铁 13 号线大钟寺站相距约 676 米、
  方位约 115°，13 号线走向约 175°），人字之跃一张的几何条件取自本包断面图件。
- **明确意象图与图纸的边界**：版权声明补写——七张渲染图均为意象图、不是图纸，由文字提示词生成、未经坐标配准、
  不由 `geometry/*.geojson` 派生，因而不表达用地性质、地块边界与精确尺寸；凡涉及用地、边界、尺寸与位置，
  一律以 `assets/figures/` 下的规划图件与断面图件为准。
- **英文侧同步**：`proposal.en.md` 同步 19 张卡与六段新正文；并统一三个英文术语变体
  （Herringbone Leap→Ren-Shaped Leap、Bell Chime Gate→Ringing Bell、Answer Ledger→Answer Book）。

## v0.6 - 2026-08-19

- **概念短片整体重做**：v0.5 的 58 秒短片为逐帧程序化渲染的图文动画，读起来是"演示文稿动画"而非"建成后是什么样"，整体废弃。
  新片 55 秒 / 1920×1080 / 24 fps / 静音，由 AI 图生视频生成五段实景感画面——道钉带、南问·鸣钟之门、中答·应答原点、
  北跃·人字之跃、东西向穿越·应答门——外加片头片尾。海报帧改用片中静帧（`assets/media/narrative-poster.jpg`，新登记）。
- **口径据实改正**：包内此前把短片描述为"程序化绘制"，对 AI 生成画面属不实陈述，清单、字幕轨、文字稿与双语展示页
  已一律改为"画面由 AI 生成，为概念意象示意——非实景照片、非工程效果图、非官方审定成果"。
- **两处形式差异主动声明**：片中人字之跃表现为下承式桁架并带端部门架，与正文"桥不做塔、不做拱、不做门架"不一致；
  片中铜道钉位于步道中部，正文口径为步道边缘。二者已写入字幕轨与文字稿，并声明形式、尺寸与位置一律以断面图与正文为准。
- **字幕逐条对回正文**：五段下三分之一字幕的每一句均取自 proposal.md 原文口径（含"只跨环路、不跨清河""步道边缘"），
  展示页字幕清单由 16 条更新为 7 条。

## v0.5 - 2026-08-18

- **多模态资产从零补齐**：58 秒静音概念短片（1920×1080/25fps，逐帧程序化渲染，素材取自包内图件与几何）、程序化绘制封面（已设 manifest.cover_image，此前未设）、程序化合成「应答钟」示意音。全部按甲方规则配齐 WebVTT 字幕（与画面烧录字幕逐字一致）、完整文字稿、音频内容说明、poster 与双语标题描述；一律不自动播放；版权声明逐项记录生成方式与权利边界，明确非实景、非实测、非官方审定。
- **英文术语统一**：三轮独立翻译核验发现英文侧五个关键词各有两套写法，按实测计数统一到多数派（Rail-Spike Band / Kilometre-Zero Post / Bell Chime Plaza / Factor Service Station / Breakpoint Living Room），少数派零残留。

## v0.4 - 2026-08-18

- **图册矢量化**：四份 A3/A0 由内嵌位图改为矢量与按落位尺寸编码的共享 JPEG，28.03 MB → 8.68 MB（−69%）；逐页引用位图 570 → 48，分辨率只升不降（A3 规划图 +27%）。同时把 9 张断面与 3 张人视以纯矢量嫁接进册，A3 由 16 页扩至 28 页。70 页全部渲染目视复核，字体缺字 0。
- **展示页数据层**：新增 9 层几何与指标快照的离线内联副本、投影与几何计算核、九视图地图渲染器、三个可运行核验脚本。投影与 pyproj 在 172 个顶点最大偏差 1.5e-9 米；几何复算 70/70 通过；拓扑复算独立命中正文声明的 0.398 / 0.087 平方米。
- 补登记此前被页面引用却未申报的四张概念意象图。

## v0.3 - 2026-08-18

- **断面图纸化**：正文 46 处文字断面落成 9 张断面图 + 3 张街道人视（中英 24 张），尺寸全部引自正文，7 类正文未给数字的部位标「待实测/待核定」不自创。修复版式引擎五处结构性缺陷（英文长度触发的引线不折行、引线穿字、页眉线侵入绘图区等），文字碰撞 94 → 0，并新增基于渲染器实测文字包围盒的检查器。
- **指标扩充**：metrics.json 6 → 87 项（77 known + 10 unknown），全部由可重跑脚本按 EPSG:4548 复算生成、连跑两次字节一致；其中绿地与公共空间面积落在审查脚本自身的交叉复算名单内并通过。空约束层不登记为「0」以免读成假阴性，10 项 unknown 均带 value=null 与原因。

## v0.2 - 2026-08-18

- **空间设计深化**：三处重点区详细设计重写至可交接深度（含九处概念断面、分带宽度与高差处理、材料意向、成本计量口径与量级分档），12 张场景卡升级为可验证协议（验证对象／方法／可量化判据／退出条件／责任角色分离），新增六项无障碍空间读数使「一票否决」有判据可依。正文 20,542 → 58,840 非空白字符。
- **事实性整改**：遗址公园二期已于 2026-08-06 建成开放，全包更新时态；官方已宣告「三道一绿全线无断点贯通」，据此把「断点」限定为**东西向穿越阻隔**（13 号线高架隔断、围墙围栏割裂、桥下无序占用）。
- **命名避让**：法定「一轴」专指中关村大街创新发展轴，本方案自有骨架统一改称「长卷主脊」，正文、指标、几何属性三层贯穿。
- **道钉带数据链**：刻字四字段（模型名称／备案单位／备案编号／备案时间）由国家级公告按批次公开、可独立核对；但公开信息属地仅到直辖市级，故「海淀归属」交由区级主管部门认定并公示，刻字与归属分离，另定双口径交叉核验、维护回路与退路口径。
- **公共空间口径说破**：1.93% 只统计方案新增的概念公共空间，已建成开放的绿廊本体计入绿地（25.04%），合并体验口径 26.97%，两个口径并列不择优。
- 公共空间规模按真实场地容量收敛（钟声广场 2.7 → 1.0 公顷等），并挂接大钟寺先导区已公开的城市更新机制作为空间供给路径（仅作机制建议，不指定地块）。

## v0.1 - 2026-08-18

- 初稿：完成 formal 包全部必交成果（中英正文、三矩阵、9 层几何、双语五图、离线展示页、A3/A0 双语图册）。
- 方向经 5 路红蓝对抗与全量防撞检索裁决为“百年应答”；符号层饱和度（道钉29份/人字265份等）已在正文如实披露，差异化立足机制层（道钉带KPI绑定/应答钟/赶考日公益边界/应答门/进化档案馆/青年策展权）。
- 纳入 2026-08-11 获批的 HD00-1601 等街区控规定位声明：本方案为已批控规之上的概念深化建议，不与法定规划并列。
- 待复核：遗址公园二期实际建成状态（46出入口以实际建成为准）；清华园车站文保范围（零公里桩为待协商概念选点）。
