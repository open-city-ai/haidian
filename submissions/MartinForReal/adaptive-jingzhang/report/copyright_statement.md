# 版权、来源与生成声明 / Copyright, Sources and Generation

方案文字、结构化数据、示意图、离线网页与图纸由 OpenAI Codex（GPT-5 family）在 GitHub 用户 MartinForReal 的授权任务中生成，并经本地确定性脚本复算与验证。成果采用 CC-BY-SA-4.0。外部论文、官方资料和案例仅以引用和事实摘要使用；其版权仍归原权利人，本提交不再分发其图像或全文。

The proposal text, structured data, diagrams, offline viewer, and drawings were generated by OpenAI Codex (GPT-5 family) within the task authorized by GitHub user MartinForReal and were recalculated and checked using local deterministic builders. The submission is licensed CC-BY-SA-4.0. External papers, official materials, and cases are used only as citations and factual summaries; their rights remain with their owners, and no third-party imagery or full text is redistributed.

Generated spatial graphics are conceptual presentation artifacts, not observed site evidence. Provisional source polygons remain explicitly marked. No remote service, tracker, secret map, private dataset, or non-public planning file is embedded.

生成过程按轮次记录。v0.1 至 v0.3 的全部内容由 OpenAI Codex（GPT-5 family）撰写与构建。v0.4 中的以下部分由 Anthropic Claude Opus 5 在同一授权任务中撰写：`proposal.md` 与 `proposal.en.md` 新增的三节正文（复杂适应系统运行模型、黏菌方法的五层阶梯与零抖动对照、系统性失效模式与停止机制对照）、`visual/assets/run-contract-tests.js` 中新增的 `CAS01` 契约用例、`changelog.md` 的 v0.4 记录，以及本段声明本身；其余内容沿用上一轮。两份 `report/*.html` 由仓库脚本 `scripts/render_proposal_html.py` 从上述 Markdown 重新生成，生成后仅把行尾由 CRLF 规范为 LF，正文没有手工编辑。`agent.json` 记录的是本次提交登记的提交方 agent，本轮未作改动；几何、指标、图版、图纸与 PDF 仍由既有确定性构建脚本产出，本轮未被改写。

Generation is recorded here by round. Everything in v0.1 through v0.3 was written and built by OpenAI Codex (GPT-5 family). The following parts of v0.4 were written by Anthropic Claude Opus 5 within the same authorized task: the three new body sections in `proposal.md` and `proposal.en.md` (the complex adaptive system operating model, the five layers of the slime-mould method with the zero-jitter comparison, and the systemic failure modes with their stop mechanisms), the new `CAS01` contract case in `visual/assets/run-contract-tests.js`, the v0.4 entries in `changelog.md`, and this statement. Everything else carries over from the previous round. Both `report/*.html` files were regenerated from that Markdown by the repository script `scripts/render_proposal_html.py`; after generation only the line endings were normalized (CRLF to LF), and no text content was edited by hand. `agent.json` records the submitting agent registered for this submission and is unchanged in this round; geometry, metrics, plates, drawings, and PDFs are still produced by the existing deterministic builders and were not rewritten.

v0.5 是一次更正轮，其中由 Anthropic Claude Opus 5 直接撰写的部分如下，逐个文件列出。权威记录：`visual/assets/regeneration-source.json` 中的 `viewer_sections`（含 `scenarios`、`scenario_fields`、`scenario_field_labels`、`review_gates`、`source_families`、`assumption_limits`、`task_coverage`）、`regional_synergy`、`ui_labels` 的 `regional` 与 `tasks` 两块，以及 `proposal_blocks` 中区域协同与治理登记表两段；`visual/assets/action-governance.json` 的表头标签。构建脚本：`visual/assets/build-proposals.js` 的区域协同段落与治理表压缩逻辑，`visual/assets/build-viewers.js` 的任务覆盖与区域协同两节生成、数量现算与语言泄漏断言。测试：`visual/assets/run-contract-tests.js` 中新增的 `PAR01` 用例及其九条变异探针、被重写为逐字重建的 `G01`、被补上否定式断言扫描与八处真变异探针的 `CAS01`。正文与展示面：`proposal.md` 与 `proposal.en.md` 中分歧边表述的更正、新增的区域创新协同关系一节、压缩后的更新行动治理登记表；`visual/index.html` 与 `visual/index.en.html` 的上述生成节；`changelog.md` 的 v0.5 记录；以及本段声明本身。两份 `report/*.html` 由 `scripts/render_proposal_html.py` 从更正后的 Markdown 重新生成，生成后仅把行尾由 CRLF 规范为 LF，正文没有手工编辑；`changelog.md` 的 v0.5 一节写明该规范化，并据此不勾选 `C10`。其余内容沿用上一轮；`agent.json`、几何、指标、图版、图纸与 PDF 本轮未被改写。

v0.5 is a correction round. The parts written directly by Anthropic Claude Opus 5 are listed here by file. Authority records: in `visual/assets/regeneration-source.json`, the `viewer_sections` block (including `scenarios`, `scenario_fields`, `scenario_field_labels`, `review_gates`, `source_families`, `assumption_limits`, and `task_coverage`), the `regional_synergy` block, the `regional` and `tasks` groups in `ui_labels`, and the regional-synergy and governance-register entries in `proposal_blocks`; the register labels in `visual/assets/action-governance.json`. Builders: the regional-synergy block and the governance-register compression in `visual/assets/build-proposals.js`, and the task-coverage and regional-synergy section generators, the recomputed counts, and the language-leak assertions in `visual/assets/build-viewers.js`. Tests: in `visual/assets/run-contract-tests.js`, the new `PAR01` case with its nine drift probes, the rewritten `G01` that rebuilds the register row by row from the record, and the `CAS01` case extended with a negated-claim scan and eight live mutation probes. Body text and publication surfaces: the corrected disagreement-edge wording in `proposal.md` and `proposal.en.md`, the new regional innovation synergy section, and the compressed renewal action governance register; the generated sections of `visual/index.html` and `visual/index.en.html`; the v0.5 entries in `changelog.md`; and this statement. Both `report/*.html` files were regenerated from the corrected Markdown by `scripts/render_proposal_html.py`; after generation only the line endings were normalized (CRLF to LF) and no text content was edited by hand, which is why the v0.5 entry records that normalization and leaves `C10` unticked. Everything else carries over from the previous round; `agent.json`, geometry, metrics, plates, drawings, and PDFs were not rewritten in this round.

v0.6 是一次独立审计修复轮，其中由 Anthropic Claude Opus 5 直接撰写的部分如下，逐个文件列出。权威记录：`visual/assets/regeneration-source.json` 中区域协同表的审核路径字段（由「负责受理的审查层级」更名为「拟议审核路径（待确认）」）与五行的措辞、`ui_labels` 中 `regional` 的列标签与引言，以及 `proposal_blocks` 中区域协同段落的引言。构建脚本：`visual/assets/build-viewers.js` 中表格横向滚动容器与列数声明的生成、重点区域表标题里加入区域名，以及 `--check` 侧新增的可及名称唯一性、容器与表标签一致、声明列数与实印列数一致三项断言。测试：`visual/assets/run-contract-tests.js` 中新增的 `B04` 用例及其消融漂移探针、被改为逐格核对登记条款的 `CAS01` 及其三条失效模式行探针与两条让步式否定探针、`PAR01` 新增的三条生成表探针。正文与展示面：`proposal.md` 与 `proposal.en.md` 中失效模式表的改写（表头、字段标名与对登记条款的逐字引用）与区域协同表的更正措辞；`visual/index.html` 与 `visual/index.en.html` 的表格与导航样式修复及上述生成节；`changelog.md` 的 v0.6 记录；以及本段声明本身。`visual/assets/action-governance.json` 与 `visual/assets/build-proposals.js` 本轮未改动，失效模式表只是逐字引用登记表中已有的条款。两份 `report/*.html` 由 `scripts/render_proposal_html.py` 从更正后的 Markdown 重新生成，生成后仅把行尾由 CRLF 规范为 LF，正文没有手工编辑；`changelog.md` 的 v0.6 一节据此仍不勾选 `C10`。其余内容沿用上一轮；`agent.json`、几何、指标、图版、图纸与 PDF 本轮未被改写。

v0.6 is an audit-repair round. The parts written directly by Anthropic Claude Opus 5 are listed here by file. Authority records: in `visual/assets/regeneration-source.json`, the regional-synergy review-route field (renamed from the responsible receiving review level to a proposed review route requiring confirmation) and the wording of all five rows, the column label and introduction in the `regional` group of `ui_labels`, and the introduction of the regional-synergy entry in `proposal_blocks`. Builders: in `visual/assets/build-viewers.js`, the generation of the horizontal scroll region and the declared column count for every table, the key-area name added to each key-area table heading, and the three new `--check` assertions covering accessible-name uniqueness, agreement between the region label and the table label, and agreement between the declared and printed column counts. Tests: in `visual/assets/run-contract-tests.js`, the new `B04` case with its ablation drift probe, the `CAS01` case rewritten to check each failure-mode cell against the registered clause it quotes together with its three failure-mode row probes and two concessive-negation probes, and the three new generated-table probes in `PAR01`. Body text and publication surfaces: the rewritten failure-mode table in `proposal.md` and `proposal.en.md` (header, field naming, and the word-for-word quotation of registered clauses) and the corrected regional-synergy wording; the table and navigation style repair and the regenerated sections in `visual/index.html` and `visual/index.en.html`; the v0.6 entries in `changelog.md`; and this statement. `visual/assets/action-governance.json` and `visual/assets/build-proposals.js` were not changed in this round; the failure-mode table only quotes clauses that the register already carried. Both `report/*.html` files were regenerated from the corrected Markdown by `scripts/render_proposal_html.py`; after generation only the line endings were normalized (CRLF to LF) and no text content was edited by hand, which is why the v0.6 entry again leaves `C10` unticked. Everything else carries over from the previous round; `agent.json`, geometry, metrics, plates, drawings, and PDFs were not rewritten in this round.
