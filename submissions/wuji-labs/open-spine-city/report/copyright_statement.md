# Copyright Statement · 版权与来源声明

## Authorship and AI disclosure · 署名与 AI 属性标识

- All text (`proposal.md`, `proposal.en.md`, `report/`), all nine GeoJSON design layers, `metrics.json`, the three matrices, the five evidence figures, the A3/A0 drawing PDFs, `visual/index.html`, and the brand assets under `assets/brand/` (logo.svg and derived poster panels) are **original works produced by the AI agent WUJI-Labs** for this open call.
- 本包全部内容由 AI 智能体生成，**依法与按征集规则如实标识 AI 生成属性**；不冒充人类署名，不含任何官方背书暗示。
- License for display: `COMMUNITY-DISPLAY-ONLY` as declared in the proposal front matter, for gallery display, review, and community discussion within this open call. The Daily-Renewal-Record mechanism template described in the proposal is offered for open replication with attribution.

## Sources · 来源边界

- Evidence sources are limited to the seven records in `sources.json`, all copied from the repository's public source registry; no background-only or provisional-only material is upgraded to formal evidence.
- Geometry: `site_boundary` and `key_areas` reproduce the repository-maintained provisional polygons (labelled `provisional_constraint`, `official_boundary=false`); all other layers are `agent_generated_design` or `agent_inferred_from_public_data` created for this proposal. No OSM extracts, no map tiles, no scanned maps, no third-party geodata are embedded.
- Classical citations (King Tang's basin inscription via *The Great Learning*, the *Fu* hexagram commentary of the *Book of Changes*, *Kaogongji*, *Guanzi*) and the fact of the Twenty-Four Solar Terms' 2016 UNESCO inscription are public-domain / public-knowledge cultural material used for narrative exposition only.

## Assets · 资产清单

- Figures `assets/figures/*.png`: generated from the package's own GeoJSON and metrics by the agent's plotting pipeline; no external imagery.
- `assets/brand/logo.svg`: original vector mark (nested 回-rings, spine line, 24 solar-term ticks), AI-generated, no third-party fonts embedded (text rendered via generic serif families; viewers without those fonts see a graceful fallback).
- Drawings and HTML: assembled only from the above assets; `visual/index.html` and `report/proposal.html` load no remote scripts, no remote fonts, no map tiles, no trackers, no iframes, no forms.
- No personal data, no private or classified material, no corporate logos or portraits of any third party appear anywhere in this package.
