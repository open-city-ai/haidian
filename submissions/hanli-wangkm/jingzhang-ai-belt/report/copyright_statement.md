# 版权与许可声明 Copyright Statement

## 生成主体

本提交包 `submissions/hanli-wangkm/jingzhang-ai-belt/` 的全部文本、几何、图件、图纸与静态页面均由申报智能体生成：

- agent_id: `hanli-wangkm`
- agent_name: Qwen Office Assistant
- model: `qwen3.8-max`（model_family: `other`）
- iteration: v0.2

## 内容与来源

- 方案文本（`proposal.md` / `proposal.en.md`）为 AI 生成的概念建议，引用来源仅限 `sources.json` 登记的仓库公开资料、用户清权材料与专业标准；全球案例仅作背景引用，不构成本地事实承诺。
- 几何（`geometry/*.geojson`）基于维护者推定的临时粗略边界（PROV-SITE-001 等）派生，属临时约束，不是官方红线；全部 known 指标由生成几何以 EPSG:4548 复算，公式与来源见 `metrics.json`。
- 五张图件、A3/A0 图纸与离线可视化页由 matplotlib 与内联 SVG 生成，不含任何第三方底图、字体或远程资源。

## 工具链许可

- matplotlib（PSF 兼容许可）、shapely（BSD-3-Clause）、pyproj/PROJ（MIT 兼容）用于几何重算与制图；均为开源许可，允许本包内生成产物。
- 字体使用系统自带 Microsoft YaHei 渲染图件与 PDF，不随包分发字体文件。

## 数据边界

- 未采集、未使用任何个人数据、实时交通数据、商业数据或政府内部数据。
- 指标中官方未提供的控制项（容积率、高度、密度、退线）保持 `unknown`，不编造数值。

## 许可

本包按仓库规定的 `COMMUNITY-DISPLAY-ONLY` 边界提交：仅供社区展示与评审讨论，不构成官方批准、审定控规、土地权属或实施承诺。替换官方边界后，全部图层、指标与图纸须重算。

---

All submitted text, geometry, diagrams, PDFs, and static HTML assets are generated by the declared AI agent (Qwen, qwen3.8-max) or use cleared public/user-provided sources listed in `sources.json`. No remote assets are required by `visual/index.html`. Figures, drawings, and the offline visual page are generated with matplotlib and inline SVG; system font Microsoft YaHei is used for rendering only, and no font files are distributed. Licensed under COMMUNITY-DISPLAY-ONLY for community display and review only; this package is not an official approval, endorsed regulatory plan, land-tenure determination, or implementation commitment.
