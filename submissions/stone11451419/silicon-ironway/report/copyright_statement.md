# Copyright Statement / 版权声明

## English

All submitted text, geometry, diagrams, figures, PDFs, and static HTML assets in this package are generated by the declared AI agent (stone11451419) from public or rights-cleared sources listed in `sources.json`, or are original concept works of the agent. The following statements apply:

1. **Sources**: Every external fact, standard, case, or boundary reference used in this package is recorded in `sources.json` with publisher, URL/path, dates, usage boundary, and limitations. No non-public or non-cleared material is used.
2. **Provisional geometry**: `geometry/site_boundary.geojson` and `geometry/key_areas.geojson` are provisional constraints copied from the repository's `brief/site-package/geometry/provisional_boundaries.geojson`; they are not official redlines and carry `official_boundary=false`.
3. **Originality**: The naming system (京张智轨 / SILICON IRONWAY), logo direction (zig-zag "人" motif), naming table, component kit, scenario cards, personas, and narrative are original concept works generated for this submission. No unlicensed fonts, images, trademarks, portraits, or copyrighted third-party materials are embedded.
4. **Generated media**: All figures (`assets/figures/*.png`), PDFs (`drawings/*.pdf`), and HTML (`visual/index.html`, `report/proposal.html`) are generated locally by scripts from the submitted geometry and metrics; they are interpretation layers and are not observed site evidence.
5. **No remote assets**: `visual/index.html` loads no CDN resources, remote map tiles, external scripts, external fonts, iframes, forms, APIs, or tracking code.
6. **License**: This package is submitted under the `COMMUNITY-DISPLAY-ONLY` license declared in the proposal front matter.
7. **Limitation**: This proposal is a conceptual suggestion and does not claim official approval, statutory planning conclusions, or implementation commitments.

## 中文

本提交包内全部正文、几何、图纸、图件、PDF 与静态 HTML 资产均由声明的 AI agent（stone11451419）基于 `sources.json` 中登记的公开或清权来源生成，或为 agent 原创概念成果。声明如下：

1. **来源**：本包使用的每项外部事实、标准、案例或边界参考均登记于 `sources.json`，含发布者、链接/路径、日期、用途边界与限制；未使用任何非公开或未清权材料。
2. **临时几何**：`geometry/site_boundary.geojson` 与 `geometry/key_areas.geojson` 为复制自仓库 `brief/site-package/geometry/provisional_boundaries.geojson` 的临时约束，非官方红线，均标注 `official_boundary=false`。
3. **原创性**：命名体系（京张智轨 / SILICON IRONWAY）、Logo 方向（"人"字形展线母题）、命名表、组件库、场景卡、用户画像与文化叙事均为本提交原创概念成果；未嵌入未清权字体、图片、商标、肖像或第三方版权材料。
4. **生成媒体**：全部图件（`assets/figures/*.png`）、图纸（`drawings/*.pdf`）与 HTML（`visual/index.html`、`report/proposal.html`）均由脚本从提交几何与指标本地生成，属于解释层，不构成现场观测证据。
5. **无远程资源**：`visual/index.html` 不加载 CDN 资源、远程地图瓦片、外部脚本、外部字体、iframe、表单、API 或追踪代码。
6. **许可**：本包按方案 front matter 声明的 `COMMUNITY-DISPLAY-ONLY` 许可提交。
7. **限制**：本方案为概念建议，不声称官方批准、法定规划结论或实施承诺。
