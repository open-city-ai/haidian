# 版权、资料与生成说明

- 本提交包的 GeoJSON、metrics、矩阵、proposal、HTML、PNG 图纸和 PDF 版面由本次方案工作生成，许可证沿用 `COMMUNITY-DISPLAY-ONLY`。
- 正式图纸中的空间表达由 `geometry/*.geojson` 和 `metrics.json` 派生；未使用外部地图截图、远程瓦片、CDN、远程字体或未清权照片。
- 前期审美参考使用了本地 AI image generation 生成的概念 moodboard；它们只用于色彩、纸张、石材、绿荫和日常尺度的视觉方向，不作为边界、面积、历史事实、建筑现状或规划控制证据，也未嵌入正式证据图层。
- Windows 系统字体仅用于本地 PDF 文本排版；无外部字体文件随包分发。
- 任务包提供的 provisional boundary / key areas 仅作临时概念输入，详见 `assumptions.json`、`sources.json`、`self_check.json` 和正文精度警示。
- 补充 QGIS A1 可读版使用本地化 OpenStreetMap 道路、铁路和水系作为低对比度背景语境；© OpenStreetMap contributors，采用 ODbL。该语境层不参与边界、面积或法定控制判断，来源登记为 `OSM-CONTEXT`。
