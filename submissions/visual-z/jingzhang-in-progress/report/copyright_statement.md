# 版权、许可与来源说明 / Copyright, Licensing and Sources

本提交的原创文本、结构化设计模型、自绘图、SVG身份资产、HTML与图纸由提交者按 `CC BY 4.0` 许可发布；`proposal.md` 的front matter为许可主声明。外部案例仅作短篇方法概括，未复制其图纸、照片、标志、合同条文或法定参数，原权利归各来源。

临时场地几何来自本仓库 `brief/site-package/geometry/provisional_boundaries.geojson`，只按其明示限制用于生成和拓扑检查。OSM只在仓库basis记录中作为背景交叉核查；若后续分发任何OSM派生数据库或成品，应按OpenStreetMap署名与ODbL分别审查。本构建没有把OSM坐标写入设计图层。

双语图和PDF使用 Noto Sans SC variable subset 渲染，来源为 `notofonts/noto-cjk` commit `f8d157532fbfaeda587e826d4cd5b21a49186f7c`，许可 `SIL Open Font License 1.1`。字体文件没有打包进提交；PDF内只嵌入ReportLab生成的字体子集。外部新闻、政府文件、标准与全球案例详见 `sources.json`，仅用于其 `usage` 字段范围。

The original proposal text, structured design model, diagrams, SVG identity assets, HTML, and drawings are released by the contributor under `CC BY 4.0`; the proposal front matter is the controlling licence statement. Global cases are briefly paraphrased as methods. No external drawing, photograph, logo, contract text, or statutory parameter is copied.

Provisional geometry comes from `brief/site-package/geometry/provisional_boundaries.geojson` and is used only within its stated generation/topology limits. OSM appears only in the repository basis note as a background cross-check; any later distribution of an OSM-derived database or produced work requires separate attribution and ODbL review. No OSM coordinates are incorporated in the design layers.

Bilingual figures and PDFs are rendered with the Noto Sans SC variable subset from `notofonts/noto-cjk` commit `f8d157532fbfaeda587e826d4cd5b21a49186f7c`, under the `SIL Open Font License 1.1`. The font file is not bundled; PDFs contain only ReportLab subsets. Public news, official materials, standards, and global cases are indexed in `sources.json` and used only within each `usage` field.
