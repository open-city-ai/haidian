# Copyright & License Statement

This package is a **formal AI-agent submission** for the Centennial Jing-Zhang AI Innovation Belt open call, coordinated by human participant `silvaling`. All deliverables are generated by the declared AI agent (`agent.json`: Seio / AstrBot-hosted LLM agent) or use cleared public sources listed in `sources.json`.

## Asset-level provenance

| Asset | Generator / Author | License & redistribution |
| --- | --- | --- |
| `proposal.md` / `proposal.en.md` and all JSON matrices (`manifest.json`, `metrics.json`, `compliance_matrix.json`, `standard_matrix.json`, `design_depth_matrix.json`, `assumptions.json`, `sources.json`, `self_check.json`) | AI agent (silvaling), reviewed by human participant | Original work of the submission; displayed under the package license below |
| `geometry/*.geojson` | AI agent, derived from official announcement text and the task book (provisional); sources registered in `sources.json` | Derived design data, provisional only; not official redlines. No third-party geometry is embedded |
| `assets/figures/*.png` (incl. `*.en.png`, `visual-identity*`, `coordination-diagram*`) | AI agent via matplotlib, generated from the package's own geometry and metrics | Original rendered graphics; no third-party images, photos or icon libraries used |
| `report/proposal.html`, `report/proposal.en.html`, `visual/index.html`, `visual/index.en.html` | AI agent (offline static HTML; rendered from `proposal.md` / package data) | Original code; no CDN, remote tiles, external scripts/fonts, API calls or iframes - fully offline |
| `drawings/a3-booklet*.pdf`, `drawings/a0-boards*.pdf` | AI agent via matplotlib | Original print layouts; no embedded third-party assets |
| Fonts | Rendered glyphs only - DejaVu Sans (matplotlib default, SIL OFL) and system CJK fonts (Microsoft YaHei / SimHei / SimSun on Windows; PingFang SC on macOS) used locally at render time; **no font files are embedded in or distributed with the package** | n/a |
| Icons / marks / logo | Original concept drawings by the agent | `JINGZHANG STACK` name and logo are **concept proposals only**, not registered trademarks; clearance required before commercial use |
| Map base | None - all map graphics are self-drawn from the package's own GeoJSON; no commercial map tiles or imagery used | n/a |

## Third-party sources

All referenced sources (official announcement, agent task book, professional standards, public registries) are registered in `sources.json` with their authority level, access date and allowed/prohibited uses. Provisional boundary sources explicitly prohibit precise-area or official-redline use; the package discloses this on every figure, PDF page and HTML section.

## Package license

The package is published under `COMMUNITY-DISPLAY-ONLY` (as declared in `proposal.md` front matter): display within the open-call community context only; any other use requires permission from the author and clearance of referenced public sources per their own terms.

## Contact

Author: `silvaling` (GitHub). Questions about provenance or licensing: open an issue in the repository.
