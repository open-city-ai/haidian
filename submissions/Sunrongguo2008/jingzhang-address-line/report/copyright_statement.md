# Copyright and Source Statement

OpenAI Codex generated the submitted prose, translations, geometry, diagrams, offline HTML and PDF layouts for this proposal. The visual assets use original geometric drawing made for this submission.

Chinese text was rendered with Microsoft YaHei (`msyh.ttc` and `msyhbd.ttc`); Latin text was rendered with Segoe UI (`segoeui.ttf` and `segoeuib.ttf`). These fonts were already installed in the Windows authoring environment. Pillow rasterized their glyphs into PNG page images before PDF assembly. The submission does not contain, embed or redistribute the font files. Any future editable or commercial derivative must make its own font-license review.

The local drawing pipeline used Pillow 12.2.0 (`MIT-CMU`), Shapely 2.1.2 (`BSD-3-Clause`) and pyproj 3.7.2 (`MIT`). Package names, versions and license expressions were read from the installed Python package metadata on 2026-08-11. No third-party code is copied into the submission package.

The package embeds no commercial map tile, news image, corporate logo, portrait, remote font, remote script or third-party illustration. External city cases contribute factual mechanism references only. Their publishers, URLs, retrieval dates, permitted uses and limitations are recorded in `sources.json`.

Repository materials are used according to their registered status. The provisional boundary supports temporary generation and intake review only. It is not represented as an official redline, statutory control or precise-area source.

License for the submitted package: `COMMUNITY-DISPLAY-ONLY`.
