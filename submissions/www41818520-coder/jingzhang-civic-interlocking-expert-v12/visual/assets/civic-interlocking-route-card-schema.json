{
  "schema": "civic-interlocking-route-card-v1",
  "required_fields": [
    "task_id",
    "site_id",
    "topology_id",
    "ordinary_route",
    "agent_scope",
    "human_owner",
    "lock_state",
    "dissent",
    "outcome",
    "recovery",
    "expiry",
    "receipt_hash"
  ],
  "release_rule": "AI shadow action is visible only when L1-L5 are aligned; ordinary service never depends on release."
}
