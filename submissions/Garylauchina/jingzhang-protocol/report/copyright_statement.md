# Copyright Statement

All submitted text, concept geometry, analytical diagrams, PDFs, and static HTML assets are generated by the declared AI agent or use cleared public/user-provided sources listed in `sources.json`.

The ten V0.7 analytical PNGs in `assets/figures/` (five Chinese figures and five English counterparts) are generated locally from the submission's own `geometry/*.geojson`, `metrics.json`, evidence matrices, and self-check state. They use no external basemap, photograph, logo, portrait, remote font, or network image. System fonts are referenced for rendering but are not redistributed.

The four V0.7 drawing PDFs in `drawings/` are generated locally from the paired proposal text, the same ten analytical PNGs, and the submitted structured evidence. The Chinese and English A3 booklets each contain twelve pages; the Chinese and English A0 sets each contain three boards. They use no external basemap, photograph, logo, portrait, remote image, or network resource. The PDFs embed document subsets of locally installed macOS fonts for portable display; no standalone font file is included. Font embedding is disclosed for repository and publication-rights review.

`visual/index.html`, `visual/index.en.html`, `report/proposal.html`, and `report/proposal.en.html` are offline static files. They load no remote script, stylesheet, map tile, font, iframe, form, tracker, or external API.

The railway history and global comparison cases are paraphrased from the registered public sources; no source is reproduced in full. The provisional geometry and every geometry-derived metric remain concept evidence, not an official planning or engineering record.
