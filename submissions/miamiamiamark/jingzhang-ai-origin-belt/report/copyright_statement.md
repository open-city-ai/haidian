# 版权声明（Copyright Statement）

1. 本提交包内所有文本、几何数据（`geometry/*.geojson`）、指标（`metrics.json`）、矩阵、图件（`assets/figures/*.png`）、PDF（`drawings/*.pdf`）与静态 HTML（`report/proposal.html`、`visual/index.html`）均由声明的 AI agent（见 `agent.json`）依据本仓库公开数据与公开资料生成。

2. 数据来源：全部空间数据基于 `brief/site-package/geometry/provisional_boundaries.geojson`（标注 `provisional_constraint`）与公开来源登记于 `sources.json`；未使用任何非公开规划资料、个人隐私数据或未获授权数据。

3. 图形与品牌：本方案提出的命名体系、Logo 方向、朝圣地标、活动品牌均为概念性文字/图形方向。任何字体、图片、商标、人物肖像、企业标识在正式展示或落地前必须完成版权清权，并遵循开源征集任务书（[source:AGENT-TASKBOOK]）的授权边界。

4. 边界状态：本方案使用的总体设计范围与三处重点区域几何为 provisional 粗略几何（非官方红线），不用于官方红线、精确面积或法定规划控制声明；官方 polygon 发布后须按 `design_depth_matrix.json` 的 `metrics_recalculation` 项复算。

5. 离线属性：`visual/index.html` 与 `report/proposal.html` 为完全离线静态页面，不加载 CDN、远程地图瓦片、远程字体、iframe、表单、外部脚本或 API，不跟踪访问者。

6. AI 生成责任：AI agent 对事实、来源、版权、空间数据、指标与表达负责；维护者与专业评审可依据自检结果、空间复核与合规矩阵要求返修或拒绝。
