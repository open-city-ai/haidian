# 方案迭代记录

本文件记录 `submissions/lqqk7/every-sense-jingzhang` 的版本演进。所有条目按仓库提交历史如实登记，不包含尚未完成的工作结论。

> **归档说明。** v7.9 及更早的 52 条版本条目已原样归档于 `visual/assets/changelog-archive.json`，搬移于 2026-08-21（v9.1），逐字节零编辑：归档文件按版本标题分块保存原文，其 `moved_text.sha256` 固定被搬移文本的内容指纹，按 `moved_text.reconstruction` 记载的方法可逐字节还原本文件切除的 138,943 字节。归档动因是 Markdown 单文件 262,144 字节上限——本文件在 v9.0 出包后实测 262,061 字节、仅余 83 字节，任何后续条目都已无法写入。所有条目如实登记的纪律不因归档改变：没有条目被删除或改写，本文件与归档文件合起来仍是完整的迭代记录。

## v9.6 - 2026-08-23

**两份报告页按渲染全链重渲，补齐 v9.0—v9.5 六轮正文落差；重渲前先以 v8.9 时点逐字节复现验证链路；`refcheck` 新增四组配对登记显式检查（F13—F16），护栏由四十七项增至五十四项**

- **本轮驱动**。承接 v9.5 条目末尾如实登记的待处置项——`report/proposal.html` 与 `report/proposal.en.html` 的正文自 v9.0 起未随 `proposal.md`／`proposal.en.md` 重渲。本轮处置该项，并把「同一处登记由两个部件构成、只动了一个」这一在 v9.3、v9.4、v9.5 连犯三次的缺陷类，由记忆改为断言。非任何外部评审、第三方意见或仓库议题提出。

- **落后的根因：渲染链存在但不自动，且此前无人能确认它完整**。两份报告页是 `proposal*.md` 经 `scripts/render_proposal_html.py` → `wpe3/post_render_report.py` → `imgdim.py` 三步生成的产物，三步都需手工触发，正文改动不会自己流到报告页。v8.9 之后正文连改六轮而重渲一次未跑，落差因此逐轮累积：v9.0 改写开篇定位段（「一条线性走廊的方案」改为「双层定位的方案」），v9.1 至 v9.5 各有增删。v9.5 曾就此作出「只补五处读数、不作重渲」的处置，理由是重渲会一次性引入五轮差异，且现行两份 HTML 携带后处理注入的样式与首屏范围警示行，重渲链是否完整包含该后处理未经核验——该理由在当时成立，但它把落差从「六轮」压成「六轮减一处读数」，并使两份 HTML 进入既非 v8.9 亦非 v9.5 的混合态：本轮实测确认，两份 HTML 相对 v8.9 出包版本的差异恰为 v9.5 手工补入的五处读数（中文 diff 十九行、英文 diff 十九行），其余六轮正文改动一处未入。

- **重渲前先验证链路：以 v8.9 时点逐字节复现，差异为零**。本轮不以「跑一遍看着对」为据。先自提交 `ead605ce6`（v8.9 出包）取出整包只读副本，用当前的 `scripts/render_proposal_html.py`（该脚本自 v8.9 至今零改动，`git diff` 为空）对 v8.9 时点的 `proposal.md`／`proposal.en.md` 重跑三步全链，再与同一提交内的 `report/proposal.html`、`report/proposal.en.html` 作字节比对：**中文 287,664 字节对 287,664 字节、英文 335,333 字节对 335,333 字节，`cmp` 零差异，连时间戳类可解释差异都不存在**。这证明三步链条完整包含 v9.5 所存疑的后处理环节——H1 去重、表格列宽与断词 CSS、首屏范围警示行、`assumption`／`self_check` 两类证据标记的补渲染，以及 `imgdim.py` 的图片宽高注入——重渲不会丢失任何一项。链路不可复现旧产物即停手不渲，是本轮的前置条件，该条件已满足。

- **重渲执行与复核清单**。链路验证通过后对当前正文重跑同一条链，逐项复核如下。**图片**：中英各 24 张 `img`，24／24 全部带 `width`／`height` 与 `max-width:100%;height:auto`，与 PG-03 登记的「正文 24／24 张图带图题、报告页双语 24／24 张注入宽高」一致，读数未变故该闸门不动。**后处理三项**：中英各 1 个 `<h1>`（去重成立）、各 1 条 `data-postrender="scope-warning"` 首屏警示行、各 1 处表格 CSS 注入、各 1 个 `post_render_report:v1` 标记。**双语对称**：两份文件行数各 533、`h2` 各 18、`h3` 各 13、`table` 各 42、证据 `sup` 各 336（四类 278 + depth 40 + assumption 与 self_check 合 18），逐项相等。**离线自足**：远程资源引用 0 处，语言切换锚点双向指向正确。**活读数**：v9.5 的桌面推演读数（`· 62 条 / items`、`matching expectation : 62 / 62`）保持在位；此前落后的读数全部补齐——件数条目由 147 更新为 153（英文侧 `all 153 file entries`）、双语自查非空行由 680/680 更新为 676/676、四类锚点自查 278/278、开篇定位段由「一条线性走廊的方案」更新为「双层定位的方案」。**旧值残留**：`147`／`148`／`150`／`152` 个文件条目、`680/680`、`678/678`、`60 条 / items`、「一条线性走廊」逐项检索，中英两侧命中数均为 0。**未改正文**：本轮重渲不需要改动 `proposal.md` 或 `proposal.en.md` 的任何一行正文；两份 md 的唯一改动是 frontmatter 的 `iteration` 由 v9.5 改为 v9.6，值等长，双语文件字节数各自不变（英文仍为 262,136 字节，距 262,144 字节单文件上限仍余 8 字节）。

- **`refcheck` 新增四组配对登记显式检查，动机与清单**。v9.3、v9.4、v9.5 三次失效结构完全相同：一处登记由两个部件组成，本应同批变更而只动了一个——v9.3 是四道闸门的 `remeasured_on` 推进而 `current_reading` 停在 v8.9；v9.4 是件数口径段首句冻结在「143」而同段增量链已走到 152；v9.5 是 PG-11 读数停在 v9.3 而双语 frontmatter 已是 v9.5。原护栏中只有 F12 断言了「双语 frontmatter × changelog 最新版号」这一条配对，覆盖不到其余。本轮新增四组：**F13 版本三联 × PG-11 登记**——把 PG-11 `current_reading` 内的版号并入 F12 所核的同一条链，四处必须逐字相同，v9.5 那次失效正落在这一面上。**F14 闸门活读数 = 实测活值**（三项）——PG-01 的行数、非空行、表格行，PG-02 的七类锚点计数与四类合计，PG-06 的条目数、带摘要条目数、`language` 与 `translation_of` 登记数，中英两侧 `current_reading` 逐值比对此刻的实测值；`remeasured_on` 是无法断言的时间戳，能断言的是读数本身，v9.3 的原型缺陷由此被拦住。**F15 件数口径四联**——`report/copyright_statement.md` 件数口径段的首句声明数、「十类之和恒等于 N」的 N、十类表逐行之和、段尾增量链终值，四者互等并全部锚到 `manifest.json` 条目数，v9.4 的原型缺陷由此被拦住。**F16 报告页正文新鲜度**（双语各一项）——把两份 HTML 剥标签取纯文本，与去 frontmatter 的对应正文按受控模式表逐类提取会随版本漂移的读数（件数条目、双语自查读数、桌面推演控制台块与汇总行、迭代号族）并断言两侧集合逐值相同，本轮所处置的六轮落后由此被拦住。四组均按文件既有的 `chk(名称, 条件, 详情)` 体例书写，编号续 F12，注释逐条写明「哪一次失效使这条断言存在」。

- **一处刻意的脆性如实登记**。F14、F15、F16 用带上下文的具名正则解析登记句与正文句，登记句改写会使正则失配并直接报失败。这是刻意选择而非缺陷：改写读数句式的同一批次必须回来更新断言，否则配对再次脱钩而检查却仍显示通过——「只会通过的自检与没有自检等价」这一结论在 v9.5 已被实证一次，本轮不重蹈。F16 的模式表是有限枚举，只覆盖当前会随版本漂移的印刷读数，今后新增此类读数须同步扩表，该局限同批登记于 `visual/assets/qa-proofing-record.json` 的 R2-09 项。

- **本包实跑结果**。`refcheck.py <包> wp4b` 通过 **54 项、零失败**（原 47 项全部保持通过，新增 7 项全绿）。新增项中无一项在当前包上报红——F14 的三组闸门读数与实测活值逐值相等（行 972／972 按 `wc -l` 口径、非空行 676／676、表格行 307／307；source 88、data 96、metric 80、standard 14、四类合计 278、depth 40、assumption 14、self_check 4；条目 153、带摘要 152、`language` 120、`translation_of` 25），F15 的四联全部等于 153（首句声明数 153、恒等式 153、十类表 13+4+41+4+9+55+18+1+6+2=153、增量链终值 153），F16 双语两侧逐模式全等。F13 在 `iteration` 推进后、PG-11 与本迭代记录更新前曾与 F12 同时报红，两项在同批更新后转绿——这正是该断言的设计意图：版本推进的任一半没跟上，护栏立即失败而不是沉默通过。

- **登记同批更新**。`proposal.md` 与 `proposal.en.md` 的 `iteration` 由 v9.5 改为 v9.6，正文零改动。PG-11 当前读数重算为三者一致于 v9.6，前一读数移入 `superseded_reading` 并写明本轮属正常推进而非查获，同时写明该同步自本轮起由 F13 断言而非由记忆保证，`remeasured_on` 保持 2026-08-23。PG-01、PG-02、PG-03、PG-06 读数经 F14 与人工双向核对，与实测活值一致，本轮不改。`visual/assets/qa-proofing-record.json` 的 R2 记录追加 **R2-09「报告页正文与 proposal 正文的活读数一致性」**，登记方法、工具（`refcheck` F16 项）、范围、双语结果、来源与两条局限，并写明该项为 v9.6 新设及其设立缘由。`report/copyright_statement.md` 本轮无需改动：件数口径四联当前全部为 153，与 `manifest.json` 一致。`manifest.json` 按门禁链固定序重签。

- **本轮不改写任何历史记录**。v9.5 条目中「本轮只对两份 HTML 施加与正文完全相同的五处读数与一处沿革句替换……不作重渲」一句为当时的事实记录与当时成立的处置理由，本轮不改写；PG-11 的两条历史查获案例与既有 `superseded_reading` 原文一律保留，新结论只以追加方式登记。两份报告页正文内出现的 `v9.5` 一处为对 v9.5 一轮工作的历史记述，随正文原样进入重渲产物，不是迭代号落后。

- **字节账**。`report/proposal.html` 287,712 → 290,800（+3,088）；`report/proposal.en.html` 335,347 → 338,011（+2,664）；`visual/assets/package-integrity-gates.json` 103,044 → 103,774（+730）；`visual/assets/qa-proofing-record.json` 140,830 → 144,275（+3,445）；`proposal.md` 218,061 → 218,061（±0）；`proposal.en.md` 262,136 → 262,136（±0，距上限仍余 8 字节）；`manifest.json` 69,186 → 69,186（±0，以 `validation_claim.self_checked=true` 的定稿态计；本轮无条目增删，重签仅改动两份报告页的摘要值，长度不变）。四项合计 +9,927 字节，包体实测终值 41,796,858 字节，距 41,943,040 字节上限余 146,182 字节。工具链侧 `wpref/refcheck.py` 由 10,457 增至 18,427 字节（+7,970），不随包提交，不计入包体。本条目自身与 `changelog.md` 的增量不计入上表（自指），包体终值以刷新后的 `manifest.json` 与提交前实测为准。

## v9.5 - 2026-08-23

**随包自检脚本的内部对抗性审计：四十四组变异实测查获两个校验器的九类静默漏检，其中十六项修前退出码为 0；补入双实现对拍脚本作为 PG-10 闸门两句承诺的可执行体，桌面样例由六十条增至六十二条**

- **本轮驱动**。由维护方对随包自检脚本自身的内部对抗性审计触发，非任何外部评审、第三方意见或仓库议题提出。此前六轮治理回路转动的对象都是「判据写得对不对」（桌面推演自查 v0.2、采用反馈 v0.3.0、独立审查 v0.3.1、外部方案对比 v0.4.0、维护方清点既有登记 v0.5.0），本轮第一次把审计对象换成「自检本身证明了什么」：审计对象为 `visual/assets/seb-tabletop-run.js` 与 `visual/assets/seb-op04-chain-run.js` 两个校验器的实现，不含基准条文；方法是在整包只读拷贝上逐项施加变异并记录退出码与输出差异，共四十四组，另有两组定向针对元检查自身。基准 `seb-spec.json` 一字未改，SEB 版本仍为 v0.5.0，本轮属实现层变更。

- **缺陷类定性：只会通过的自检与没有自检等价**。审计同时确认了两件相反的事。判定逻辑一侧是真的：判据由基准驱动而非写死在工具里，样例期望与判定实现不同源（削弱判据函数、删判定分支、改基准条文均产生不一致），几何篡改灵敏度到毫米级（一个顶点位移一厘米即被捕获），证据链复演器以子进程真实执行桌面校验器并比对其退出码。而「全部通过」这个结论一侧不成立：两个校验器都没有任何期望总数或下界断言，样例条目、声明条目、选点依据、几何线位乃至整张判定码登记表都可以消失而输出零变化。删掉整张 `violation_code_registry.codes` 与从判定函数删掉整条来源对齐判据这两次变异，产出与干净运行**逐字节相同**的输出并退出 0。比漏检更坏的是表述：证据链复演器在未登记码在用时照常打印「二十三个可发码全部登记在基准登记表内」，在几何少一条路时照常打印「五条中心线长度逐条与入档一致」——自检把「没有检查」写成了「检查通过」。

- **变异实证摘要：修前退出码为 0 的漏检十六项，修后无一项仍为 0**。桌面校验器七项：删最后一条样例（59／59）、删中间一条反例样例（59／59）、样例数组清空（0／0）、样例复制为一百二十条（120／120）、删整张判定码登记表（输出逐字节不变）、从判定函数删来源对齐结论（输出逐字节不变）、等级闸门码改为未登记码并同步样例期望（60／60）。证据链复演器九项：删 stage-4 一行、清空声明集、删两条期望理由码、删 derived 一个比值子键、清空选点依据、容差放大至一万米且全线读数平移五十米并使 derived 自洽、几何删 ROAD-002、几何删 ROAD-003、等级闸门码改为未登记码。修后逐项复跑：桌面侧六项落在退出码 2、一项落在退出码 1；复演侧两项落在退出码 2、七项落在退出码 1；十六项无一项仍为 0。

- **九条静默漏检逐条修法**。其一，元检查在自身输入缺席时把自己关掉——`seb-tabletop-run.js` 第 283-285 行由返回空列表改为返回「登记表缺失，码集自检无从执行」并整次拒绝，与第二实现同判。其二，可发码集合与实际发出的码不同源——等级闸门码与分母完整性码从第 154 行起的字面量表移出（理由注于第 143-153 行），改由第 258-261 行读基准值登记；`seb-op04-chain-run.js` 第 649-656 行同法处理字段规则码与等级闸门码。其三，档案自供容差无上界——第 903 行设 `MAX_TOLERANCE_M = 0.01`，第 905-911 行超限即退出码 2；上界取值依据是本包全部长度读数按毫米取整，一厘米已远宽于任何合理的浮点余量。其四，stage-4 是档案除以档案的重言式且打印档案值——第 941 行起留存逐行计算值，第 984-987 行改印计算值，第 1014-1023 行的比值一律读计算值。其五，反例的期望理由码缺席即降级为只比判定——第 1117-1133 行要求期望为 REJECT 的声明必须携带非空期望理由码。其六，`derived` 子键可随档案消失——第 1001-1008 行改为必检键常量清单，接入距离比在 v0.2 与 v0.3 两种口径下键名不同故要求至少声明其一，第 1029、1039 行的条件判断由真值判断改为 `!== undefined`。其七，无总数与下界断言——样例集顶层声明 `expected_fixture_count` 62、`expected_accept_count` 23、`expected_reject_count` 39，`seb-tabletop-run.js` 第 370-406 行逐项比对、不等或为零即退出码 2，第 1336-1340 行把比对结果打印为可读读数；`seb-op04-chain-run.js` 第 921-935 行以「实现侧状态集 × 档案任务清单」推出应有行数并报缺行、多行与重复行，第 745-752 行与第 1100-1111 行分别为选点依据与声明集设零条下界、标识唯一性与至少一条反例下界，第 1181 行收尾条件加入声明数大于零。其八，几何只被正向遍历——第 798-812 行对几何线位、入档长度表与 AI 关闭态分类表三处标识集合作两两双向比对。其九，来源对齐判据零反例覆盖——样例集补入 SM-N1 与 SM-N2 两条反例，分别覆盖强制对齐一侧（节点标识与要素标识相同、未声明来源，`operating_mode` 被改写）与声明来源一侧（推演用标识自行声明来源指向 OP-03，`responsible_role` 被改写），样例集由六十条增至六十二条。

- **四条健壮性缺口与三条表述缺口**。解析失败与结构缺口此前一律裸抛并落在退出码 1，与「有样例不一致」同码：`seb-tabletop-run.js` 第 1148-1163 行分别捕获基准与样例的解析失败、第 1353-1359 行为整个入口加顶层捕获，`seb-op04-chain-run.js` 第 98-108 行为读入函数加捕获、第 87-91 行注册未捕获异常处理，一律改为退出码 2；第 329-337 行列出本工具真正解引用的七条组件与字段路径并由第 339-368 行逐条校验，第 423-424 行使结构性前提先于一切判据执行；样例标识与声明标识的唯一性分别由第 378-386 行与第 1106-1108 行断言。表述侧：第 817 行硬编码的「五条」改由 `roads.length` 生成；码集自检文案本身未改，其真值条件改由同源集合保证；两个工具头部注释（第 55-72 行与第 52-69 行）改写为如实口径——**退出码 1 此后只承载「判定或读数与期望不符」一件事，解析失败、结构缺口与未捕获异常一律落在退出码 2**。另修两条改进项：桌面校验器新增「[E] 组件执行次数」段（第 1325-1332 行），读者据此可看出某组判据本轮是否真的跑过而不只是存在；复演器第 966-973 行去掉行级读数向全局默认值的两层回退。

- **新增 `visual/assets/seb-crosscheck-run.js`（15,262 字节）：闸门承诺的可执行体**。该脚本不作任何 SEB 判定、不接触任何真实参与者或运行中的系统、不写入 `metrics.json`，只把 PG-10 原本以叙述文本记录的读数与承诺改写成可执行断言。三段：**[A] 定点读数**——两个校验器的退出码与全部汇总读数与脚本内写死的期望逐项比对，样例集或声明集变动时脚本与闸门 JSON 必须同批改，「跑了多少条」不再只存在于叙述文本里；**[B] 判定码登记表双向比对**——两个实现各自导出本轮可发判定码全集（`--print-emittable-codes`，默认运行不打印、正常输出一字不变），须逐个登记在基准登记表内，且登记表中每个码都须至少被一个实现可发，只查一个方向会让登记表里长出无人执行的孤儿码；**[C] 同一非法输入两实现同判**——把同一处变异施加在包的一份临时镜像上分别喂给两个实现，退出码必须相等且等于预期，本批五组（对照、删整张登记表、闸门码改未登记码、基准截断为非法 JSON、删 `node_schema` 组件）。镜像建在系统临时目录下并在结束时删除，包内文件只读。

- **新基线读数**。`node seb-tabletop-run.js` 退出码 0，六十二条样例与期望 62／62 一致（通过 23、拒绝 39），汇总段另打印「条目总数复核 : 声明 62 · 实跑 62」；`node seb-op04-chain-run.js` 退出码 0，声明判定 4／4、读数不符项 0，其前置校验器摘录行随之为 62／62；`node seb-crosscheck-run.js` 退出码 0、断言不成立项 0，桌面校验器 40 个可发码、证据链复演器 23 个可发码与基准登记表 40 个登记码双向相等，五组非法输入两实现退出码逐组相等。原六十条样例的字段与期望判定一字未改，逐条判定结果与修前完全相同。

- **一处口径变化如实登记**。翻转一条样例期望这一变异，此前得 59／60、退出码 1；本轮之后因正反两侧声明计数与实到条目不符而**先于判定被拒，退出码由 1 变 2**。这是引入声明式计数的直接后果：数据不自洽的优先级高于判定不一致——一份自身计数就对不上的样例集，不应当先被逐条判定再报「有一条不符」，而应当在判定之前被整次拒绝。

- **未修缺口如实登记，三项**。其一，证据链复演器内 v0.4.0 生命周期、权限边界与测量声明三组判据在本档案输入类型下零覆盖（档案的等级申报、权限声明与测量声明各为零条），约三百行判据代码只靠注释声明「触发即判」；不修的理由是要使其有覆盖必须向 `seb-op04-chain-data.json` 注入本包并不存在的输入类型，属档案语义变更而非实现加固，超出本轮范围；该三组判据在桌面校验器一侧有正反样例覆盖，故是「不在这一实现内」而非「包内零覆盖」。其二，复演器的选点依据条数与声明条数目前只有零条下界与唯一性断言，没有像样例集那样的绝对定数比对，原因是档案尚未声明相应的期望条数键，补键属档案变更，与其一同批处理。其三，桌面校验器的 `skipped` 统计仍只覆盖等级定义、判定规则与双联交接三个组件，不含来源对齐、测量声明与权限边界；本轮以组件执行次数段从另一侧提供了同类信息，跳过统计本身的口径扩展留待下一轮。另有一项刻意不做：审计中被判为「已核无异」的部分不写成新的通过性断言——毫米级灵敏度与前置链真实执行本来就成立，把它们补写成断言只会增加自检的行数而不增加自检的效力。

- **登记同批更新**。变更回执 `CR-2026-08-22-010` 续写于 `visual/assets/seb-change-receipt-v05.json`（该文件因此不再是「一版本一回执」，而是 v0.5.0 基准的条文变更与其实现层变更同处一卷，文件级说明同批改写），九条静默漏检登记为失败样本 **FS-068 至 FS-076**，逐条写明缺陷、触发变异、修前行为、修后行为与修复位置，并在登记块开头写明发现方式——九条全部由内部对抗性审计查获，不是由任何一次正常运行或任何注入样例提出；其中七条的修前行为恰恰是「退出码 0、输出与干净运行逐字节相同」，把它们记成「本工具自查发现」会是第二次同型的自我证明。PG-10 闸门改写：当前读数换为上列三组新读数，旧读数移入 `superseded_reading` 并写明两句承诺曾被实测证伪，`criterion` 保持原文并加注由对拍脚本执行，证据文件与复现命令加入对拍脚本，`remeasured_on` 推进至 2026-08-23，并新增查获案例 PG-10-D5。`manifest.json` 文件条目由 152 增至 153（对拍脚本以 `role=visualization`、`language=neutral` 登记，与两个校验器同类），带摘要条目由 151 增至 152，`language` 登记由 119 增至 120，`translation_of` 保持 25；样例集条目的 `title_zh`／`title_en` 读数同批更正。`report/copyright_statement.md` 三处总数与两处英文段落由 152 改为 153，F 类由 54 增至 55，F 类逐资产登记行与第六节代码与依赖台账各补入对拍脚本一行，件数口径段续写 v9.5 增量句。正文五处读数按同一事实同步：`proposal.md` 与 `proposal.en.md` 的样例条数、正反两侧条数、控制台输出块的三行读数与证据链摘录行；`report/proposal.html`、`report/proposal.en.html` 同五处；`visual/index.html` 与 `visual/index.en.html` 的复现引导句。`visual/assets/qa-proofing-record.json` 的双语结构等价记录同批复测：非空行由 675／675 改为 676／676，行数由 971／971 改为 972／972，表格行与四类锚点均不变，复测说明按既有体例追加 v9.5 一句。PG-06 与 PG-01 读数随之重算，PG-11 一并更正（详见下条）。

- **摘录省略如实标出，双语同位**。桌面校验器的汇总段本轮多打印一行「条目总数复核」，而正文的运行输出摘录明文声明「以下为实际运行结果的节选，省略处以省略号标出」。为不使摘录出现未标注的缺行，双语正文各在同一位置插入一行省略号，行数各增 1、非空行各增 1、表格行未变，PG-01 读数由行 971／971、非空行 675／675 重算为 **972／972、676／676**，表格行 307／307 不变。英文正文出包字节由 262,122 增至 262,136，距 262,144 字节单文件上限余 8 字节；为把增量压在余量内，英文侧「sixty fixtures」与「all sixty verdicts」两处改用数字写法「62」，沿革括号内补入的 v9.5 说明取最短形式。

- **同批查获：PG-11 读数停留 v9.3，v9.4 一轮未随迭代号推进**。本轮核对台账时查获：PG-11（版本号一致性闸门）登记的读数为「三者一致于 v9.3」，而 v9.4 已把双语 frontmatter 与本迭代记录同批推进至 v9.4，闸门读数未随之重算，v9.4 当轮未发现。此为本包第三次同型登记纪律失效——v9.3 查获四道闸门读数停留 v8.9 时点而时间戳先走，v9.4 查获件数口径段锚句停留 v8.6 时点，本轮是闸门读数停留而被判对象已推进。三次的共同结构是：一处登记由两个部件构成，两个部件本应同批变更而实际只动了一个。旧读数移入 `superseded_reading` 并写明查获经过，当前读数重测为三者一致于 v9.5。

- **同批查获且本轮不修：两份报告 HTML 的正文自 v9.0 起未随 `proposal.md` 重渲**。核对正文改动落点时查获：`report/proposal.html` 与 `report/proposal.en.html` 的最后一次内容更新在 v8.9，而 `proposal.md` 与 `proposal.en.md` 在 v9.0 改写了开篇定位段（由「一条线性走廊的方案」改为「双层定位的方案」），此后 v9.0 至 v9.4 五轮的正文变更均未反映到两份 HTML 中。旁证有二：两份 HTML 的版权段仍写「全部 147 个文件条目」／「all 147 file entries」，自查段仍写「非空行 680/680」，两者恰为 v8.9 时点读数（与 PG-01 `superseded_reading` 登记的停留读数「行 974／974、非空行 680／680」吻合），而当前正文为 153 与 676/676。本轮只对两份 HTML 施加与正文完全相同的五处读数与一处沿革句替换（字节增量与正文侧逐字节相等，中文各 +48、英文各 +14），**不作重渲**：重渲会一次性引入五轮正文差异，与本轮「只碰必须碰的」范围不符，且现行两份 HTML 携带后处理注入的样式与首屏范围警示行，重渲链是否完整包含该后处理需单独核验。该缺口在此如实登记为待处置项，不以本轮已改动读数为由略过。

- **本轮不改写任何历史记录**。`changelog.md` v8.x 条目中「反向引用断言所核的 `· 60 条 / items` 与 `matching expectation : 60 / 60` 两行原样保留」一句为当时的事实记录，本轮不改写；该句所指的两行目标读数已按本轮实测更新为 62。`seb-spec.json` 的修订说明、`seb-change-receipt-v05.json` 中 CR-2026-08-18-009 的原文、`seb-change-receipt-op04.json` 全文、PG-10 的旧读数一律原样保留，新结论只以追加方式登记。

- **字节账**。新增 `visual/assets/seb-crosscheck-run.js` 15,262 字节；`visual/assets/seb-tabletop-run.js` 71,289 → 85,824（+14,535）；`visual/assets/seb-op04-chain-run.js` 52,956 → 68,605（+15,649）；`visual/assets/seb-tabletop-fixtures.json` 336,088 → 340,879（+4,791）；`visual/assets/seb-change-receipt-v05.json` 18,800 → 70,879（+52,079）；`visual/assets/package-integrity-gates.json` 91,510 → 103,044（+11,534）；`visual/assets/qa-proofing-record.json` 139,105 → 140,830（+1,725）；`report/copyright_statement.md` 66,193 → 66,994（+801）；`manifest.json` 68,749 → 69,186（+437，以 `validation_claim.self_checked=true` 的定稿态计）；`proposal.md` 218,013 → 218,061（+48）；`proposal.en.md` 262,122 → 262,136（+14，距上限余 8 字节）；`report/proposal.html` 287,664 → 287,712（+48）；`report/proposal.en.html` 335,333 → 335,347（+14）；`visual/index.html` 66,533 → 66,536（+3）；`visual/index.en.html` 73,228 → 73,225（−3）。三个自检脚本的注释增量占其总增量的多数：修法本身约一百五十行，其余为逐条写明「此前是什么行为、哪一次变异证伪了它」的双语注释，用意是使下一位读者不必重跑审计即可看出每一条断言因何存在。本条目自身与 `changelog.md` 的增量不计入上表（自指），包体终值以刷新后的 `manifest.json` 与提交前实测为准。

## v9.4 - 2026-08-22

**件数口径段锚句更正：`report/copyright_statement.md` 起始句停留 v8.6 时点值 143，段尾增量链已正确终于 152，单行内部矛盾，两处数字更正为 152**

- **本轮驱动**。由 v9.3 只读 delta 审计（内部）查获，非外部评审提出。审计核对件数口径段全文时发现：段落首句「十类件数按 `manifest.json` 的 143 条声明逐路径归类得出，十类之和恒等于 143」自 v8.6 写死后，后续九版（v8.7—v9.3）逐版新增文件时只在段尾追加了增量叙述（143→147→148→150→152），从未回改这句起始锚句，形成同一段落内锚句与段尾数值不一致的单行内部矛盾。段尾增量链本身逐版核对无误，终值 152 与 `manifest.json` 当前条目数一致；缺陷仅限锚句未随之更新。本批仅更正锚句两处「143」为「152」，不涉及任何件数、文件或登记行的实质变更。
- **性质归类**。与 v9.3 查获的「时间戳先走」问题同族：`package-integrity-gates.json` 的 `remeasured_on` 曾单独推进而 `current_reading` 未同批重算；本次是锚句单独停留而增量链已同批更新，两者都是「一句话内的两个部件本应同批变更、实际却只有一个部件走了」。教训延续 v9.3 已入册的纪律——锚句与增量链必须同批走，不得只追加尾部、不回改头部。
- **零其他改动**。本轮不涉及 `proposal.md`、`proposal.en.md`、`manifest.json`、图版、证据文件或任何其他登记行；`proposal.md`／`proposal.en.md` frontmatter `iteration` 由 `v9.3` 改为 `v9.4`，等字节。

## v9.3 - 2026-08-21

**制度主体与财政结构两份新文件入包；A 批十六张图版无损重过滤回收 379,628 字节，生成器同批对齐；正文双语挂载因英文字节缺口按停手条款中止并如实登记缺口**

- **本轮驱动**。由 v8.9 城市治理与城市设计概念审查提出的两条优先级问题触发。H3：制度写到了闸门、席位与回避规则，却没有发起主体——评审读到的是一份没有主语的制度，「谁来办」在包内始终为空白。H4：权利写到了底线三项承诺（可见性、值守、预算），却没有说清这三项由谁出钱、削减压力从哪一侧先到——权利没有财政闭环，等于把最不允许被砍的东西挂在最没有承担人的位置上。两条缺口性质相同：机制齐备而承担缺位，因此本轮补的不是新主张，而是既有主张的承担结构。

- **其一：`visual/assets/institutional-scaffold.json`（28,698 字节）**。五类发起主体各给触发条件——属地政府平台类、场地与园区运营方类、高校联合体类、社会组织联合类、多方联合体类——并沿五条配置轴逐类展开差异：A 角色归属、G3 复核席任命路径、回避规则、经费来源与底线暴露面、审批路径。**闭合的是格式空白而不是主体空白**：全部条目状态登记为 `unknown_pending_real_sponsor`，写的是「若发起主体属这一类则制度如何配置」，不是「已经这样配置」；真实主体出现后按四步落位。这一性质在文件内与本条目内均如实写明，不以配置表冒充主体确认。

- **条件化谱系的三条硬判据**。五类之间的差异不是措辞差异，三条判据逐条写死。其一，**高校联合体类的 A 角色必须落在常设执行机构而不是课题组**：课题组随项目周期解散而 A 角色不能随之消失；同类另附一条席位要求，G3 复核席须含非学术界的参与者代表，因为本类的特有风险正是把独立复核做成同行评议。其二，**社会组织联合类必须附一份可执行的资源来源声明**：该类的 A 角色离使用者最近而资源承载力最弱，没有可执行的资源来源，A 角色只是名义。其三，**多方联合体类持有 A 席位的一方须同时承担人力科目**：否则出现负责的人不发工资、发工资的人不负责的结构。

- **回填范围与不可回填项**。四步落位的结果只回填两处：A 角色结果回填 `service-desk-gonogo.json` 的 GD-2（责任角色表 A・R・C・I），经费结果回填 GD-4（两类预算：建设与运维）。**GD-1（场地授权）与 GD-3（许可与保险）明示不在回填范围**：五条配置轴产不出这两项的证据，回填它们等于以制度设计冒充授权文件。

- **零虚构条款**。文件内与台账登记行两处同文写明：五类是类型学不是名单，不点名任何真实机构、企业、高校或社会组织，不表述任何一方已同意、已接洽或已表达意向；截至提交，本方案团队未与任何场地方、运营主体或授权主体发生接触，不存在任何意向、约定或承诺。

- **其二：`visual/assets/finance-structure-model.json`（21,384 字节）**。四科目——建设（FA-1）、年度运维（FA-2）、人力（FA-3）、采购模式（FA-4）——对四种承担机制：公共财政全担、场地方承担、技术供应商承担、混合分担；每种各给激励、风险与对底线三项承诺的具体威胁路径。四条路径的长短与顺序互不相同：财政全担走「预算 → 值守 → 可见性」的连锁，最慢但最整体；场地方承担不经过预算项，直接命中可见性与值守，「不消费也能停留」是第一冲突点；供应商承担最短，合同期满时一次性命中退役连带撤除；混合分担的危险不在断供而在恢复无人——按比例分摊时任一方退出即三项同时部分缺口，而基准只认可削减与不可削减两态。

- **SR-1 与核心配置要求**。五年结构关系三条中，**SR-1 是本文件的核心读数：FA-1 在窗口内只出现一次（退出时再现一次），FA-2 与 FA-3 逐年重复，因此五年口径下的削减压力集中在 FA-2 与 FA-3，而这两项恰好各自对应底线三项中的预算项与值守项——削减压力与底线暴露面在结构上重合**。由此推出一条配置要求：**底线三项须逐项指定单一承担方，允许三项分属不同主体，但任一项都不得按比例分摊。** SR-3 另登记 FA-1 与 FA-2 的比例关系为 unknown，因此任何以「建设已完成、运维可压缩」为由的削减，在本包内不构成可核对的理由。

- **零金额与 unknown 纪律**。两份文件全文不含任何金额、单价、比例、分摊系数或增长率，落包前逐文件正则扫描确认零命中；量级一律以结构与相对关系表达，不可比较者登记 unknown（`institutional-scaffold.json` 9 处、`finance-structure-model.json` 18 处）。五年是本文件自设的观察窗，用途只是把只出现一次的科目与逐年重复的科目分开，不是任何期限承诺；该自陈写在文件内。

- **两文件的引用锚点逐一回包解析**。落包前逐条解析：GD-1／GD-2／GD-3／GD-4 解析至 `visual/assets/service-desk-gonogo.json` 并逐项核对名称语义一致（场地授权／责任角色表／许可与保险／两类预算）；IF-1／IF-2／IF-3 解析至 `visual/assets/sensory-design-code.json`；G0／G3 与 L0—L3 解析至 `visual/assets/seb-spec.json` 与 `service-desk-gonogo.json`；SP-1—SP-5 与 SR-1—SR-3 为两文件自定义标识，在各自文件内定义；引用的五个包内文件（`seb-spec.json`、`sensory-design-code.json`、`service-desk-gonogo.json`、`seb-op04-90day-pack.json`、`proposal.md`）逐一存在。两文件均以 `python -m json.tool` 校验通过后入包。

- **正文双语挂载中止，缺口如实登记**。两段挂载段（段一条件化制度载体、段二五年财政结构）已按从紧口径写就，英文两段合计 4,551 字节（含段间空行），而 `proposal.en.md` 出包前为 262,122 字节，距 262,144 字节单文件上限仅余 22 字节，**净缺口 4,529 字节**。本轮按「不得删事实硬闯」的纪律停手，未挂载任何一侧，中文侧同步不挂，以保双语逐行对齐不变。英文侧可用余量已按四种口径实测：文档内长度 60 字符以上的完全重复句 0 处；重复 n-gram 的净可回收量不足 300 字节，且绝大部分落在脚本运行输出的原文摘录内，属证据原文不可动；重复表格单元 416 字节，全部为专有名词不可删；41 类冗词句式扫描仅命中 5 处。四段样本手工压缩实测（第 19／54／75／834 行合计 7,255 字节）可回收约 95 字节，即 1.3%；第 963 行 2,492 字节可回收约 38 字节，即 1.5%。按该速率外推至全文可压区（正文段落与列表行合计 175,412 字节，表格与代码块为证据原文不动），上限约 2,600 字节，仍差约 1,900 字节，且需在中英两份文件上做约 250 处微改。**结论：v9.2 一轮已取走 3,956 字节的复述去重与 638 字节的结构性收束，同类余量在本版已经见底；再取只能动证据陈列本身，属删事实，本轮拒绝执行。** 两份文件本版以自述字段（`document_zh` / `document_en`）随包，与既有 12 份未挂正文的证据类 JSON 同例，登记于 `manifest.json` 与逐资产台账；正文挂载与其字节筹措另列一轮处理。

- **A 批图版无损重过滤：十六件，回收 379,628 字节，像素零变化**。包内 43 件调色板 PNG 按落盘链路分两批：B 批由 `pngquant` 加 `zopflipng -y --filters=0 -m` 配方收尾，v9.1 及更早已完成；A 批由 Pillow `save(optimize=True)` 收尾，从未过 zopflipng。全 43 件重扫命中 19 件，取其中收益不低于 6 KB 的 16 件按 `zopflipng -y --iterations=5 --filters=0` 重过滤：`key-areas` 37,561 与 35,185、`brand-system` 32,553 与 35,400、`ecosystem-map` 25,306 与 30,994、`enterprise-journey` 22,074 与 28,354、`section-profiles` 21,379 与 23,752、`op04-detail` 20,046 与 17,969、`metrics-evidence-audit` 18,970 与 17,470（以上七题各中英两版，均在 `assets/figures/`），`visual/assets/scene-fallback` 6,393 与 6,222，合计 **379,628 字节**。剩余三件（`dual-state-plan` 中英两版与 `verification-overlay-04-green`）收益分别为 393、72、1 字节，不予取用。

- **零损证据与不重测声明**。16 件逐件以 `magick <f> -depth 8 rgba:-` 解码后比对像素 md5，改写前后 **16／16 完全一致，0 处不符**；原文件 chunk 序列为 IHDR／PLTE／IDAT…／IEND，无任何 ancillary chunk（无 sRGB、gAMA、pHYs、iCCP、tEXt、tRNS），输出 chunk 类型集合逐件一致；位深与调色板不变。因像素逐字节相同，**本批不触发任何图版字号、对比度或可读性读数的重测**，`visual/assets/qa-proofing-record.json` 的相应读数按原值保持；双语三项结构读数（非空行 675／675、表格行 307／307、四类证据锚点 278／278）经复测同样不变，正文自查句因此不作改动。

- **对 v9.1「近零收益」结论的更正**。v9.1「图件再压一张」条登记的判断——「抽样五张，四张零回收、一张仅回收 393 字节，历史批次已把无损熵编码用尽，该档位不再有可用余量」——是抽样有偏的结论：五张样本全部落在 B 批，即已由 zopflipng 配方收尾的文件，其零回收只说明 B 批已到底，不能外推到 A 批。按当前口径的正确表述是：无损档位对 B 批无余量，对 A 批当时尚有 380,094 字节余量。原条目按「已发布条目不重写」纪律保持原样，更正登记于 `visual/assets/package-integrity-gates.json` 新增的 `caliber_notes` 一节（`RCB-01`），A 批基线（`--iterations=5 --filters=0`）与 B 批基线（`-m`）两档口径在该节分别写明。

- **生成器同批对齐与其边界**。A 批 16 件中，唯一在工具链内保有完整 PNG 落盘链路的是 `visual/assets/scene-fallback{,.en}.png`，其生成器 `w88-fb/compose.py` 已同批追加同一条 `zopflipng -y --iterations=5 --filters=0` 后处理，并把候选成品单独落盘以便空跑核对。空跑实测结论如实登记：**该生成器的空跑产物与包内成品像素不一致，且该偏差在本轮改动之前即已存在**——以 v9.2 随包件对比，空跑产物与随包件的解码像素 md5 不同、字节数相差 45（中文版）与 52（英文版），成因在上游的 Pillow `quantize(MEDIANCUT, FLOYDSTEINBERG)` 与当年产出时的 Pillow 版本不同，不在本轮新增的压缩步骤。新增步骤本身经独立验证为无损：量化产物与 zopfli 产物的解码像素 md5 两版均完全一致。因此本条只登记「压缩基线已对齐」，不登记「生成器可逐字节复现包内图」；后者对本文件在 v9.2 时点即不成立。其余 14 件的临时生成源按 `report/copyright_statement.md` C 类登记不随包提交，工具链内无对应脚本可同步，本轮不作虚假声称。

- **登记同批更新**。`manifest.json` 文件条目由 150 增至 152，两件均以 `role=sources`、`language=neutral` 登记，16 件图版的 `sha256` 同批刷新。五处计数按同一事实同步，均为等字节替换——`proposal.md`「全部 152 个文件条目」、`proposal.en.md`「all 152 file entries」、`report/copyright_statement.md`「all 152 file entries」，另含台账保证段两处（「本包全部 152 个登记资产」与「every one of the 152 registered assets」）。台账资产类别总表 F 类由 52 增至 54，件数口径段与两件的逐资产登记行同批补入。`visual/assets/package-integrity-gates.json` 新增 `caliber_notes` 一节。双语 frontmatter `iteration` 由 `v9.2` 改为 `v9.3`，等字节。

- **同批查获：四道自检闸门读数停留 v8.9 时点，时间戳却已被推进**。本轮核对台账时查获 `visual/assets/package-integrity-gates.json` 的一处登记纪律失效：PG-01（双语三项等价）、PG-02（证据锚点解析率）、PG-06（manifest 摘要全覆盖）、PG-11（版本号一致性）四道闸门的 `current_reading` 仍是 v8.9 时点读数，而 `remeasured_on` 已被推进至 2026-08-21——**时间戳先走了，读数没有随之重算**，v9.0 至 v9.2 三轮均未发现。四道闸门的判据本身从未失效：逐轮改动都是双语同增同删，被判对象一直合格；失效的是台账对自身读数的登记。本批按 v9.3 当前版本逐项重测对齐——PG-01 行 974／974 → **971／971**、非空行 680／680 → **675／675**、表格行 315／315 → **307／307**，并补登逐行列数不一致 0；PG-02 四类合计 271／271 → **278／278**（data 90 → 96、metric 79 → 80，六处 data 与一处 metric 均由 v9.2 两段挂载段带入）；PG-06 条目 147 → **152**、带摘要 146 → **151**、`language` 登记 115 → **119**（`visual/assets/changelog-archive.json` 一条未声明 `language`，故为 115 + 4 而非 115 + 5；该件为单件非成对，不构成闸门失败），并补登条目数与包内实际交付文件数 152 全等、条目路径缺失 0；PG-11 三处 v8.9 → **v9.3**。四道闸门的 `remeasured_on` 一并如实置为 2026-08-21，被替换的旧读数按本文件既有体例移入 `superseded_reading_zh` 与 `superseded_reading_en` 并写明漂移成因。其余七道闸门经逐项复核与当前版本一致（PG-03 的正文图题 24／24、报告页宽高 24／24、展示页替代文本 25／25 与图版 47 件均已实测确认），不在本次更正范围。**教训入册：时间戳与读数必须同批走——`remeasured_on` 只能与重算后的读数一同写入，不得单独推进。**

- **字节账**。新增 `visual/assets/institutional-scaffold.json` 28,698 字节、`visual/assets/finance-structure-model.json` 21,384 字节；16 件图版合计由 3,529,146 降至 3,149,518（−379,628）；`report/copyright_statement.md` 64,841 → 66,193（+1,352）；`visual/assets/package-integrity-gates.json` 80,997 → 91,510（+10,513，含 `caliber_notes` 一节与四道闸门的重测更正）；`proposal.md` 与 `proposal.en.md` 双双持平于 218,013 与 262,122（本轮对两文件的改动全部为等字节替换）。包体由 41,939,837 降至 41,639,797 字节，距 41,943,040 字节上限余 303,243 字节。

## v9.2 - 2026-08-21

**外部分岔补齐：三区方案 B 登记与三条外部情景矩阵两份新文件入包，双语正文各挂两段；英文正文字节由 36 处规则密度压缩与两处结构性收束自筹**

- **本轮驱动**。由 v8.9 城市治理与城市设计概念审查提出的优先级问题触发：三处重点区各只登记了一个首选空间组合（方案 A），没有任何被比较掉的对手；未来一侧只有本方案自身的分期与退出机制，没有外部世界分岔的压力测试。两处缺口性质相同——判断看不出是被比较出来的还是被叙述出来的。本批补的是比较与压力测试，不新增任何设计主张、图版、几何与数值来源。

- **其一：`visual/assets/plan-b-register.json`（41,131 字节）**。三处重点区各登记一对方案：众智园「贯通通道 / 边界渗透」、原点社区「独立平行安静线 / 主脊融合设计」、大钟寺「固定片区分工 / 可迁移服务桌网络」。三个 B 案全部由包内既有候选与既有读数推出，不新增任何空间要素。**方案 B 不是稻草人**由原点社区一对承担举证：B 案在夜间安全一维上真实胜过 A 案。A 案（独立平行安静线）除没有服务收入接口、在任何削减压力下居第一顺位外，还把包内已登记的张力 CB-03-T1 铺满全线——静界面判据不设动态发光与主动声音输出，全线按此组织又不与主脊人流相邻，夜间即成一条连续低监视度暗区；而承担自然监视职能的既有条款只对活性界面类写了入口间距上限（`district-strategy.json` 的 `frontage_rules.classes[active_frontage].criteria[0]`），静界面类只要求「每段仍有人工可及入口」，没有对应的间距判据。B 案（融合设计）把低刺激分段落在主脊上，暗区不再连续，自然监护由主脊既有人流与既有人工可及入口提供，不靠新增照明、不靠新增巡查，把全线连续风险降为分段风险。**同时明确写明它不解决 CB-03-T1**：该张力在 `concept-design-register.json` 中的状态仍为 `identified_not_resolved`，B 案只降低风险量级，不改变状态登记。夜间安全一项不给照度数值、不给巡查频次、不作任何人群安全统计结论，凭治安相关方的书面意见判定，未接触任何主体前状态如实登记为 `unknown`。

- **方案 B 登记的两类判据声明与即时可判计数**。三组决策条件均为三元（继续选 A / 切换 B / 两者均放弃），逐条以 `judgeability` 字段标注两类：即时可判者凭书面事实、字段取值或几何事实即可判定，须预注册后可判者才依赖读数与阈值。全文件**即时可判 16 条、须预注册后可判 3 条**，每一对至少含一条即时可判条件（众智园 5+1、原点社区 6+2、大钟寺 5+0）。该比例是判据而不是巧合：本包六项审计读数当前全部为零值、无任何阈值已预注册，条件若全部依赖读数，这份登记在当前状态下没有任何可执行的判断入口。凡涉及读数比较的阈值一律沿用包内既有的三方复核测前书面预注册纪律，不可回溯，本文件不预先给出任何阈值数值——先给数值等于用一个无依据的数字决定一次真实取舍。切换不附加额外程序门槛，与「允许降级、降级不附加额外程序门槛」同口径；第三个分支（两者均放弃）不是修辞，OP-04 的处置是它的包内先例。

- **其二：`visual/assets/scenario-matrix.json`（28,213 字节）**。三条外部世界分岔各登记不变量、可变项与退出条件：SC-1 AI 廉价普及、SC-2 强监管收缩、SC-3 人工服务劳动力短缺。三者失败模式互不相同——第一条是判据被效率论证掉（AI 通道近乎免费、人工通道是唯一有边际成本的一侧时，「使用率太低」成为削减人工窗口的理由），第二条是许可端关闭致使证据既不能产生也不能流通，第三条最锋利，因为它不是外部世界的事实：本方案把最不可削减的一项押在最短缺的一种资源上，这是方案自身结构里最脆的一处。矩阵只有一条统一禁令：情景是调整构件的依据，不是调整判据的依据；三条中任何一条都不得被用作放宽判据、降低阈值或延长宽限的理由，退出亦然。退出的形态是固定的——撤 AI 层，留非 AI 通道与留城资产，基准文本许可不回溯撤销，并留下一份带否定结论的记录与一份变更回执，那份记录本身是成果而不是失败。

- **情景矩阵的不变量来源纪律与退出条件即时可判**。不变量原则上不是本文件新设的，每一条注明来源条款；确有新增者只有 **IV-1-4 一条，条目内明示为本文件新增判据，须按版本治理登记，不得混入既有条款**。第一条分岔另有一处写死：L4b 复审到期日的长度可调，但「等价决定进入、公共增益决定保留」这条既有条款不调——AI 层变便宜不是公共增益，因此不构成续期理由。**退出条件 6 条全部即时可判**（每条分岔 2 条，`judgeability` 均为 `immediate`）：底线某项被声明为可削减、许可被书面拒绝、人工接管角色字段在十节点上均无法合规填写等，三类都不需要任何读数——理由与方案 B 登记同源：一个依赖尚未存在的读数才能触发的退出机制，等于没有退出机制。另登记三条不予收录的分岔（极端气候、人口老龄化、土地市场变化）连同理由，其中人口老龄化的排除理由是它与本方案判据同向，把顺风写进压力测试会让矩阵变成自我恭维。

- **草案阶段与落包复核各查获一类问题**。两文件在起草与返工审核阶段查获并修复了若干处指向包内不存在条目的悬空引用；落包时按同一口径复核一遍，两文件引用的 11 个包内文件与 20 个标识符（CB-03-T1、BK-01—BK-04、OP-01／OP-02／OP-04、PUBLIC-007、ROAD-001／ROAD-005、R-04／R-10／R-12、JZ-01／JZ-04、S02、G0／G3、L4b）全部解析，无一悬空。落包时另查获一处定位冲突：挂载段草案建议把情景矩阵段落在「开放机制表之后、年度公开报告固定字段之前」，而该段正文引用「上文『等价决定进入、公共增益决定保留』一条」——按草案建议的行号落位会把引用放在被引条款之前。按文义锚点改为落在续期判据段之后、年度公开报告段之前，草案的两项文字描述同时成立，引用顺序也正确。

- **双语挂载与英文字节自筹**。两段挂载段中英各一份，落在正文第 263 行（片区策略章，界面规则段之后、强度包络段之前）与第 834 行（更新项目清单章，续期判据段之后、年度公开报告段之前），双语逐行对齐不变。英文正文出包前距 262,144 字节单文件上限仅余 17 字节，格式层零损耗储备已在 v9.0 用尽，挂载段的英文字节只能靠删内容自筹。自筹口径限于概念审查授权的**规则密度压缩**：重复表述去重、同一命题多载体收单（保留证据等级最高的载体）、空话收紧；**不删除任何 [source:]／[data:]／[metric:]／[assumption:]／[standard:]／[depth:]／[self_check:] 锚点与任何事实**，压缩前后七类锚点逐类计数在改写脚本内以断言校验，一处未减。共 36 处行内改写（例如 OP-04 章重复一遍的规范快照版本校验句、品牌章重复一遍的字号与对比度实测读数、蓝绿章重复一遍的三处地标职能、参考文献章重复一遍的案例限度与依据限度、四处重复一遍的「基准四选一」枚举），回收英文 3,956 字节。另有两处结构性收束：执行摘要「命名对照卡」表降格为单段枚举（信息一条不减，表数 43→42、表格行 315→307、数据行 229→223，做法沿用 v8.0.1 把国土空间创新方向表降格的先例），零阶段资源框架表删去四行内容完全相同的「口径」列（该列表述已由表前引句逐字承载，列数 7→6，表格行数不变），两处合计回收 638 字节。英文压缩合计 4,594 字节，挂载 4,589 字节，净减 5 字节。中文侧同步等价：行内压缩 3,238、结构性收束 526、合计 3,764 字节，挂载 4,204 字节，净增 440 字节。

- **登记同批更新**。`manifest.json` 文件条目由 148 增至 150，两件均以 `role=sources`、`language=neutral` 登记。五处计数按同一事实同步——`proposal.md`「全部 150 个文件条目」、`proposal.en.md`「all 150 file entries」、`report/copyright_statement.md`「all 150 file entries」，另含台账保证段两处（「本包全部 150 个登记资产」与「every one of the 150 registered assets」），均为等字节替换。台账资产类别总表 F 类由 50 增至 52，件数口径段与两件的逐资产登记行同批补入。正文双语自查句的三项结构读数按当前版本重算：非空行 681/681 → 675/675、表格行 315/315 → 307/307、四类证据锚点 272/272 → 278/278（data 由 90 增至 96，六处新增全部由两段挂载段所带）；`visual/assets/qa-proofing-record.json` 的三项分项读数与两段复测说明同批重算更新，逐项写明增减来源与口径。双语 frontmatter `iteration` 由 `v9.1` 改为 `v9.2`，等字节。

- **字节账**。新增 `visual/assets/plan-b-register.json` 41,131 字节、`visual/assets/scenario-matrix.json` 28,213 字节；`proposal.en.md` 262,127 → **262,122**（距 262,144 上限余 22 字节）；`proposal.md` 217,573 → 218,013；`manifest.json` 64,429 → 66,031（+1,602）；`report/copyright_statement.md` 63,832 → 64,841（+1,009）；`visual/assets/qa-proofing-record.json` 134,188 → 139,105（+4,917）；`changelog.md` 130,082 → 140,345。包体 41,852,095 → 41,939,837 字节，距 41,943,040 字节上限**余 3,203 字节**。本批未压缩任何图件，也未新增任何图版；下一批若再新增文件，须先按 v9.1 的做法腾出包体余量。

## v9.1 - 2026-08-21

**迭代记录归档重构：v7.9 及更早的 52 条条目逐字节原样搬入 `visual/assets/changelog-archive.json`，`changelog.md` 由 262,061 字节降至 130,082 字节；同批再压一张图版为新增字节腾位**

- **本轮驱动与性质**。出于包维护需要，不新增、不修改任何设计主张、数值、图面内容与来源登记。v9.0 出包时 `changelog.md` 实测 262,061 字节，距 Markdown 单文件 262,144 字节上限仅余 83 字节（上限见 `scripts/validate_submission.py` 第 293 行 `MAX_MARKDOWN_BYTES`，逐文件判据在第 3025 行）。83 字节写不下任何一条版本条目的标题行，v9.0 条目已被迫按余量压缩篇幅并留话「下一批须先定归档策略」——本批先解决这件事，否则此后每一批的改动都无处如实登记。

- **归档落点与白名单依据**。v8.1 曾试过的「changelog 分卷」被提交校验白名单阻断，本批先重查阻断机制，结论是原判断成立且可用范围比记忆更窄。`scripts/validate_submission.py` 第 2951—3007 行按路径逐段分派：包根目录（`len(parts) == 4`）只接受 `proposal.md`、`proposal.zh.md`、`proposal.en.md`、`changelog.md`、`FEEDBACK.md`、`risk.json`、`spatial.json`、`simulation.json` 与第 120—130 行 `PACKAGE_ROOT_JSON_FILES` 九项固定文件名，其余一律落入第 3004 行的兜底错误；`report/` 由第 177—183 行 `ALLOWED_REPORT_FILES` 封闭为五个文件名；`assets/figures/` 与 `visual/assets/` 的扩展名白名单（第 111、184 行）均不含 `.md`。全包唯一接受新增 `.md` 的位置是 `assets/media/`（第 114 行 `ALLOWED_MEDIA_SIDECAR_EXTENSIONS`），但该目录下的 `.md` 被第 711—813 行媒体校验强制要求以 `role=transcript` 登记，即必须把版本归档谎报为某件媒体的文字稿；本批不接受在包元数据里写下不实声明，故弃用该路径。落点因此定在 `visual/assets/changelog-archive.json`：`.json` 在该目录白名单内，单文件上限 524,288 字节（第 294 行 `MAX_JSON_BYTES`），`manifest.json` 的角色词表本就含 `changelog` 一项（`brief/site-package/schemas/manifest.schema.json`），无须借用不相称的角色名；同目录已有 `review-archive.json` 等归档类文件，语义同族。

- **搬移范围与零编辑声明**。搬移 `changelog.md` 原第 122,194—261,137 字节，即 `## v7.9` 起至 `## 待复核事项` 前的 138,943 字节，含 v7.9 至 v1.0 共 52 条版本条目。**逐字节原样搬移，零编辑**：条目正文未经任何改写、重排、删节、合并或格式调整，仅按 `## ` 版本标题切块存入 `entries` 数组；归档文件 `moved_text.sha256` 固定被搬移文本的内容指纹 `8d22498cdaf48e6b653363cac0bcead39a396e0a3a6ee755767389bdd1b8ec4a`，`moved_text.reconstruction` 记载还原方法，按该方法拼回的文本与被切除段逐字节相同（写入前已断言校验通过）。「所有条目如实登记」这条纪律靠零编辑保住，而不靠归档时的取舍——没有任何一条条目被删除、缩写或重述。`changelog.md` 保留 v9.0 起的近期条目与「待复核事项」「公开资料与合规说明」两节，顶部新增一段归档说明指向归档文件并写明还原方法；归档文件头部登记来源文件、搬移日期、动因、零编辑性质、覆盖范围与还原方法，中英双语各一份。两文件都远离各自上限：主文件 130,082 字节（余 132,062），归档文件 144,798 字节（余 379,490）。

- **图件再压一张，为新增字节腾位**。归档使包体净增，本批先测零风险档位：对包内调色板 PNG 抽样跑 zopflipng 无损重压，五张样本中四张零回收、一张仅回收 393 字节——历史批次已把无损熵编码用尽，该档位不再有可用余量。转入有损档位后只动一张：`assets/figures/district-strategy.png` 经 pngquant `--quality=82-96 --speed 1 --strip` 后接 zopflipng `-y --filters=0me`，484,123 → 424,510 字节，**回收 59,613 字节，PSNR 48.26 dB**（判据 39 dB），调色板由 187 色降至 88 色，超出 6% 容差的差异像素 97 个（占 5,625,000 像素的 0.0017%），中英文字、细线与色块经局部放大逐处比对无可见差别。一张即覆盖本批全部新增，其余候选一律不动：`mobility-bluegreen` 两版早前已压到底，本批不再触碰；`assets/figures/scene-*.webp` 六件受 `scene-provenance.json` 与 `manifest.json` 三方哈希绑定，不为几千字节牵动三处登记；`heritage-sequence.png` 等三张在温和档位下再压反而增大，如实弃用。该图的再压参数已补入 `report/copyright_statement.md` 的逐资产登记行，图面内容未改。

- **登记数字同批更新**。`manifest.json` 文件条目由 147 增至 148，新条目 `visual/assets/changelog-archive.json` 以 `role=changelog` 登记。三处正文计数按同一事实同步——`proposal.md`「全部 148 个文件条目」、`proposal.en.md`「all 148 file entries」、`report/copyright_statement.md`「all 148 file entries」，另含台账保证段两处（「本包全部 148 个登记资产」与「every one of the 148 registered assets」），五处均为等字节替换。台账资产类别总表 F 类由 49 增至 50，件数口径段与 F 类逐资产登记行同批补入该文件。双语 frontmatter `iteration` 由 `v9.0` 改为 `v9.1`，等字节。

- **字节账**。`changelog.md` 262,061 → 130,082（切除 138,943、归档说明与本条目合计新增 6,964）；新增 `visual/assets/changelog-archive.json` 144,798；`report/copyright_statement.md` 62,991 → 63,832（+841）；`manifest.json` 63,998 → 64,345（+347，哈希刷新另计）；`assets/figures/district-strategy.png` 484,123 → 424,510（−59,613）；`proposal.md` 217,573 与 `proposal.en.md` **262,127** 两者字节未变，后者距同一上限仍余 17 字节。包体 41,897,529 → 41,852,095 字节，距 41,943,040 字节上限**余 90,945 字节**，较本批开工时的 45,511 字节回升一倍有余；`changelog.md` 距单文件上限余 132,062 字节，后续批次的登记跑道已重新打开。

## v9.0 - 2026-08-21

**双层定位重构：由「沿线走廊」改为「以京张遗址公园廊道为参照系、由邻近节点构成的服务网络」，906 米偏差由定位消化；L4b 新增「公共增益决定保留」的续期判据**

- **本轮驱动与篇幅约束**。由 v8.9 城市治理与城市设计概念审查（内部四视角自审延伸）触发；不新增设计主张、图版、文件与数值来源，改动落在双语正文与 `qa-proofing-record.json`。**本条按余量压缩**：`changelog.md` 出包前距 262,144 字节的单文件上限仅余 4,405 字节且无零损耗回收余地，篇幅不代表工作量；下一批须先定归档策略。

- **其一：定位由单层改为双层**。本包此前自述为「一条线性走廊的方案」、主脊「沿京张遗址公园组织」，而 `alignment-reference.json` 登记的主脊与廊道中位偏差为 906 米：自述与自家读数不符，此前靠重算限定句挂着。本版改为两层：**廊道**是公共参照系与叙事锚点，其人群与议题即服务对象；**网络**是主脊、三处重点区、三处接口与十个完成点构成的邻近网络。口径性质：旧读法要求几何落在廊道上，新读法只要求落在近旁并对廊道人群负责。九处开篇按此口径中英逐段改写：执行摘要、交通章各两段，设计依据、三层范围、总体设计、文化、蓝绿各一段。

- **906 米由「待修正」改为「由定位消化」**。读数一字未改、不重算数值，变的只是其地位：执行摘要补一句——数百米的偏差不是对位误差，正是这一定位的含义。**issue #846 处置口径同步改写**：此前把该偏差登记为待解的对位问题，本版改为由定位本身消化；该 issue 的另一项事实（与已建段不相交、约 412.5 米）与 A-11 的重算限定保留，#1029 不受影响。**另修正两处走廊口径旧译**：`weak cross-corridor` → `cross-street`、`to the whole corridor` → `to the whole site`（同段 `corridor's lowest` 一并改）。

- **其二：L4b 新增公共增益判据**。落位在分期章开放机制表之后，为本版唯一新增段落。复审到期不再按「无违规即续」处理：续期须在三方复核前提交保留理由，把 AI 层在等价前提下的公共增益与其持续成本并置——增益为人工通道容量释放、覆盖时段与语言扩展、失败样本的公共知识；成本为能耗、维护与备件、数据治理工作量、值守者劳动负担。两栏沿用包内既有**预注册纪律**：类目与计量方式由三方复核组在复审期开始前书面确定，事后不得改写口径；**八项全部未测，段内不带数值**。到期回退由惩罚重构为**默认结局**：给不出正向理由、或理由只能由制作方自出，回退即是一个没有公共增益的 AI 层的正常终点，服务按关闭态运行是基准承认的合法结论、不计为事故——与 SEB v0.4.0「停止条件的对象是 AI 层而非服务」同一逻辑，由「停止」推广至「不续期」。

- **结构读数同批更正，由闸门查获**。新增段落使双语非空行由 680 增至 681、四类锚点由 271 增至 272（metric 79→80）。出包前 F5 反向引用断言报四项失败——正文自查句与 `qa-proofing-record.json` 的结构读数仍停在旧值；四处按当前版本重算更正，正文改动为等字节替换。

- **字节账**。`proposal.md` 215,828 → 217,573（+1,745）；`proposal.en.md` 262,140 → **262,127**（−13），距上限余 **17 字节**。英文净减系自筹而非删内容：新增段落初稿毛值 +2,505，段内自身收紧 460 后实际入正文 2,045；既有段收空话回收 102；表格回收 1,956，合计 2,518。表格两项：**90 天资源表口径列四行去重 112 字节**，四行共有的 `not an investment estimate` 已由表下正文承载，删该分句（28 × 4），中文侧同批删「非投资测算，」（18 × 4 = 72）；**英文表格内侧空格全量去除 1,844 字节**，272 行的单元格分隔由 ` | ` 收紧为 `|`（行首尾空格 v8.8 已去），渲染逐字不变属零损耗。该储备本版**一次用尽**（复点 0 行），英文侧此后已无格式层面储备。

## v8.9 - 2026-08-21

**四视角自审驱动的缺陷修复批：把风险条目由八项收口为十二项的全链一致、给「跨轨主证据为零」补上读数限度、把时间轴口径与 OP-04 状态从展示页落到四册与主图，并新增第十一道版本一致性闸门**

- **本轮驱动**。本版不新增任何设计主张、不新增任何图版、不新增任何文件，只做一次自审缺陷修复：四个视角对包内既有登记逐项复核，得到三项主修与九项顺手件。三项主修分别是同一读数在不同印刷面上不一致（风险条目八项与十二项并存）、一处强判断缺少读数限度（执行摘要的「跨轨主证据为零」）、以及一组只在离线展示页出现而未落到 PDF 与主图的口径（时间轴口径与 OP-04 前提状态）。全部修改可逐条追溯到既有登记，没有一处引入新的数值来源。

- **其一：风险条目由八项收口为十二项，全链一次到位**。`risk.json` 的 `risks[]` 自 v8.0 起已是十二条（R-01—R-12，其中 R-09—R-12 由服务等价基准 v0.4 的反向等价、条件差别、到期未复审与退役连带撤除四条引入），`metrics.json` 的 `risk_item_count` 亦已登记为 12，但印刷面与结构化登记上仍有六处停留在八项，本版逐处收口。图版侧：`assets/figures/metrics-evidence.png` 与 `.en.png` 的结构计数格中「风险条目 8 · R-01—R-08」改为「12 · R-01—R-12」，生成器 `build/metricsfig.js` 与 `build/metricsfig2.js` 的同名字面量与页源 `out/fig-{zh,en}.html` 同批改写后按登记配方直渲重出。展板侧：A0 板 04 的关键数格与 A0 板 06 的结构计数行由 8 改为 12，中英各一处。结构化登记侧：`design_depth_matrix.json` 的 `phasing_implementation` 与 `risk_missing_data` 两项，其 `evidence_summary_zh` 中的「R-01—R-08」改为「R-01—R-12」，两项的 `risk_ids` 由八条扩为十二条（G3 闸门逐项核对全部十二条，两项的绑定范围本就应为全集）；`risk_missing_data` 一句同时更正两处旧读数——「九项假设」改为「十二项假设」（`assumptions.json` 现为十二条），并把「绑定代表场景、六个工作包」的范围如实拆写为「十二项逐条绑定触发、停止阈值、责任角色与恢复条件，其中 R-01—R-08 另绑定代表场景、六个工作包与 G0—G3」，因为 R-09—R-12 确实不绑定 JZ 工作包。`standard_matrix.json` 的对应句改为「代表场景 S01、S05、S08 接入 R-01—R-08 中的相关条目与 G0—G3」以消除「八项即全集」的读法。`visual/assets/seb-op04-90day-pack.json` 的 G3 交付条件由「八项风险条目的停止与恢复证据逐项核对」改为「十二项」，与 `proposal.md` 分期章早已写明的「逐项检查 R-01—R-12 全部十二项风险条目」一致。

- **其二：执行摘要的「跨轨主证据为零」补三项读数限度**。该句是本包最强的一处现状判断，此前只给结论不给限度。双语核心矛盾段在该句后各补一句，三项限度逐项取自 `visual/assets/osm-fabric/osm-fabric-readings.json` 的 `crossing_inventory.rail_crossing_focus` 与 `data_quality_notes`：场地内跨在营地面轨道的穿越记录共 8 条，按 50 米单链聚类后为 1 处位置；8 条全部只带高程分层的次级证据（主证据 0 条、无证据 0 条）；快照中的地面轨道要素只覆盖场地纵向跨度约 9.675 公里中的 2.106 公里，即 21.8%。第四句写明未被快照测绘的穿越设施不在本清单内。三项读数逐字与读数文件一致，本版不重算任何数值。

- **其三：时间轴口径与 OP-04 状态由展示页落到四册与主图**。「AI 开启 = AI 关闭」是 G3 验收后的目标状态而非现状读数、49.3% 是现状条件下待消除的差距清单——这一组口径此前只写在离线展示页第 07 章的图版说明里，四册与主图上没有。本版三处落位，措辞均自展示页守则段转录，不新写主张。**A0 板 01**：判词行末补「AI 开启 = AI 关闭 是 G3 验收后的目标状态，不是现状读数」（英文对应句同批补入，两语均落在既有行内，中文由一行增为两行、英文行数不变）；「AI 关闭」图注末补「OP-04 前提待现场审计」；十个运行节点行的 OP-04 由「G0」改为「G0 待审计」。**A3 第 01 页**：底栏限定行末补「49.3% 为现状差距读数，AI 开启 = AI 关闭 为 G3 验收后的目标态」（英文对应句同批补入），既有六项限定一字未删。**`mobility-bluegreen` 主图双语**：图头之下、页头带内的空带新增一行「时间轴口径：49.3% 是现状条件下待消除的差距清单，AI 开启 = AI 关闭 是 G3 验收后的目标状态」（绝对定位，不进入任何既有流，既有元素盒位置逐一复核不变）；03 卡的说明行改写为以 OP-04 的前提状态起句。三处落位前后均按 1 CSS px 精度量测版心：A0 双语六板、A3 双语十六页 `scrollHeight` 与 `clientHeight` 全部相等，`mobility-bluegreen` 双语四张卡片的内容底边全部落在卡内。

- **顺手件九项，逐项登记**。**其一**，`compliance_matrix.json` 三处 `report_sections` 的「服务等价基准 SEB v0.4.0」改为 v0.5.0（中英共六处），与 `proposal.md` 现行章标题一致；同一性质的失效引用另在 `visual/assets/ethics-protocol.json`（三处）、`visual/assets/op04-pilot-readiness.json`（两处）与 `report/copyright_statement.md`（一处）一并更正，归档版本文件 `visual/assets/seb-spec-v0.4.0.json` 内的历史引用按存档纪律不动。**其二**，六个失效的 `self_check_ids` 映射到四门实际检查 ID：`PROFESSIONAL_REVIEW` → `PROFESSIONAL_EVIDENCE`，`KEY_AREAS_TRUST`、`LAND_USE_TOPOLOGY`、`BOUNDARY_TRUST` → `SPATIAL_REVIEW`，`VISUAL_REVIEW`、`VISUAL_STATIC` → `VISUAL_PACKAGING`；映射后逐条去重，`compliance_matrix.json` 20 处、`design_depth_matrix.json` 14 处、`standard_matrix.json` 9 处，三个文件此后不再出现四门以外的检查 ID。**其三**，双语 frontmatter 的 `iteration` 由 v8.5 改为 v8.9，并把该口径固化为闸门（见下条）。**其四**，离线展示页双语的评审路线标题由「八分钟」改为「十分钟」（`eight minutes` 改为 `ten minutes`），与其下逐条计时之和 1+2+2+1+1+1+2 = 10 分钟一致。**其五**，`site-overview` 双语图版统计栏「11.41 km²」后的星号删除：该星号自始无对应脚注，而其正下方的说明行「提交 GeoJSON 临时复算 / Provisional calculation from submitted GeoJSON」已完整承载该限定，删除星号即消除悬空引用，图面其余部分一字未动。**其六**，v8.8 记录中「图上不出现……断面……的任何数值」一句在本条内拆读澄清（不改写历史条目）：三张概念推演图确实不提出任何断面设计，但 CD-01 图面印有通道断面四带取值（导路 2.50 · 触觉 0.30 · 设施 1.20 · 缓冲 1.50 米），该四值逐字引自 `visual/assets/sensory-design-code.json` 的母版卡，在 `concept-design-register.json` 中登记为数据派生层 `band_widths`，属既有值的引用而非本批新提出的数值；该拆读同步写入登记文件的 limitations 一条，车道数、路权划分、停放规模、钟点值、容积、渗透系数、溢流标高、管径与照度仍全部不出现。**其七**，`osm-fabric-readings.json` 的 FAB-03 与 FAB-04 各补一条判读说明，写明中位街块 1387.366 平方米与超大封闭街块占面积 52.324% 并不矛盾：前者按计数取中，分母含 411 个小于 500 平方米的路幅面与人行道面；后者按面积加权，51 个大于 4 公顷的街块贡献 4,968,672.328 平方米。该分布呈双峰，两读数不得互相换算。本条只解释既有读数，不改动任何数值。**其八**，`visual/assets/district-strategy.json` 的入口间距参考标尺补口径成分与弃用理由：该中位数偏向小面一侧的成因、以及面积 ≥1000 平方米子集的中位数 3922.71 平方米（等效方形边长约 62.6 米）为何不被采用——1000 平方米下限本身属方法选择，且该子集排除了真正细粒的小街块；取全部街块中位得到的是更小的上限值，对入口间距而言落在更严的一侧。**其九**，控规批复报道的来源层级下调：`sources.json` 中 `JZ-CORRIDOR-CONTROL-PLAN-APPROVAL-20260811` 的 `source_type` 由 `official_media_report` 改为 `media_report`，`usage` 末补一句层级说明（未取得公告、批复文本或法定图则，故登记在 official_public 之下一级，正文只作为报道引用而非官方结论）；双语正文该表格行的整句加粗同批去除，使版式不再赋予其官方级的视觉权重。

- **新增第十一道自检闸门 PG-11「版本号一致性闸门」，因为同一缺陷已连犯两轮**。`visual/assets/package-integrity-gates.json` 增设 PG-11：判 `proposal.md` frontmatter 的 `iteration`、`proposal.en.md` frontmatter 的 `iteration` 与 `changelog.md` 最新条目版号三者是否逐字相同，任一落后即失败，复演命令为随包可执行的单条 `grep`。该闸门登记两条真实查获：**PG-11-D1**，v8.1—v8.4 连续四个版本 `iteration` 停在 v8.0，v8.5 修正但未建立检查口径；**PG-11-D2**，v8.6—v8.8 连续三个版本停在 v8.5，由本版出包前的自审查获。闸门台账的总数随之由十道改为十一道、有登记案例的闸门由九道改为十道，查获缺陷总数由 27 条改为 30 条（PG-11 两条、PG-07 一条），双语正文自查段、`manifest.json` 的该文件标题与登记文件自身的四处「十道」表述同步刷新。生成器侧的反向引用断言集同批增设 F12 版本号一致性断言（断言总数 46 → 47），使该口径此后由检查而非由记忆拦截。

- **PG-07 口径复点，改正一处自 v8.5 起的漏计**。本版重排四册后按逐页复点 Type3 字体条目，发现 v8.5 登记的清点本身有误：A3 中文册实为 12 个条目而非 11 个，漏计的正是第 2 页的一个 `PingFangSC-Regular` 条目——即已知的未清权字体名称例外应为三处（第 2 页一处、第 15 页两处）而非两处。口径注记按 2026-08-21 的复点整体改写为 A0 中文册 3 个、A0 英文册 0 个、A3 中文册 12 个、A3 英文册 5 个，并登记为 PG-07-D2。其中 A0 中文册由 2 个增为 3 个是本版所致：板 01 新增的「审」字不在该页既有子集内，按包内既有补字机制（限定 `unicode-range` 的追加 `@font-face`，不重建既有子集、既有字形逐像素不变）补入，打印管线将其写为一个思源黑体 Type3 条目，仍在已清权的 OFL 范围之内。本闸门的判据结论未变：四册零非 OFL 嵌入字体程序。三处 `PingFangSC-Regular` 例外的处置去向亦未变，须重出 A3 中文册并补字方可消除。

- **顺带查获并修复一处英文图版截断**。复核 `mobility-bluegreen.en.png` 的 02 / GAP 卡时发现其末段说明溢出卡片并被裁断，图面停在「…is a different (larger) caliber —」，「gap union」的定义整句读者看不到。该缺陷与本轮三项主修无关，属既有版面缺陷，因本版本就要重出该图版而一并修复：该段改写为一句可完整容纳的定义句「Domains overlap; "gap union" = AI-on union minus AI-off union, below the per-node sum.」，中文侧对应段本无溢出，未作改动。修复后该卡内容底边落在卡内，定义句完整可读。

- **B-6 一处已识别表述瑕疵，本版登记不修**。CD-01 三联的中文图面读数条写作「设想入口间距 50.5 m · 街块中位等效边长（参考值）」，中点分隔使后半句读似另一项缺值读数；其原意是同位语——该设想间距即取自本区街块中位规模的等效方形边长（众智园 50.5 米、原点社区 41.0 米、大钟寺 31.1 米），英文侧无此歧义。修正须重出三张概念推演图版，本版据实登记于 `concept-design-register.json` 的 limitations，留待下一批图版重出时一并处理。

- **派生件与量化配方**。三张图版双语重出后，四册内嵌的派生件按既有配方逐件重建并逐字节对账：未受影响的 6 件（`a3-keyareasBand`、`a0-keyareas`、`a0-brand` 各中英一件）与改动前逐字节一致，证明重建参数复原无误；受影响的 12 件重出（`a3-siteBand`、`a3-coverPlan`、`a3-landuseBand`、`a3-mobilityZoom`、`a3-metricsDenom`、`a0-landuse`、`a0-mobility`、`a0-metrics` 各中英一件，其中 `a3-landuseBand` 与 `a0-landuse` 因源图版解码链变动而字节变化、图面内容不变）。**`a0-mobility` 的 JPEG 配方由 q66 改为 q62**，理由与实测如下：本版三张图版重出后包体一度超出 40 MiB 硬线 22,710 字节，须在派生件上找回，且回收量须留出下一版的工作余量。该派生件是 A0 展板 04 的整版底图，也是四册内单件体量最大、原配方质量档最低的一件，故在其上逐档实测后一次调到位：双语合计由 1,838,525 字节（q66）降至 1,741,847 字节（q62），省 96,678 字节；中间档 q64 实测为 1,789,321 字节。逐像素实测 q62 对 q66 的 PSNR 中文 39.43 dB、英文 39.25 dB，RMSE 中文 1.068%、英文 1.090%；差值图经自动色阶放大后残差仅落在文字与线划的抗锯齿边缘，平面色块无带状化，逐区抽查界面三分类色、珊瑚色差集面、闸门徽章色与双语卡片正文均无可见变化。配方值按本包既有命名纪律写在文件名内（`a0-mobility-{zh,en}-w4200q62.jpg`），A0 板 04 双语页源的引用同批更新。生成器的派生件配方表另补入此前遗漏的 `a3-coverPlan` 两件——该件与 `a3-siteBand` 同源于 `site-overview`，此前不在自动重建清单内，须手工补出，本版将其纳入清单以免再次遗漏。

- **字节账**。图版六件：`metrics-evidence.png` 279,849 → 282,592（+2,743）、`.en.png` 290,508 → 291,265（+757）、`mobility-bluegreen.png` 713,604 → 734,951（+21,347）、`.en.png` 730,139 → 737,658（+7,519）、`site-overview.png` 370,607 → 370,336（−271）、`.en.png` 351,906 → 351,694（−212），合计 +31,883。四册：`a3-booklet.pdf` 2,134,551 → 2,134,467（−84）、`a3-booklet.en.pdf` 2,023,919 → 2,023,470（−449）、`a0-boards.pdf` 4,198,326 → 4,172,715（−25,611）、`a0-boards.en.pdf` 4,356,433 → 4,315,492（−40,941），合计 −67,085，页数 16 × 2 与 6 × 2、页面尺寸 420.20 × 297.01 毫米与 1188.89 × 840.99 毫米均未变。正文：`proposal.md` 215,594 → 215,828（+234），`proposal.en.md` 261,994 → 262,140（+146）。**英文正文的 146 字节全部在段内自筹**，未动用表格单元格内侧空格储备：核心矛盾段三处措辞收紧（`the topological fact that` 删去、`both readings are computed` 改为 `both are computed`、`compounds that do not connect to one another` 改为 `compounds that do not connect`）合计省 47 字节，控规行去粗体省 4 字节，闸门数与查获数改写净增 2 字节。终值 262,140 字节，距 262,144 字节上限余 **4 字节**——下一批英文正文的任何增量都须先动用上述储备手段，本条如实登记以免下批误判余量。

- **计数终值**。双语总行数 975 对 975、非空行 680 对 680、表格行 315 对 315、四类证据锚点 271 对 271，四项均与改前一致，逐行结构不一致数为 0；本版正文只在既有行内增删字符，未新增或删除任何行、任何锚点。`assumptions` 占位 14 对 14、`self_check` 占位 4 对 4 亦未变。

- **收口链**。`report/proposal.html` 与 `report/proposal.en.html` 按同一渲染链重渲（逐行比对，双语各只有三段发生变化，与正文改动逐段对应）；`manifest.json` 全部 146 条声明哈希重签，文件条目数与十类件数均未变（git 追踪 147 件）；反向引用断言集 47 项零失败；四门自检全绿。包体终值 41,889,544 字节 = **39.949 MiB**，距 40 MiB 硬线余 53,496 字节。

## v8.8 - 2026-08-21

**概念推演图三张与设想要素登记文件：把重点区、交通与蓝绿三处只到判据层的深化补上一次空间组合，并为这一新体裁写明授权、声明纪律与准入规则**

- **本轮驱动**。一轮外部专业评估列出五项欠缺，其前三项同属一个性质：判据齐备而空间组合缺席——重点区详细设计只到街块诊断与动作类型，没有一次落成的空间组合；交通系统没有一张把轨道、公交、慢行、骑行与货运并置的分层图；蓝绿系统只有三层关系图，没有落在走廊上的分层表达。本版不新增任何判据、不改动任何既有数值与状态，只以**概念推演**这一新体裁把既有判据落成三次空间组合，并把「哪一笔是数据、哪一笔是设想、设想凭哪一条既有判据落笔」逐要素登记。

- **其一：新增三张概念推演图（合计 1,095,460 字节）**。`assets/figures/district-concept-sections.png`（CD-01，415,920 字节）、`transport-system-concept.png`（CT-02，329,002 字节）、`bluegreen-system-concept.png`（CB-03，350,538 字节），三张均为 3000 × 1875 像素、中英双语同图单件，在 `manifest.json` 按 `role=proposal_figure`、`language=neutral`、`required=false` 登记。生成方式与包内既有原生图版一致：手写 HTML 经 Chromium/Playwright 渲染，画布 1200 × 750 CSS 像素、输出倍率 2.5；全部数值、坐标、编号与判据文字直读包内既有结构化文件（`visual/assets/district-strategy.json`、`visual/assets/sensory-design-code.json` 与 `geometry/` 下六个 GeoJSON），不引入任何包外数据源，本轮未接触网络。

- **其二：新增 `visual/assets/concept-design-register.json`（40,896 字节）**。文件按 `role=sources`、`language=neutral` 登记，内容是三张图逐要素的登记账：15 个数据派生图层逐层写明来源文件与字段，22 个设想要素（CD-01 十四个、CT-02 四个、CB-03 四个）逐个写明所凭的既有判据锚点，另登记一处已识别设计张力的处置判据。文件自述其不是设计说明也不是图纸目录：它不解释设计意图，只登记划分与来源。

- **概念体裁的授权与声明纪律**。三张图在最显著位置声明性质后才落笔，纪律共四条，逐条落在图面与登记文件上。其一，**性质声明逐字印在图头之下、任何图版之上**：「建筑体量、通道线位与界面划分均为设计设想，不基于权属、红线或测绘资料，不代表现状或规划真实情况」，中英并置；三张图右上角另有角标「CONCEPT SPECULATION · 非法定规划图」。其二，**图面纪律**：实线为数据派生，虚线为概念设想，两类在同一图例内分组标注，肉眼可辨。其三，**准入规则**：未在登记文件中登记的设想要素不得出现在三张图上，登记条目的 `judgment_source` 指不到包内锚点的同样不得出现。其四，**拒绝清单一字未动**：三张图对权属、红线、控规指标与测绘精度的既有拒绝全部保留，图上不出现车道数、断面、路权划分、停放规模、钟点值、容积、渗透系数、溢流标高、管径与照度的任何数值。

- **与既有片区策略图版的关系：两口径并存，互不改写**。片区策略图版 `assets/figures/district-strategy.png` 承诺「只标类型，不画通道线位」，该承诺在本版继续有效、该图版未作任何改动。本批在概念声明之下把同一批判据落成一次空间组合，因此图上出现通道线位、建筑体量与界面分段。两种口径并存的条件已写入登记文件：既有图版仍是判据层的正式表达，本批只是该判据层的一次概念演示，两者互不改写、互不替代。

- **与「深度倒挂」声明的关系**。本批是倒挂中间层的概念补层。它不改变「无官方资料不给权属级结论」的立场，不把任何设想升格为主张，也不改变包内任何既有判据、数值或状态。该关系同时写在三张图的图注与登记文件的 `declaration` 一节。

- **CB-03-T1：一处已识别设计张力，登记为未解决**。静界面判据要求「不设动态发光、不设主动声音输出」，而夜间断点读数把「照明不足、导视不可读或声环境失序新增的断点」逐处计数；两者作用在同一段界面上，暗区越纯粹，夜间独行者遇到断点的风险越高。该张力由 CB-03 首次写出，但两条条款都是既有条款，张力本身内生于既有判据，不是本图新引入的主张。处置判据同样取自既有条款：夜间断点在静界面段单独取样，静界面仍须有人工可及入口。本文件不提出照度数值、不提出巡查频次、不提出任何人群安全统计结论，也不指认任何具体人群的风险水平；张力状态如实登记为 `identified_not_resolved`。

- **量化配方由 65-98 改为 55-90，理由逐档实测**。三张图沿用 `pngquant` + `zopflipng` 两步，但配方一档，理由登记如下。**65-98**：三图合计 1,336,030 字节，超出当时包体余量 1,229,021 字节，装不下。**40-70**：三图合计 1,108,991 字节，装得下，但逐像素实测发现它**改变了图面的语义色**——金色 `#d6a13b` 被映射为 `(207,150,114)` 的浅赭，绿色被映射为 `(118,137,137)` 的青灰，界面三分类（活性／展示／静）与断面四带的辨色因此失效，不可接受。**55-90**：装得下，且全部品牌色的单通道最大偏差 ≤ 8/255，仅三个孤立像素上的主色偏差为 17/255，浅绿填充偏差 ≤ 12/255，是同时满足「装得下」与「不改语义色」两项的唯一一档。三张图实际装包合计 1,095,460 字节。配方与三档实测读数登记在生成器的配方模块说明中，本条为其如实转录。

- **配套两项，同为保语义色而非为省字节**。其一，三张图的底图层压到**干道与轨道两层**：底图模块把快照要素分为五个绘制层级，本批只铺 `major`（motorway／trunk／primary 一族）与 `rail` 两层，其余层不铺——小面积语义色因此不被调色板挤掉，图注如实说明底图只有路网与轨道、无建筑与水系。其二，图版内唯一一处孤立绿（断面第四带）改用与绿带填充同族的 `#8fae8a`，使其落入同一色簇而不单独占用调色板槽位。两项均为出图时的版面决定，不改变任何数据与判据。**ODbL 履行**：三张图的底图由随包 OpenStreetMap 精简快照派生，故均为 ODbL 1.0 衍生作品，「© OpenStreetMap 贡献者 · ODbL 1.0」署名与快照编号 `osm-fabric-snapshot-20260815` 已印在图面页脚。

- **挂载走混合路线：在 markdown 字节约束下把说明分三层**。英文正文距仓库 256 KiB 单文件上限只余 35 字节，而出图时拟定的双语完整图版说明合计 2,516 字节，正文装不下。本版据此把说明分层：**第一层在正文**，双语各挂三行图引，片区策略节的图引后跟一段三图共用的极简图版说明（性质声明 + 实虚线划分 + 登记文件锚点），交通与蓝绿两处各跟一句回指；**第二层在离线展示页**，三张图分别挂入第 06 章与第 07 章，`figcaption` 承载出图时拟定的完整双语图版说明，展示页不占 markdown 字节；**第三层在登记文件**，逐要素判据来源与划分全文登记。三层同源、详略递增，任何一层都不与另一层冲突。正文三处挂载位置为片区策略节（紧随既有片区策略图版与其图版说明之后）、接驳与走廊分段节末、蓝绿章既有蓝绿三层结构图版之后。

- **英文正文字节自筹明细**。改前 `proposal.en.md` 为 262,109 字节，距 262,144 字节上限余 35 字节；本轮三处挂载、一段共用说明、两句回指与自查段的读数连锁合计净增 1,013 字节，全部在全文自筹，且自筹一律不删事实、不删锚点、不降低可及性。自筹三项：其一，双语正文全部 43 张表的分隔行由 `| --- |` 收紧为 `|---|`，省 382 字节；其二，去掉表格行首尾各一个空格（272 行），省 544 字节；其三，措辞收紧 20 处（如 `Machine-readable location in this package` → `Machine-readable location`、`at the same time` → `at once`、`none constitutes an investment commitment` → `none is an investment commitment`），省 202 字节。三项合计自筹 1,128 字节。前两项属源码排版收紧：离线渲染逐项复核，双语各 43 表、272 数据行、234 表头单元、1,003 数据单元，中英全等，渲染结果与改前完全一致。中文正文同步施行同样两项收紧以保持双语源码体例一致（同省 926 字节，中文侧无字节上限）。终值 261,994 字节，距上限余 150 字节。

- **登记的连锁刷新**。`manifest.json` 文件条目 143 → **147**（三张图与登记文件，四条均 `language=neutral`），全部条目重新签名。`report/copyright_statement.md`：类别总表 C 类 38 → 41、F 类 48 → 49，十类之和 143 → 147；C 类段首件数与图版清单同步更新，逐件登记表新增三行（数据来源与生成方式、ODbL 与快照署名、字体与量化配方、逐图已登记局限各列齐备），F 类新增登记文件一行；作者权利保证的中英两处 143 → 147。`visual/assets/qa-proofing-record.json`：R1-05 范围 44 → 47 件并新增三图字号实测段，R2-01 非空行 674 → 680，R2-04 四类锚点 270 → 271，R2-06 图版 44 → 47 件、双语同图单件 12 → 15，R3-06 `img` 22 → 25 并如实分列实测项与推算项，R2 复测说明按当前版本重算并更正一处存量偏差（v8.4 登记的四类合计 268 未随 v8.7 的 `data` 增量同步）。`visual/assets/package-integrity-gates.json`：PG-01 行 962 → 974、非空行 674 → 680；PG-02 四类锚点 270 → 271；PG-03 正文图与报告页宽高 21 → 24、展示页 `img` 22 → 25、图版件数 44 → 47；PG-06 条目 143 → 147、带摘要条目 142 → 146、`language` 登记 111 → 115。

- **计数终值**。双语总行数 975（`wc -l` 口径 974）、非空行 **680**、表格行 315，逐行结构类型不一致数为 0；四类证据锚点由 270 增至 **271**（`source` 88、`data` 90、`metric` 79、`standard` 14），新增一处 `data` 指向本版新增的登记文件，`depth` 40、`assumption` 14、`self_check` 4 三类未变。正文图引由 21 增至 **24**，双语报告页各注入宽高 24 张；离线展示页 `img` 由 22 增至 **25**，25 张全部带非空 `alt` 与宽高属性；图版文件由 44 增至 **47**。三张新图字号按接收分辨率口径单独实测：页面源逐元素最小 12.0 CSS 像素，按短边压至 768 像素的重采样比 0.4096 折算等效 12.29 像素，不可读文本占比 0%。

- **护栏与四门**。反向引用断言护栏 `refcheck` 通过 **46 项、零失败**；`manifest.json` 重签后 `self_check_submission.py --mark-self-checked` 判为 `formal-review-ready`、可进入正式评审，确定性校验 `changed_files` 147 条与 `manifest.json` 条目数相等；`validate_local_submission.py --strict-manifest` 零错误（唯一告警为临时边界的既有常驻告警）；`participant_preflight.py` 零阻断、零告警。

- **包体终值**。git 追踪文件 147 件、41,896,253 字节 = **39.955 MiB**，距 40 MiB 硬线余 46,787 字节。本波新增合计 1,136,356 字节（三图 1,095,460 + 登记文件 40,896）。

## v8.7 - 2026-08-20

**空间对齐登记层：主脊与遗址公园廊道的关系由文字表述转为可复算读数、正文两处失实表述等字节更正、site-overview 双语图版叠加廊道参考线**

- **本轮驱动与前置侦察定谳**。本版处置的是一处**未登记的失实表述**，不是新增设计内容。前置侦察对四件事作了定谳，本版据此执行。其一，仓库议题 #846 登记的 412.5 米，量的是继承自任务包的官方「总体设计范围」多边形自身与 OSM 测绘公园多边形之间的关系；该公园多边形（`leisure=park`）不在本包任何数据内，本包无从复核该数值，也不以任何读数替代它。其二，本包提交的 `SITE-001` 与官方 `PROV-SITE-001` 逐字节相同，移动该边界被官方三重禁止，故本版**不动边界**，只登记偏差。其三，本包自身的真实问题在主脊：正文两处「本方案的主脊即沿这条已建成公共基底组织」「已建成公共基底上的可达性补足与接口缝合」在包内数据上没有支撑，属未登记失实，本版以读数更正。其四，可用于度量该关系的唯一包内数据是快照中的铁路廊道，其作为遗址公园的位置代理只支持偏差量级参考，该限度须与读数同处登记。

- **其一：新增 `visual/assets/alignment-reference.json`（78,358 字节）**。廊道参考线几何**仅从包内既有 OSM 精简快照派生**，本轮未发起任何新的数据抓取、未引入任何包外数据源、未接触网络；来源指向快照整体 SHA-256 `855296cd…` 与两片分片各自的 SHA-256，选取规则只用 `name` 字段（「京包线」与「京包客专线」），任何人按同一规则可从同一快照得到同一集合（23 条 way、214 个顶点、14,086.884 米）。文件按本包 `osm-fabric-readings.json` 的既有范式组织：声明、来源、定义、代理风险、廊道逐 way 几何（含完整坐标串）、四项读数、交叉引用与复算脚本全文嵌入，脚本无随机数、不依赖集合迭代顺序，同一输入重复执行输出逐字节一致（已实测两次 SHA-256 相同）。ODbL 1.0 署名与相同方式共享声明照既有格式登记。
- **三条声明（双语，落在 `declaration.statement`）**。声明一：本文件所称「遗址公园廊道参考线」是 OpenStreetMap 登记的铁路廊道中心线，不是遗址公园红线，不是公园边界，**不代表真实情况**；公园的法定范围、用地边界与建成范围均不在本包任何数据内。声明二：本包快照的 Overpass 查询只取 `highway` 与 `railway` 两类要素，产生议题 #846 中 412.5 米读数的公园多边形不在本包数据内，本文件因此无法复核该数值。声明三：廊道数据的纬度带止于 40.007178（快照中廊道要素的最北顶点），以北无覆盖，凡纬度高于该值的采样点与节点一律不出读数、不外推、不以到最近端点的距离顶替。
- **代理风险 PROXY-01 与读数同处登记**。廊道参考线由两条命名线合成，其中「京包客专线」17 条 way、11,526.107 米多为新建高速铁路走廊，与遗址公园的关系是空间伴行而不是同一实体；「京包线」6 条 way、2,560.778 米（`usage=tourism` 的 `rail` 与 `disused`，另有一条 `razed`）才是遗址属性更接近的部分。把合成线整体作为公园的位置代理，会把高铁走廊的走向一并计入，故全部偏差数值**只作偏差量级参考**，不得用于判定主脊选线是否正确、不得支持任何与公园边界的对位结论、也不构成设计依据或合规判定。

- **其二：四项偏差读数（ALN-01 至 ALN-04），口径与 DEV-01 完全一致**。沿提交中心线每 25 米取一采样点（末段不足 25 米补一个终点采样），在 EPSG:4548 平面上量算到廊道参考线的最短欧氏距离，分位数按线性插值；唯一新增的限制是上述数据带截断。ALN-01 主脊 `ROAD-001`：中位 905.879 米、P90 1,265.793 米、最大 1,275.264 米、最小 1.810 米、均值 813.089 米，数据带内样本 299 个（全线 379 个，截断 80 个）。ALN-02 安静替代线 `ROAD-005`：中位 1,389.166 米、P90 1,574.371 米、最大 1,693.829 米、最小 232.089 米，数据带内样本 317 个（全线 380 个，截断 63 个）。ALN-03 范围关系：廊道参考线全长 14,086.884 米，落入 `SITE-001` 的 5,441.313 米全部集中在纬度 39.941482 至 39.960482 的场地南段，落在范围外的为 8,645.571 米（占 61.374%）。ALN-04 十个独立完成节点到廊道参考线的距离：带内 6 个（中位 871.164 米、最小 245.280 米、最大 1,539.902 米），带外 4 个（OP-01、OP-05、OP-07、OP-10）逐条标注「超出廊道数据带，不出读数、不外推」并写明理由，不以到廊道北端点的距离顶替——那个距离量的是数据边界而不是与廊道的真实关系。
- **读数复核与前置侦察值的差异登记**。本版四项读数由本轮复算脚本独立算出，与前置侦察的口头值逐项比对：主脊中位由侦察值 900 米复核为 905.879 米（侦察值为百位约数）、P90 1,266 与最大 1,275 相符、最小值 1.810 米（侦察值 2 为个位约数）；数据带内样本数由侦察值 298 复核为 **299**，差异出自数据带上界的取法，本版明确取廊道几何最北顶点纬度 40.007178 并以「小于等于」判定，该口径已逐字写入文件的 `definitions.data_band` 与三条声明之三。安静线中位由侦察值 1,398 米复核为 **1,389.166 米**，差异同源于数据带上界取法。范围外长度 8,645 米复核为 8,645.571 米，相符。凡与侦察值不符者，一律以本轮复算值入档。
- **交叉印证**。ALN-03 与本包 `osm-fabric-readings.json` 的 `DQ.rail_corridor_extent` 同向：快照中的地面轨道要素在场地内只覆盖约 2.106 公里，占场地纵向跨度约 21.763%；两项读数互为旁证，均指向「场地北段在本快照上不存在廊道要素」这一事实。ALN-01 须与 DEV-01 并读：主脊对现状可步行网络的中位偏差为 2.940 米，对廊道参考线的中位偏差为 905.879 米——两者合读的结论是主脊沿现状街道组织，与廊道参考线保持数百米量级的偏差。

- **其三：`metrics.json` 新增三项指标（42 → 45 条）**。`spine_to_heritage_corridor_median_offset_m` = 905.879、`quiet_route_to_heritage_corridor_median_offset_m` = 1,389.166、`site_boundary_to_heritage_corridor_outside_length_m` = 8,645.571，单位一律 `m`（沿用既有 `unit` 取值），`status=known`、`confidence=low`，`assumptions` 逐条写明快照口径、代理线限度、数据带截断与样本数，`calculation_metadata` 登记投影 EPSG:4548、复算触发条件与复算命令。ALN-04 的节点距离表不设指标键：其读数为逐节点表而非单一标量，登记在读数文件本体。
- **其四：`assumptions.json` A-11 由零登记扩写为有数登记，`risk.json` R-06 补停止条件**。A-11 的 `statement` 补入 412.5 米的性质判定（官方框自身与不在包内的公园多边形之间的关系，本包不复核）、不动继承边界的理由（`SITE-001` 与官方 `PROV-SITE-001` 逐字节相同）与本版四项读数的具体数值及代理线限度；`impact` 补入正文表述的更正去向与三项新增指标的 `confidence=low` 与重算条件；`affected_files` 由三条扩为十二条（补入 `roads.geojson`、`constraints.geojson`、`proposal.en.md`、`alignment-reference.json`、`osm-fabric-readings.json`、双语 `site-overview` 图版与双语 A3 册）；`risk_ids` 由 `R-05` 扩为 `R-05` 与 `R-06`。R-06「空间争议」的 `pause_threshold` 双语各补一句：凡把廊道参考线读数用于判定主脊选线正确与否、用于与遗址公园边界对位、或用以顶替官方测绘成果者，即为越界使用，相关工作立即停止并退回读数登记口径；R-05 与 R-06 的 `assumption_ids` 同批补入 `A-11`。

- **其五：正文两处失实表述等字节改写（双语同位）**。第 19 行「本方案的主脊即沿这条已建成公共基底组织」改为「本方案的主脊在继承的临时范围内沿现状街道组织，与快照测得的铁路廊道中位偏差 906 米」，并新挂一处 `[data:visual/assets/alignment-reference.json]` 锚点；第 88 行「已建成公共基底上的可达性补足与接口缝合」改为「现状街道基底上的可达性补足与接口缝合」，同段「在已经开放的公园上检验」改为「在已经开放的公园沿线检验」，A-11 句尾补「主脊相对该廊道参考线的偏差本版已登记为读数」，复用既有 `[assumption:A-11]` 锚点不另增。英文侧逐句对应改写，`the spine of this proposal is organised along that built public ground` 改为 `organised along existing streets inside the inherited provisional extent, a median 906 m off the snapshot-measured rail corridor`，`an already-built public ground` 改为 `the existing street ground`，`on a park already open` 改为 `along a park already open`。图面与正文的显示数值一律按包内三位有效数字规则写作 906 米，全精度读数只在结构化文件与本记录中出现。
- **英文正文字节自筹明细**。改前 `proposal.en.md` 为 262,106 字节，距 262,144 字节上限仅余 38 字节，而三处改写与一处新增锚点净增 164 字节，全部在全文自筹。自筹一律采用措辞收紧，不删事实、不删证据锚点、不改行数，合计回收 161 字节：其一，全文十处 `approximately` 改为 `about`（与文中既有 `about` 用法一致），回收 80 字节；其二，三处 `demonstrate` 改为 `show`，回收 21 字节；其三，两处 `additional` 改为 `extra`（代码块内的第三处不动），回收 10 字节；其四，两处 `shows that` 与一处 `states that` 去掉冗余连词，回收 15 字节；其五，`consists of` 改为 `has`、`concerning` 改为 `on`、`is one of the four` 改为 `is one of four`、`in its own design_role` 改为 `in its design_role`、`can be used to overturn` 改为 `can overturn`，合计回收 35 字节；其六，改动段内两处同义紧缩（`this package's response is that **the correction is already made in its own submitted geometry**` 改为 `**the correction is already made in this package's own submitted geometry**`，`as described in the announcement text` 改为 `the announcement describes`）。终值 `proposal.en.md` 262,109 字节，距上限余 35 字节；`proposal.md` 由 215,440 字节改为 215,614 字节。
- **全包同类表述扫描与逐处判定**。以「主脊沿／已建成公共基底／organised along／built public ground」四组词逐文件扫描全部交付件，命中四处并逐处判定：正文双语两处已按上款更正；`report/proposal.html` 与 `report/proposal.en.html` 由正文重渲得到，随渲染链同步；`visual/assets/sensory-design-code.json` 的 `MO-1`（铁路资产方在场性）原写「主脊沿京张铁路遗址组织」，属同源过强表述，本版改为「主脊在继承的临时范围内沿现状街道组织，与快照登记的铁路廊道参考线中位偏差 905.879 米」并注明该处为 v8.7 更正，双语同做；`changelog.md` 的历史条目按只登记不追改的既定纪律保留原文。离线展示页两处含「遗址公园」的表述经逐句判读为设计意图陈述而非现状对位断言（触觉文化带的布置意图、场景图标题），不属同类失实，本版不动，该判定连同判据一并登记于此。

- **其六：`site-overview` 双语图版叠加廊道参考线，取景一字未动**。`geom_params.site_overview()` 与 `_site_ring` 未作任何改动，五族取景参数不变。图带内在 OSM 底图之上、全部设计线位之下新增一条灰色点划线（`#8a8a8a`，线宽 2.4，`stroke-dasharray="11 3.5 2.5 3.5"`），几何为上述 23 条 way，裁剪至图带矩形之内，落在取景带之外的部分不绘出；图例增一行「遗址公园廊道参考线 / Heritage corridor ref.」，小样以重复渐变还原点划线观感；图幅右上空白带加两行偏差标注，第一行写明该线为 OSM 铁路廊道代理而非公园边界，第二行印中位偏差 906 米、P90 1,270 米、廊道 14,100 米中 8,650 米在总体范围外（三位有效数字）；图注补一句代理声明。新增文字沿用图面既有字号（SVG 标注 12 px、图例 12.6 px），不改变已登记的最小字号读数。
- **版心回归与逐区像素核验**。图注补句一度使 `mapcap` 由一行变两行，而页源 `.wrap` 为 `height:656px; overflow:hidden`、`.body` 为 `flex:1`，多出的一行会压缩下方栏高并裁掉左栏末行。本版据此把双语图注压回一行（中文删「全部」二字并将「灰色点划线」改为「灰点划线」，英文将 `on an OSM road-network basemap; every line, frame and node placed from the submitted GeoJSON` 收紧为 `on an OSM basemap, from the submitted GeoJSON` 并以 `grey dash-dot = OSM rail proxy, not a park boundary` 承载代理声明），并以逐区像素比对确认改动未越界：与改前 `raw` 逐像素比对，页眉区（0–420 px）、页脚与左栏底区（2700–3000 px）、节点表栏（3400,1560 起 1300×1200 px）三处绝对误差均为 **0**，非零差异只出现在图带、图注行与图例栏三处预期改动区。
- **图版与图纸的字节变化**。`site-overview.png` 359,091 → 370,607 字节（+11,516），`site-overview.en.png` 354,907 → 351,906 字节（−3,001，英文图注收紧所致）；量化配方沿用该图版自 v8.4 起的登记配方 `pngquant --quality=40-70 --speed 1` + `zopflipng -y --filters=0 -m`，不因本轮更换配方，以免引入与内容无关的字节漂移。A3 派生件按既有配方重出四件（`a3-siteBand` 与 `a3-coverPlan` 各中英一件），A3 双语册第 02 页的页脚指引行同批补一句代理声明（「图中灰色点划线为遗址公园廊道参考线，OSM 铁路廊道代理，非公园边界」，英文对应），该页图例只列五条提交线位不并入廊道参考线，以免被读作本方案的设计线位；补句后实测该行仍为一行、页面 `scrollHeight` 与 `clientHeight` 相等即无溢出。A3 双语册按同一渲染链重排并合并字体子集，`a3-booklet.pdf` 2,084,915 → 2,134,551 字节、`a3-booklet.en.pdf` 1,978,911 → 2,023,919 字节，页数 16 × 2 与页面尺寸 420.20 × 297.01 毫米均未变。A0 双语展板的六张图源不含 `site-overview`，本版不重排 A0，两个 A0 文件逐字节不变。

- **登记的连锁刷新**。`manifest.json` 文件条目 142 → 143（新增 `visual/assets/alignment-reference.json`，`role=sources`、`language=neutral`、双语标题齐备），全部条目重新签名。`report/copyright_statement.md` 的十类件数 F 类 47 → 48、总数 142 → 143，逐资产登记表补入该文件一行（ODbL 1.0 派生数据库，注明廊道参考线为位置代理而非公园边界），地图数据台账与英文 OSM 段各补一句登记 `site-overview` 新增的廊道参考线，保证段的资产件数 142 → 143。离线展示页双语的 `site-overview` 替代文本与图注补入廊道参考线与三项显示读数。`visual/assets/package-integrity-gates.json` 的 PG-02 由四类合计 268 刷新为 270（`source` 88、`data` 89、`metric` 79、`standard` 14）、PG-06 由条目 142／141 带摘要刷新为 143／142，`language` 登记 110 → 111，两道闸门均加 `remeasured_on: 2026-08-20`，旧读数连同变化原因写入 `superseded_reading`，不以新读数覆盖历史。`visual/assets/qa-proofing-record.json` 的四类锚点读数、A3 双语册字节读数与量测日期同步刷新。

- **计数终值**。双语总行数 963（`wc -l` 口径 962）、非空行 674、表格行 315 三项均未变，逐行结构不一致数仍为 0；四类证据锚点由 269 增至 **270**（`source` 88、`data` 89、`metric` 79、`standard` 14），新增的一处为 `[data:visual/assets/alignment-reference.json]`，双语同位同数；`depth` 40、`assumption` 14、`self_check` 4 三项未变。`assets/figures` 图版文件仍为 44 件。`metrics.json` 指标 42 → 45 条，`assumptions.json` 条目仍为 12 条，`risk.json` 条目仍为 12 条，`sources.json` 来源仍为 44 条（本版全部数据取自包内既有快照，不新增来源）。中文正文 215,614 字节，英文正文 262,109 字节（距 262,144 字节上限余 35 字节）；包体 38.827 MiB（git 追踪字节口径，`participant_preflight.py` 报 38.8 MiB），距 40 MiB 硬线余约 1.17 MiB。
- **护栏与四门**。反向引用断言护栏 `refcheck` 通过 46 项、零失败；双语报告页按 `render_proposal_html.py` → `post_render_report.py` → `imgdim.py` 全链重渲，双语各 21 张图注入宽高；`manifest.json` 重签后 `self_check_submission.py --mark-self-checked`（确定性校验、空间复核、图形封装、专业证据四项全 PASS）、`validate_submission.py` 与 `participant_preflight.py` 三门全绿。

## v8.6 - 2026-08-20

**专业评估驱动的硬伤修复批次：安静线声学表述降格、49.3% 与「AI ON = AI OFF」的时间轴口径加固、零授权验证计划的合规审慎声明与执行暂缓登记**

- **本轮驱动**。本版由一轮外部专业评估驱动，处置其指出的三处硬伤：一处真推理错误（以干道贴线率下声学结论）、一处传播口径冲突（标题的等价主张与主图的收缩读数在快读下互相矛盾）、一处现实敏感性缺口（零授权验证计划未登记辖区合规确认义务）。三处逐项处置如下，评估未指出的内容一律不动。
- **其一：安静线声学表述降格（推理错误更正）**。走廊分段读数此前把「安静替代线干道贴线率 50%、比主脊低 34 个百分点」写成「替代线确实更安静第一次拿到快照证据」。贴线率量测的是到最近城市干道要素的距离，属干道暴露的代理指标，不是声环境实测；由代理指标推出声学结论是一步不成立的推理。本版保留全部读数，把结论降格为「干道暴露显著更低（贴线率 50% 对主脊 84%）」，并写明声学效果属待实测项、噪声读数在零授权现场验证计划的测量范围内。改动落在双语正文走廊分段段与 `visual/assets/district-strategy.json` 的 `segmentation.lines` ROAD-005 条目（该条另登记本次降格的原因，使更正本身可追溯）。
- **全包同类表述扫描**。以「更安静／quieter／声环境更好／噪声更低／贴线率」六组词逐文件扫描交付件：同类结论表述仅存两处（双语正文第 651 行、`district-strategy.json` ROAD-005 读数），均已降格；`visual/assets/sensory-design-code.json`、`visual/index.html` 与 `visual/index.en.html`、`metrics.json` 与其余几何、指标、图版数据文件均无同类结论；图版渲染脚本不引用该结论文字，二十六张图版内不存在该表述，本版因此不涉及任何图版重出。
- **其二：49.3% 与「AI ON = AI OFF」的时间轴口径加固（传播冲突拆弹）**。展示页图题印「AI ON = AI OFF」，主图题印「AI 关闭后可达域收缩 49.3%」，两句并置在快读下互相矛盾。既有正文已声明「差集是审计靶区不是达标声明」，但未说明二者分属不同时点。本版在双语正文可达域段补时间轴口径：49.3% 是现状条件下待消除的差距清单，「AI ON = AI OFF」是 G3 验收后的目标状态，本图度量的是自现状到目标之间的工作量，不是方案的结果。同一句以对应措辞补入 `visual/index.html` 与 `visual/index.en.html` 的可达域守则段，并以 `Timeline caliber` 一条补入 `metrics.json` 的 `ai_off_reachability_shrink_ratio.assumptions`（JSON 不占正文字节，该条写全式）。三处口径一致，读数未变。
- **其三：零授权现场验证计划的合规审慎声明与执行暂缓登记**。`visual/assets/field-verification-plan.json` 新增顶层字段组 `legal_prudence`（双语三条声明加双语执行决定）：零授权是本计划的设计口径而非对任何辖区法律状态的判断；执行者在任何辖区开展前须自行确认当地对公共空间测量、拍摄与访谈活动的法律与管理要求（含铁路沿线区域的特殊管理规定），存在不确定时不执行；本包不构成对任何辖区合规性的判断，也不代替具备资质的法务意见，该条为执行前置条件而非免责措辞。同批把 `status` 由 `plan_ready_execution_pending_all_values_zero_or_unknown` 改为 `plan_ready_execution_deferred_all_values_zero_or_unknown`，`data_backfill.current_state` 双语补登执行决定为暂缓——方法作为方案交付物保留，执行与否及其时点由未来经授权主体在完成合规确认后决定；文件 `version` 由 0.1.0 递增为 0.2.0。双语正文验证计划小节的「计划已备，执行待定」相应改为「计划已备，执行暂缓」并行内补合规确认义务一句；`manifest.json` 该文件的双语标题同步改登。六项读数仍全部为零值或 unknown，本版不产出任何现场数值。
- **专业评估的定性判断与后续批次预告**。评估的定性结论为：本包在验证协议一侧（成对测试、闸门、失败样本纪律、零授权量测办法）的完成度明显高于空间设计一侧，短板不在证据纪律而在空间设计深度。本版接受该判断，但不在本批次内以增写篇幅回应——空间设计深化须有可核对的依据，不能以措辞补足。据此登记后续计划：v8.7 拟处理图版层的口径同步（可达域主图与片区策略图版按本版三处口径复核并按需重出）；v8.8 拟就空间设计深度作一次实质推进，深化对象与可核对依据在开工前先行登记。两条均为计划，不构成任何已完成结论。
- **英文正文字节自筹明细**。改前 `proposal.en.md` 为 262,098 字节，距 262,144 字节上限仅余 46 字节，本版三处新增口径句净增 449 字节，全部在全文自筹。自筹一律采用措辞收紧，不删事实、不删证据锚点、不改行数：其一，改动三段内部收紧十五处（可达域段删与「两口径并列」重复的「任何一方都不替代另一方」、删与末句重复的「不是现状达标声明」，验证计划段删与前半句重复的「一个数字都还没有产出」，其余为同义紧缩）；其二，段外十一处长句同义紧缩，逐章为画像章四处、站点起点条件章与肌理读数章各两处、范围章、分期章、深度倒置章各一处（如 `so the fact that the geometry is relationship-diagram grade is proven` 改为 `so the geometry's relationship-diagram grade is proven`）。中文侧对七处同步执行（四处实质删除加画像章三处同义紧缩），保持双语逐句对等，中文正文由改前 215,038 字节改为 215,440 字节。终值 `proposal.en.md` 262,106 字节，距上限余 38 字节。
- **计数终值**。双语总行数 963（`wc -l` 口径 962）、非空行 674、表格行 315 三项均未变，逐行结构不一致数仍为 0；四类证据锚点 269 持平（`source` 88、`data` 88、`metric` 79、`standard` 14），`depth` 40、`assumption` 14、`self_check` 4 三项未变。正文自查段与 `visual/assets/qa-proofing-record.json` 的锚点读数因此无需改动。
- **护栏与四门**。反向引用断言护栏 `refcheck` 通过 46 项、零失败；双语报告页按 `render_proposal_html.py` → `post_render_report.py` → `imgdim.py` 全链重渲；`manifest.json` 重签后 `self_check_submission.py --mark-self-checked`、`validate_local_submission.py --strict-manifest` 与 `participant_preflight.py` 三门全绿。

## v8.5 - 2026-08-20

**五线独立盲审终报清单驱动的终修批次：七项索引表可追溯性补链、用地要素字段口径补齐、三道闸门口径注记（含 PDF Type3 字体新登记例外）、英文报告图版复用逐张判读、展示页深证据入口**

- **本轮驱动与清单口径**。本版为五线独立盲审终报清单驱动的终修批次。清单待闭合项为两条 S3、六条 S4 与一条 P1 级建议（P1-4），清单对其余各线的结论为无异议。下列条目按清单逐项登记处置，未列入清单的内容一律不动。
- **S3-2 官方七项设计要求索引表的可追溯性补链（三处同做）**。其一，`sources.json` 新增第 44 条来源 `PUBLIC-BRIEF`，对象为 `brief/public-brief.md`，`source_type` 登记为 `user_provided_cleared`、`public_status` 登记为 `public-draft`，并在 `not_usable_for` 中如实写明该文件自述状态为「公开版草案，正式发布前需维护者确认」，因此不是官方公告正文、不是审定结论，也不构成边界、法定控制或实测数据的依据。其二，双语正文索引表段首补 `[source:PUBLIC-BRIEF]` 锚点（中英同位，各一处）。其三，同段补一句口径注：本表七项逐条对应公开任务书草案的七条重点方向、表内标题为节略；官方公告 1.5 按三层范围展开为十个子项、智能体任务书为六项任务，两套口径均已由 `compliance_matrix.json` 逐条覆盖（该文件当前 23 条要求，含 1.5.1.1—1.5.3.3 十个子项与一条重点区必选项总条、`agent.1`—`agent.6` 六项）。
- **来源数 43 → 44 的连锁刷新**。逐资产权利台账编制口径段的「四十三条来源」改为「四十四条来源」；A0 双语展板第 06 板与 A3 双语册第 09 页的「43 条登记来源」读数同步改为 44，四册按既有渲染链原样重出并逐册以文本层比对确认差异仅此一处（`a0-boards.pdf` 4,198,324 → 4,198,326 字节、`a0-boards.en.pdf` 4,356,433 字节不变、`a3-booklet.pdf` 2,084,915 字节不变、`a3-booklet.en.pdf` 1,978,912 → 1,978,911 字节）；「27 条官方公开材料」未变，新增条目不属 `official_public`。QA 记录的 A3 英文册字节读数随之刷新。
- **S3-1 `geometry/land_use.geojson` 字段口径补齐**。LU-001—LU-004 四个要素补 `official_boundary: false` 与 `evidence_status: "concept_only"` 两字段，取值与字段位序逐字跟随 `buildings`／`constraints`／`green_space`／`phasing`／`public_space` 五个几何文件的既有体例（五文件全部要素该两字段取值一致）。此前该口径只写在四个要素的 `rollback_state_zh`／`rollback_state_en` 文本里而未落为字段，属登记形式缺口而非口径冲突。本次为纯属性插入，几何坐标零改动：改前改后四个要素 `geometry` 对象的规范化 SHA-256 均为 `f6ac07737a254c1fe05f4a3e613b1d2ce78a24b0e5d7989a6c15395e4d75c7c6`。
- **`geometry/roads.geojson` 同口径补齐**：五条线位要素同批补 `official_boundary: false` 与 `evidence_status: "concept_only"` 两字段（与 land_use 同一体例与位序），几何坐标经规范化哈希比对零改动；至此六个自绘几何文件的证据状态字段口径全部一致。
- **双语正文 frontmatter `iteration` 由 v8.0 改为 v8.5**。v8.1—v8.4 四轮均未同步该字段，属登记漏改；本版一次改正，正文其余内容不受影响，该字段改动为零字节增量。
- **逐资产权利台账补 manifest 自条目摘要豁免说明**。编制口径段的 `manifest.json` 登记项后补一句：`manifest.json` 自身条目按结构性排除不带 `sha256`，自引用不可能自含自身哈希，该排除已登记于随包自检闸门 PG-06 的判据。此前台账只声明覆盖全部文件条目，未说明这一条结构性例外。
- **三道自检闸门补口径注记（`visual/assets/package-integrity-gates.json`）**。三条一律只登记既有事实，不新增检查、不产生任何指标数值。
  - PG-06（manifest 摘要全覆盖）补 `deterministic_validation.changed_files` 口径：该字段是校验器相对基线分支列出的差异文件清单，本包相对基线为整体新增，故清单恒等于包内全部交付文件（当前 142 条，与 `manifest.json` 条目数相等），不是某一版的改动清单，逐版改动仍以本文件为准。
  - PG-07（PDF 字体审计）补 Type3 判据口径，并把当前读数由「四册零非 OFL 条目」精确为「四册零非 OFL 嵌入字体程序」。本闸门判定的对象是嵌入的字体程序（`FontFile`／`FontFile2`／`FontFile3`），四册中此类条目仍只有 Source Han Sans SC 的 Regular／Medium／Bold 三个子集。除此之外打印管线还会写出 Type3 字体条目：Type3 不含任何字体程序，字形以 PDF 路径算子逐个写入页面，原判据未覆盖该类。逐册清点为 A0 中文册 2 个、A0 英文册 0 个、A3 中文册 11 个、A3 英文册 5 个。**本轮由此查获一处此前未登记的例外**：A3 中文册第 15 页的两个 Type3 条目（各只落一个字形），其 `FontDescriptor` 的 `FontName` 为 `PingFangSC-Regular`，即两个字符在渲染时回退到 macOS 系统字体后由打印管线转为轮廓路径。该两条不随包分发任何字体程序，因此不构成字体文件再分发，但描述符名称已越出本包已清权的 OFL 范围。本版如实登记为已知例外，处置待定：消除该例外需重出 A3 中文册并把这两个字形补进思源黑体子集，而生成链的字体子集步骤（`wp4b/mkfont.py` 与 `build/tokens.js`）在本次运行环境中已缺失，本版无法完成补字，故不作未经验证的改动。
  - PG-08（QA 记录条目自洽）补量测脚本口径：`qa-proofing-record.json` 的读数由一次性量测脚本产出，这些脚本不属于交付件、不随包分发；条目登记方法、工具类型、日期与逐项结果，读数按方法可复算，但本闸门不提供可直接重跑的随包命令（与 PG-10 登记 `reproduction_command` 的口径不同），相应读数一律以「方案方自行执行的自查」计。
- **英文报告页复用五张图版的逐张判读与登记**。`report/proposal.en.html` 内嵌的 `tactile-corridor-map`、`district-strategy`、`heritage-sequence`、`green-section-principle`、`bluegreen-structure` 五张图版无英文单独版本。逐张判读结论为：五张均属图内中英逐行并置的双语单版图，标题、图例、判据行、注记与底图署名各带英文对照行，英文侧因此直接共用同一文件，不构成语言缺口，也不需要另出英文版；五张此前已按 `manifest` 的 `language=neutral` 登记、不设成对件，本版把上述判读结论与一条已登记局限（少量二级注记如坐标系与快照批次以中文为主、英文对照为节略）补入 `qa-proofing-record.json` 的 R2-06 条目。此项按 QA 登记处理，不占用英文正文字节。
- **P1-4 展示页深证据入口**。`visual/index.html` 与 `visual/index.en.html` 首屏标题区各加一个直达按钮，链接到第 16 章交互场景（`#chapter-16`）——该章是评审可亲手切换 AI 开关并复演 OP-04 证据链的入口。按钮体例跟随既有导航（同色板胶囊样式、纯 CSS、无脚本、键盘可聚焦），双语各一处；无头截图自检确认 1440 × 900 视口下首屏版式未变形、导航条与评审路线区块位置不变。
- **英文正文字节自筹明细**。本版新增的英文口径注句净增 306 字节，而改前 `proposal.en.md` 距 262,144 字节上限仅余 56 字节，故按删除测试在全文自筹。自筹一律采用措辞收紧，不删事实、不删证据锚点、不删控制台样例条目、不改行数：其一，画像章与显示位数章共十七处长句收紧（如「it does not replace … and it does not automatically turn」改为「it neither replaces … nor automatically turns」、「labelled as full recalculated values」改为「labelled as such」、四处「so that」改为「so」），合计回收 142 字节；其二，全文十处「together with」改为「with」、两处「has to be」改为「must be」、一处「that gate has」改为「it has」，合计回收 101 字节；其三，索引表段自身的措辞收紧与两处「on the basis of」改为「by」，合计回收 53 字节。三批合计回收 296 字节。`proposal.en.md` 由 262,088 字节变为 262,098 字节，距 262,144 字节上限余 46 字节；`proposal.md` 由 214,769 字节变为 215,038 字节。
- **计数终值**。双语总行数 963（`wc -l` 口径 962）、非空行 674、表格行 315 四项均未变，逐行结构不一致数仍为 0；四类证据锚点由 268 增至 269（`source` 类 87 → 88，`metric` 79、`data` 88、`standard` 14 未变），`depth` 40、`assumption` 14、`self_check` 4 三项未变。正文自查段与 `qa-proofing-record.json` 的锚点读数同步改为 269／269，复测日期改为 2026-08-20。`manifest.json` 文件条目仍为 142（本版无新增或删除文件），改签条目为本版实际改动的文件。`sources.json` 由 43 条增至 44 条，其中 `official_public` 仍为 27 条。包体 git 追踪 142 个文件合计 40,490,661 字节（38.615 MiB）。
- **护栏与四门**。反向引用断言护栏 `refcheck` 通过 46 项、零失败；双语报告页按 `render_proposal_html.py` → `post_render_report.py` → `imgdim.py` 全链重渲，逐页文本层比对确认差异仅为本版两处正文改动。

## v8.4 - 2026-08-18

**审查驱动的终修批次：展示页图版挂载补齐、权利台账十类件数对账与触觉交付补段、自检闸门六道读数刷新、短片英文权利声明残留读数修复**

- **展示页挂入 v8.3 新增两张图版，替代文本读数升至 22／22**：`visual/index.html` 与 `visual/index.en.html` 各挂入 `assets/figures/bluegreen-structure.png`（蓝绿三层结构与十节点铺装径流去向类型）与 `assets/figures/heritage-sequence.png`（百年遗产解说序列），两页 `img` 元素各由 20 增至 22，全部带非空 `alt` 且带宽高属性。此前两张图版只在正文与报告页在场、未挂上离线展示页，属挂载遗漏而非内容缺口。
- **逐资产权利台账十类件数逐路径对账，三处方向相反的存量偏差一次更正**：十类件数按 `manifest.json` 的 142 条声明逐路径归类，十类之和恒等于 142。C 类由 37 更正为 38（v8.2 新增的 `assets/figures/district-strategy.png` 未计入，v8.3 新增两张关系图版时又只加计一件）；F 类由 43 更正为 47（v8.0 新增的 `visual/assets/seb-spec-v0.4.0.json` 与 `seb-change-receipt-v05.json`、v8.2 新增的 `visual/assets/district-strategy.json` 与 `field-verification-plan.json` 共四件未计入）；G 类由 23 更正为 18（v8.1 精简包体时删除的双态漫游五件未从计数中扣除，同批删除使 G 类叙述中的「AI 生成场景双态短片」一语失去对应文件，本版一并删去）。三处偏差数量恰好互相抵消、总和始终为 142，因此历次自检未能发现，本版把该抵消机制连同更正一并写入台账的「件数口径与本版更正」段。同轮补齐 F 类逐资产登记的十六个漏列文件，F、G 两类的逐资产登记行现与 `manifest.json` 逐路径一一对应。
- **台账 §三 新增「J 触觉交付」段（两件）**：`assets/tactile/op04-papercraft.svg` 与 `.en.svg` 此前只在 §二 类别总表计数，缺逐资产登记行。本版补齐生成方式（程序绘制的参数化 SVG，毫米坐标直写，viewBox 210 × 297 与 A4 幅面等值）、内容来源（四条通行带带宽按 1:50 缩放自 `visual/assets/sensory-design-code.json` 的 OP-04 母版卡 `band_widths`——多模态导路带 2.50 m、触觉引导带 0.30 m、设施带 1.20 m、安静缓冲绿带 1.50 m，均为设计目标值；折叠构造 6 条折线 7 段展开序列与盲道带凸点阵 2 列 × 18 行为纸模自身构造，不引用任何第三方几何、图像或数据集）、字体（仅声明字体栈 `Source Han Sans SC, Arial`，由打印或查看环境本地解析，不嵌入、不分发字体文件）与已登记局限。两版几何元素经逐条比对确认同源，仅文字层分中英。
- **three.js 登记行归位，字体台账修复列错位**：`visual/assets/three.min.js` 一行原误置于 §四 字体台账表内，四列对六列表头、语义亦不属字体登记。本版移入 §六 代码与依赖台账并按该表「代码资产／运行环境／第三方依赖／网络访问／许可」五列口径重写；§三 F 段既有的 three.js 逐资产登记行不动，两处不重复。同表另补触觉纸模一行，使 J 类资产在字体台账中不再缺席。
- **随包自检闸门台账六道读数按 2026-08-18 实测刷新，旧值一律降为历史串**：PG-01 由 907／638／292 刷新为行 962／962、非空行 674／674、表格行 315／315；PG-02 由四类合计 232／232 刷新为 source 87／87、data 88／88、metric 79／79、standard 14／14（合计 268／268），depth 40／40、assumption 14／14、self_check 4／4 另计；PG-03 正文图题与报告页宽高两项由 16／16 刷新为 21／21，展示页替代文本刷新为 22／22，图版文件 44 件未变；PG-06 由 135 条／134 条带摘要刷新为 142 条／141 条带摘要、`language` 登记 110 条、`translation_of` 25 条；PG-10 由样例 40 条（通过 13、拒绝 27）刷新为 60 条（通过 23、拒绝 37），两个校验器本轮实跑退出码均为 0。六道均加 `remeasured_on: 2026-08-18` 字段，旧读数连同其变化原因写入 `superseded_reading_zh/en`，不以新读数覆盖历史。刷新中同时暴露该台账内部时点不齐：PG-01 停在 v8.0.1，PG-02 停在 v7.9，本版一并对齐到当前版本。
- **PG-08 判定：条目总数由 29 改为 28，闸门改名**：`visual/assets/qa-proofing-record.json` 的 R4 组实测四项（R4-01 至 R4-04）。R4-05「双态漫游成片完整性」随 v8.1 撤下双态漫游五件套一并删除，故 R1 六项 + R2 八项 + R3 十项 + R4 四项 = 28 项。条目减少的原因是被测对象已移出本包，不是检查被省略，该判定连同原因写入历史串。闸门名同步由「QA 记录 29 项闸门」改为「QA 记录条目自洽闸门」（英文 `QA record item-consistency gate`），不再把会随被测对象增删而变的条目数绑进闸门名，判据未变。
- **PG-05 读数加精度限定**：「与 `metrics.json` 登记逐项一致」改为「与 `metrics.json` 登记逐项一致（按各文件登记精度）」，英文同步为 `each at the precision that file registers`。几何复算读数本身未变，限定的是「一致」二字的口径。
- **短片英文权利声明的残留读数修复与同构句压缩**：`assets/media/journey.md` 英文摘要段仍把可达域图版的题名印作 46.7%，与中文侧镜头 03 重摄记录（现行图题 49.3%）不一致。本版改正为 49.3%，并补齐中文侧已有、英文侧缺失的「v7.7 与 v8.0 两次读数口径改写」交代（v7.7 为图题加上本图口径，v8.0 为道路几何加密后读数重算）。同段两处「权威现行件优先于片中画面」的同构表述合并为段尾一条总则，四张图版各自变化的逐张清单外指第二节镜头记录与 `changelog.md`，重复的「in a later package revision」删去一处；该段净减 195 字节，全文行数不变。
- **正文双语图版字号句合并为覆盖三张的一句**：「v8.2 新增的片区策略图版按同一口径另测为最小 12.0 像素、等效 12.3 像素」改写为「v8.2—v8.3 新增三张图版按同一口径另测，最小 12.0 像素、等效 12.3 像素」，使 v8.3 新增的蓝绿三层结构与百年遗产解说序列两张图版的字号实测在正文有据可查。三张的下限与等效值本就同为 12.0 与 12.29 像素，逐图实测早已登记于 `visual/assets/qa-proofing-record.json` R1-05，本版只是把正文的表述范围对齐到该登记。改写为行内改写，双语行数、非空行、表格行与四类锚点四项均不变；英文侧净增 5 字节，仓库单文件上限余量由 61 字节降至 56 字节。
- **感官设计准则三处措辞与计数按几何实测更正**：按 EPSG:4548 复算十个独立完成点与其登记公共空间面的空间关系，九点落在所属面内，OP-08 位于 PUBLIC-008 面外约 55 m。据此把 `visual/assets/sensory-design-code.json` 中两处分派依据的「所落公共空间面」改为「所落或紧邻的公共空间面」并写明该实测距离（节点铺装径流三类判据的 `nature`，以及 OP-08 所属的 TP-3 地标记录型判据），使判据表述与几何事实一致；同轮更正 BG-L2 节点绿量载体行的面数——原印「十个独立完成点所落的十处公共空间面」，实为八处（PUBLIC-004 与 PUBLIC-008 各承载两个完成点，故面数少于点数），双语同改。TP-1、TP-2 的成员节点全部落在面内，判据不动。
- **`assets/.DS_Store` 删除**：该文件为 macOS 目录元数据，从未被 git 追踪（仓库根 `.gitignore` 首行即 `.DS_Store`），本版从工作目录一并清除，避免打包分发时随目录带出。
- **计数与登记终值**：`manifest.json` 文件条目 142（本版内容改动 12 个文件：正文双语、报告页双语、逐资产权利台账、短片说明、闸门台账、QA 记录、感官设计准则、离线展示页双语与本记录，全部条目重新核对签名）；双语正文行数 962／962、非空行 674／674、表格行 315／315，四类证据锚点 268／268，depth 40、assumption 14、self_check 4 双语持平；报告页双语各 21 张图注入宽高、21 张带替代文本；离线展示页 `img` 22／22 带非空替代文本；图版文件 44 件。反向引用断言 46 项零失败。

## v8.3 - 2026-08-18

**五节感官设计准则一次登记；FAB-03 构面读数补方法敏感性对照；参与矩阵的屏幕阅读器口径收紧；执行摘要自筹字节删三处重复；新增蓝绿三层结构与百年遗产解说序列两张关系图版**

- **新增 `visual/assets/sensory-design-code.json`：五节判据一次登记，不引入任何新的设计主张**。该文件的自述纪律有四条，逐条可核：其一，五节的每一条判据、区间与类型划分都由本包既有素材推出，推出来源逐条写在各条目的 `derived_from` 字段，读者可按该字段回到原文件核对；其二，文件内一切数值均为设计目标值或参考区间，不是法定值、规范限值、造价或工程结论；其三，不承诺任何执行主体、任何一笔资金、任何一次活动与任何一个日期；其四，不产出任何指标读数——七项包容性指标与六项空间审计读数的分子、分母与口径均不因本文件而改变。正文对应五处以一行引句挂载，各带一个 `[data:]` 锚点，不新增章节。
- **第一节 蓝绿网络与雨洪：三层结构与类型判据**。三条层带各登记载体与职能——BG-L1 主脊绿带（GREEN-001／002／003，spine_greenway，挂 ROAD-001）、BG-L2 节点绿量（GREEN-004／005／006，low_stimulation_buffer，挂 ROAD-005，并含十个独立完成点所落的十处公共空间面）、BG-L3 河道概念联系（清河与小月河，只作概念性联系登记，本层不承担任何工程职能）。三条层间关系判据 BG-R1／R2／R3 分别管住「安静线绿量不得由主脊代偿」「就地滞留在先、市政衔接在后，蓝线与防洪核验前不提直排」「河道关系只登记概念，不提线位、不标蓝线、不推廊道宽度」。十节点的铺装径流去向按三类登记：DR-A 透水铺装自渗加相邻生物滞留带承接（OP-01、OP-05），DR-B 线性排水加市政衔接（OP-02、OP-04、OP-10），DR-C 生物滞留与市政衔接并置且接口缓坡面不受纳（OP-03、OP-06、OP-07、OP-08、OP-09）；分类依据是该节点所落公共空间面的既有 `space_type` 与其是否相邻绿带，两项都是包内既有属性，不新引入判据。滞留容积、渗透系数、溢流标高、管径与市政接口位置五项一律登记为现场核定条件，本节不出任何数值。
- **「雨天断点」读数与雨洪设施的对应关系只回答一件事：读到断点之后该看哪一件设施**。三类成因各自指向——积水指向该段滞留带容积与溢流标高及通行面横坡方向；湿滑指向铺装材料判据，且湿滑断点一经登记即为该段材料判据未成立的现场证据，不由材料送检替代；排水口阻挡轮椅指向线性排水沟位置与盖板方向及滞留带边界，该类断点同时是触觉带连续性读数的输入，两项读数在同一位置出现时合并登记、不重复计数。本节不产出任何读数：六项空间审计读数当前全部为零值，对应关系不回答「应当有几个断点」。四问深化法（种什么、地形标高、雨水去向、夜间照明）向其余段的适用触发登记为三项同时成立——限定范围可撤回的场地授权、土壤与地下管线与现状树木调查、雨天断点与夜间断点各至少一轮读数；任一项不成立，该段只保留原则不进入做法。预先为十段各写一份四问只会得到九份没有现场输入的复制品，因此不在本包内展开。
- **第二节 节点设计参数：母版与区间规则**。OP-04 的八要素卡（通行净宽、纵坡、过街缝合、铺装材料、排水、触觉系统、声环境、维护边界）登记为母版，其取值为已按 GB 55019-2021 与《无障碍环境建设法》框架逐项给出的设计目标值。其余九个节点不新给任何数字：参考区间一律由母版卡的既有取值围成，下限取母版登记的规范框架下限、上限取母版登记的设计目标值；区间收紧为单值只发生在一种情形——该节点承担连续通行面，此时上限即为取值，理由是连续通行面在全带上必须同宽，一段收窄即整条底线不成立。三类节点画像 TP-1 连续通行段型、TP-2 前厅型、TP-3 地标记录型另附校准触发模板。九节点的区间未经 OP-04 级逐项核定，是参考值不是法定值，该限定与区间一并登记。
- **第三节 百年遗产解说序列与材料经济**。序列沿主脊自北向南，与三处重点区的既有空间次序一致：HS-1 众智园工程起点叙事（绑定 OP-07 共轨门）、HS-2 原点社区生活变迁叙事（绑定 OP-06 万感原点）、HS-3 大钟寺转运与采用叙事（绑定 OP-08 百感台）。序列的作用是把包内已有的解说载体按节点串起来，不新造载体；任一段可单独进入、可跳过、可重复，这是既有的触觉文化带主张，序列不改变它。HS-3 的「转运」只取其空间事实的现成含义，不引用任何未经核验的站场史实。遗存材料再利用给四条类型判据：MR-1 结构安全核定前只做展示性再利用，展示性的界线可核对——构件移除后通行、停留与疏散不受影响即为展示性；MR-2 任何再利用不得标注保护范围、等级或控制线，构件上的文字亦不得出现该类表述；MR-3 三条可核对下限，不减通行净宽、不产生新的高差断点、不带尖锐边缘与积水凹陷；MR-4 轨道、道砟与站台构件各有一条类型限制。本节不提出取用任何一件构件，也不判断任何一件构件可否取用。
- **第四节 实施制度框架：只登记候选，不作选择**。三种执行主体候选形态（IF-1 政府平台公司、IF-2 公园运营方扩权、IF-3 多方联合体）各给适配判据、前提与空缺，本方案不承诺也不推荐其中任何一种；截至登记日，本方案团队未与任何场地方、运营主体或授权主体发生接触，不存在任何意向、约定或承诺。闸门持有者原则单列一条：G3 独立复核的持有者须独立于执行主体，三种候选形态一律适用，持有者与执行主体重合的任一安排登记为不成立。成本按两类量级结构登记而不给造价——CM-A 可自持类要求两条判据同时成立（该节点存在可产生服务对价的界面，且该界面的现金、人工与无账户三条等价路径不因收费而消失），第二条不成立即不得归入本类；CM-B 为纯公共底线的需补贴类。四方在场（铁路资产方、区级、公园运营方、社区）作为事实登记，并附协调机制与仍然敞开的空缺。
- **第五节 参与矩阵：五类活动 × 五条感官通道 = 二十五格**。活动为意见征集、成对测试、G3 复核旁听、否决登记、公开回执查阅；通道为视觉、听觉、触觉、语言文字、数字与线下。自检规则只有一条：任一格为空即该活动不成立，空格不得以「其他通道可以代替」结案——通道之间是并列关系而不是替代关系，与服务层「任一感官通道单独可用即可走完」同属一条判据。读数为 `filled_cells` 25、`empty_cells` 0、`executed_cells` 0，并直书「二十五格填满指的是判据已写出，不是形式已具备」，`executed_cells` 保持零直到第一次参与活动真实发生。
- **参与矩阵的屏幕阅读器口径按包内既有局限收紧**。「意见征集 × 数字与线下」一格原可被读成线上界面已具备无障碍能力，本版改为按展示页既有无障碍走查口径逐项写出（键盘路径无陷阱且反向可逆、替代文本非空、禁用脚本后静态后备呈现、颜色非唯一编码），并直书该走查为方案方自查、未经屏幕阅读器实测，故该格只登记为「按既有走查口径执行」。该收紧与 `qa-proofing-record.json` 已登记的三条局限（未经屏幕阅读器实测、未经具资质译者逐词审校、未做实物印品检查）同口径，不另立标准。
- **FAB-03 构面读数补一份方法敏感性登记，不改动本读数任何数值**。以同一快照、同一自述口径另写一份独立实现复算：基准路径（构面前按共享顶点去重）得 1,991 面、场地内 1,288 块、中位 1,387.366 平方米、街块面积总和 9,495,999.944 平方米，与既有登记值逐位一致；随后逐项改动单一实现细节即出现差异——不做邻接段去重得 1,985 面（场地内 1,286 块，中位 1,390.047 平方米，面积总和不变）；投影坐标取整至毫米再去重得 1,989 面（场地内 1,286 块，面积总和低 0.025%）；先做几何 noding（unary_union）再构面得 2,360 面（场地内 1,522 块，中位 1,124.442 平方米、较本读数低 19.0%，面积总和高 9.97%），最后一项正是本读数刻意不做 noding 所避免的量级。据此登记两条纪律：面数与面积总和不得跨实现按面值比较；候选街块的块级引用一律以逐项几何校验为准——十个开放化候选的面积、周长与质心已按登记值逐项比对，差值分别小于 0.05 平方米、0.05 米与 1×10⁻⁶ 度。
- **执行摘要自筹字节：删去与后文重复的三处展开**。英文正文的 256 KiB 仓库单文件上限在本版前只余 209 字节，两张新图的挂载需要 148 字节，因此本版不新增修饰而是先腾字节：执行摘要「其二 万感服务桌」删去「不消费也能停留」承诺的整段重述与七项开办条件的逐项枚举（两处在后文均有完整登记），「其三 点位否决权」删去否决行使条款与异议去向的整段重述并改为指向「更新过程本身的公共利益」一节。三处删除均为重复叙述，无任何判据、读数或证据锚点随之消失。
- **新增蓝绿三层结构关系图版 `assets/figures/bluegreen-structure.png`（3,000 × 1,875 像素，378,243 字节，中英双语同图单件）**。关系图而非平面图：三条水平层带自上而下并置，层间以带向箭头挂 BG-R1／R2／R3 三条关系判据，BG-L3 以虚线绘出并在图面直书「虚线为概念性联系，不表示蓝线」；十个完成点在 BG-L2 带内按 DR-A／DR-B／DR-C 三色打点，OP-04 另加「母版」标记并直书其排水取值是 DR-B 的来源而不是由 DR-B 推得；右栏给「雨天断点」三类成因各自指向哪一件设施，并直印「六项空间审计读数当前全部为零值」；底栏给三类类型判据全称。图面免责逐条印出：类型判据非工程参数、现场核定前不构成工程结论、图上不出现容积与渗透系数与溢流标高与管径与照度的任何数值、层带与打点不表示位置与长度与面积与比例。
- **新增百年遗产解说序列图版 `assets/figures/heritage-sequence.png`（3,000 × 1,875 像素，346,885 字节，中英双语同图单件）**。自北向南的序列轴（北在上，与全带图一致），三段各占一行、每行三栏——叙事一句、绑定节点与地标、解说载体图标组；十二件载体全部标为包内既有交付物，并直书序列不新造载体。轴上显式画出「可停 / 可跳过 / 可重复」三操作态。HS-3 行直印「转运只取空间含义，不引用未经核验的站场史实」；底栏给 MR-1 至 MR-4 四条材料判据，并直印「结构安全核定前只做展示性再利用」「不标注保护范围、等级或控制线」「材料来源、权属与许可须逐项清权后才可使用」。图面不出现文物保护等级、保护范围与控制线，不做线路复原，历史锚点一律留在正文。
- **两图的字号下限是版面约束而不是事后读数**。输出 3,000 × 1,875 像素、评审接收分辨率短边压至 768 像素，重采样比 0.4096，故接收分辨率下每行文字的高度不得低于图宽的百分之一；两图均以 1,200 × 750 CSS 像素按输出倍率 2.5 渲染，CSS 字号下限因此取 12 像素、等效 12.29 像素。该约束把整幅图的可用文本行数封在约四十八行以内，两图的每条中英短句都按所在栏宽压到单行，超出即改写。逐元素实测（含全部 SVG 图标注记与徽章）出现的字号为 12 / 13 / 15 / 16 / 22 CSS 像素，两图下限均为 12.0 像素，不可读文本占比 0%；同轮以 Playwright 量测版心，两图 body 滚动尺寸均为 1,200 × 750 对视口 1,200 × 750，零溢出元素。两图的文字与结构直读 `sensory-design-code.json`，节点归类、层间关系配对、类型名称与材料判据编号均由该文件驱动，图面短句只对既有字段作压缩。
- **计数与登记的连锁刷新**。`manifest.json` 文件条目 139 → 142（新增一个数据文件与两张图版，全部条目重新签名，本轮改签 7 项）；双语正文行数 948 → 962、非空行 667 → 674、四类证据锚点 263 → 268（全部为 `data` 类，五处均指向本版新增的数据文件），表格行 315、depth 锚点 40、assumption 锚点 14、self_check 锚点 4 四项未变，逐行结构不一致数仍为 0；`assets/figures` 图版文件 42 → 44，其中中英双语同图单件由 10 张增至 12 张。逐资产权利台账同步登记两张新图的程序绘制方式、字体许可与已登记局限，并逐项声明两图均不含 OSM 背景、也不由 OSM 数据或随包快照几何派生，因此不适用 ODbL 的署名与相同方式共享条件；C 类件数 35 → 37、F 类件数 42 → 43，总数三处由 139 改为 142，正文两处由 140 改为 142。`qa-proofing-record.json` 的图版文件数、双语等价三项读数与新图字号量测同步刷新。中文正文 214,621 → 214,771 字节，英文正文 261,935 → 262,083 字节，距 256 KiB 上限余 61 字节。

## v8.2 - 2026-08-18

**三区街块诊断读出一条单向粒度梯度并据此分区施策；走廊分段与轨道接驳首次以快照读数给出判据；零授权现场验证计划补齐执行办法；新增片区策略合幅三联图一张**

- **三区街块诊断：一条单向梯度，两种问题**。把 FAB-03 的街块集合按三处重点区临时多边形分组（分组判据为街块代表点落入该多边形），先整体复现母读数并逐位一致后再分组，得到：众智园 132 个街块、中位 2,547.8 平方米、为全场地中位 1,387.366 平方米的 1.84 倍、面积超过 40,000 平方米的超大街块 9 个占该区街块面积 60.05%；北京 AI 原点社区 151 个、中位 1,677.7 平方米、1.21 倍、超大街块 7 个占 42.52%；大钟寺 84 个、中位 969.0 平方米、0.70 倍、**超大街块为零、占比 0.00%**。三区的中位规模与超大块占比同向递减，构成一条单向梯度，其设计含义是分区施策而不是统一模板：北段的问题是街块本身，南段的问题是界面。
- **大钟寺零超大街块这一读数改变了该区的策略取向**。该区不适用开放化，因此该区的候选按「阈值以下」登记：三个次级候选面积分别为 38,682.6、21,898.9、21,155.4 平方米，全部低于 40,000 平方米的超大判据阈值；其中两个的围合 way 全为步行专用或内部服务路。该区的设计回应因此从「把街块打开」转为「把界面分类」，与轨道接驳读数互相印证——最近接入点簇距该区 101.6 米、距该区横向接口中心线 100.9 米，属贴邻而不在区内，判据落在这 100 米区间内的过街与界面连续性上。
- **开放化候选只作排序建议，不作可开放性判断**。十个候选（众智园 4、原点社区 3、大钟寺 3）逐个登记面积、周长、质心、围合 way 数与前三条围合 way 的 OSM way 编号、与提交中心线及十个完成点的最短距离、动作类型（贯通通道／边界渗透／角部开放）、排序依据与前提条件。每个候选另附一条现状拓扑事实的逐字限定：所断言者仅为「该面在可步行网络拓扑上未被进一步细分」，不含产权、围墙、内部通道存否或可否开放的任何结论；四项前提（产权核验、内部通道现场踏勘、G0 用途与产权闸门、G3 独立复核）逐候选列出。
- **走廊分段：以贴线率读数支持「替代线确实更安静」这一此前只有设计意图的主张**。沿五条提交中心线每 50 米取一个采样点，逐点量测到最近城市干道要素与最近步行专用要素的距离，按三条判据归类。读数为：主脊 ROAD-001 干道贴线率 84%、慢行专用段 0%、共存段 16%；低刺激安静替代线 ROAD-005 干道贴线率 50%、慢行专用段 13%、共存段 38%。替代线比主脊低 34 个百分点，且主脊全线无一处满足慢行专用判据。该对比是本包首次以快照读数支持该主张；此前该主张只有设计意图，没有可核对的依据。同一读数同时修正一个容易发生的误读：主脊不是独立于城市干道的园路，它在绝大部分长度上与干道贴线并行，深化时的主要问题是路权关系与过街。三处横向接口的读数为 ROAD-002 的 59/0/41、ROAD-003 的 22/22/56、ROAD-004 的 43/17/40。三个归类阈值（60 米、30 米、120 米）均为本文件的方法选择，不是规范值，改变阈值将改变全部占比，该限定随读数一并登记。
- **轨道接驳按坐标与距离登记，不指认站名**。从同一快照取出全部 3 条 railway=station 与 25 条 railway=platform 要素，投影至 EPSG:4548 后按 150 米单链聚类合并为 11 处接入点簇，其中 5 处落在某一重点区 800 米内。11 处中只有西直门一处在快照中带有名称标签，其余 10 处的 way 无 name 标签，因此本包按坐标与距离登记接入点，不指认站名——指认站名会超出快照能够支持的范围。四条接驳序按距离关系排出：众智园两簇均在区内（0 米）、距主脊 17.5 与 44.3 米；大钟寺最近簇 101.6 米；原点社区最近簇 685.2 米但距主脊仅 30.5 米；西直门 2,293.2 米，按区域接驳而非片区接驳登记。
- **强度包络明确拒绝给出数值，并说明拒绝的理由**。该块不给参考高度带、参考容积率区间或任何强度数值。理由不是资料不足，而是本包已在控规官方参照台账中承诺：取得适用于自身范围的官方控制文件之前，不为任何地块提出容积率、建筑高度、建筑密度或绿地率的数值建议。以「参考区间」形式给出强度数字会与该承诺直接冲突，并把一个无依据的数字放进评审可以援引的位置。该块因此只表达方向判据与不变量，在没有控制值时即可核对，在控制值到位后仍然成立。
- **界面规则以本场地自身粒度为参考标尺，不引外部经验值**。活性界面的人工可及入口间距上限参考取各区街块中位规模的等效方形边长——众智园约 50.5 米、原点社区约 41.0 米、大钟寺约 31.1 米，参考区间取该值的 0.8 至 1.0 倍。取现状粒度而非外部经验值，是为了让判据在本场地可当场复核；参考值不是法定值，该限定随规则一并登记。
- **零授权现场验证计划把六项读数从待办层推进到可执行层**。此前六项读数只有定义、一句方法与责任方，没有记录格式、样本量与有效性条件。本版补齐三件：六项读数的逐项执行办法与记录字段定义、无障碍路径实测协议（VoiceOver 与 TalkBack 各做一次同一到达任务，全程不看屏幕，只测公开可访问界面）、最小访谈集（居民 8—12、商户 6—10、公园使用者 10—15，按六个维度的任务约束提问）。「零授权」的含义严格限定为全部动作在公共可进入空间内以公众身份完成，不需要场地授权与主体许可，不进入受限区域，不触碰设施，不采集任何个体身份信息。诚实边界与计划并列登记：**计划已备，执行待定；六项读数与本计划涉及的一切现场值截至签发日全部为零值或 unknown，一个数字都还没有产出**；执行角色如实登记为提交者本人或其自愿邀请的志愿者，本方案没有已组建的团队，也不虚构团队。
- **新增片区策略合幅三联图 `assets/figures/district-strategy.png`（3,000 × 1,875 像素，484,123 字节，中英双语同图单件）**。三分幅 A／B／C 对应三处重点区，取同一比例尺（1 CSS 像素 = 6.2 米）与同一取向（图面旋转 90°、北向右），使三区粒度差异在图面上直接可比。分幅 A 标出四个候选的动作类型记号与质心标注，**只标动作类型，不画任何通道线位**；分幅 B 的画面主题是三块到 ROAD-003 的距离均为 0 米——横向接口能否成段由这三块决定；分幅 C 按「本区没有超大街块」反转画法，候选以虚线登记为阈值以下，画面转为沿 ROAD-004 与 ROAD-005 的界面分类。共用图例条另设入口间距参考标尺，把 50.5／41.0／31.1 米三个数值画成同比例小方块，使粒度递减直观可见。图面免责逐条印出：快照口径派生、参考值非法定值；街块轮廓为 OSM 拓扑闭合面，不代表产权边界或围墙；重点区范围为临时多边形；界面分类按适用条款派生，不是逐段实测分配。ODbL 1.0 署名与快照编号印在页脚。
- **图上街块轮廓的复算与逐候选校验**。候选街块轮廓不另编几何：按 district-strategy.json 自述的 FAB-03 构面口径（可步行网络平面图 polygonize，只用 OpenStreetMap 共享顶点拓扑、不做几何 noding）由随包精简快照就地复算，再以面积、周长、质心三项逐候选比对该文件的登记值，十个候选的面积差与周长差均小于 0.05 平方米与 0.05 米、质心差小于 1×10⁻⁶ 度，一致后才入图。可步行网络 way 数复算为 3,507 条，与读数文件登记值一致。
- **字节账：「少即是多」的纪律在本版由英文版单文件余量直接约束**。英文正文的 256 KiB 仓库单文件上限在本版前只余 921 字节，因此新增图版的挂载写法按极简执行：图引一行加一段图注，中文图注 357 字节、英文图注 399 字节，均在 400 字节以内，且不新增任何证据锚点。替换后中文正文 213,701 → 214,256 字节，英文正文 261,223 → 261,827 字节，距上限余 317 字节。本版三节正文与三张表的增量全部由内容承担、不由修饰承担：新增的六个四类证据锚点中有三处指向本版新增的两个数据文件，其余三处指向既有读数文件。
- **计数与登记的连锁刷新**。`manifest.json` 文件条目 136 → 139（新增两个数据文件与一张图版，全部条目重新签名）；双语正文行数 905 → 948、非空行 639 → 667、表格行 299 → 315（数据行 219 → 229、表数 40 → 43）、四类证据锚点 257 → 263、assumption 锚点 12 → 14（assumptions.json 条目数仍为 12，两处均为既有条目的新增引用）、标题 29 → 32，逐行结构不一致数仍为 0；`assets/figures` 图版文件 41 → 42，其中中英双语同图单件由 9 张增至 10 张。逐资产权利台账同步登记新图的程序绘制方式、ODbL 衍生署名、字体许可与四条已登记局限，C 类件数 35 → 36，总数三处由 136 改为 139。`assumptions.json` 的 A-CONTROLS-001 影响文件补入两个新数据文件；`design_depth_matrix.json` 的「三大重点片区详细设计」与「道路、轨道、慢行与停车组织」两项按行内最小口径补记本版证据。`compliance_matrix.json` 本版不动：该文件的条目字段只接受章节名、GeoJSON 图层、指标与图纸四类引用，本版新增的三节均为三级标题、其所属二级章节已在相关条目的 `report_sections` 中登记，无需新增引用。
- **新图字号按同一接收分辨率口径单独实测**。生成期对图版 DOM 逐元素读取 computedStyle 的 font-size，出现的字号为 12／13／16／23 CSS 像素，下限 12.0 像素（含全部 SVG 图面注记）；按输出倍率 2.5 与接收分辨率短边压至 768 像素的重采样比 0.4096 折算，下限字号等效 12.29 像素，不可读文本占比 0%。同轮以 Playwright 量测版心，body 滚动高度 750 对视口 750，零溢出。该读数不并入既有 26 张带文字图版那一轮的逐张量测，单独登记于 `visual/assets/qa-proofing-record.json`。

## v8.1 - 2026-08-18

**四册 PDF 的分页字体子集合并为整册共用一份；双态漫游五件套整体撤下并逐处收口；正文四项收束，英文版单文件余量由 330 字节增至 9,169 字节**

- **四册 PDF 字体子集合并（回收 929,613 字节，逐像素无损）**：四册此前由逐页 Chromium 渲染再以 pypdf 装配而成，每页各自携带同一字重的一份独立子集——A3 两册各 33 个字体程序流，A0 两册各 13 个。本版按字形标识空间取并集，把同一字重的全部分页子集合并为整册共用的一份，四册均降至 3 个字体程序流。合并的无损性由三项结构判据同时支撑：全部副本的字形标识到字形名映射零冲突、Unicode 到字形标识映射零冲突、共有字形的轮廓编译字节与 `hmtx` 度量逐项零冲突；`head.unitsPerEm`、`hhea.ascent/descent`、`head.macStyle`、`OS/2.usWeightClass` 与 `fsSelection` 五项全局度量在各副本内均为单值，确认各副本确为同一字面的子集。回收明细：`a3-booklet.pdf` 2,658,093 → 2,084,915 字节，`a3-booklet.en.pdf` 2,131,939 → 1,978,912 字节，`a0-boards.pdf` 4,353,372 → 4,198,324 字节，`a0-boards.en.pdf` 4,404,793 → 4,356,433 字节。
- **合并结果的验证口径与一处渲染器行为的如实登记**：四册页数与逐页页面尺寸均不变（A3 十六页 420.20 × 297.01 毫米，A0 六页 1188.89 × 840.99 毫米）；四册合计四十四页以 `pdftocairo` 全页栅格逐像素比对合并前后，最坏页均方根误差为 0，即像素完全一致。比对口径由 `pdftoppm` 改为 `pdftocairo` 的原因一并登记：poppler 的 Splash 后端按字体对象缓存字形位图，多页共用同一字体程序后对同一轮廓给出不同的抗锯齿边缘，该差异在 300 dpi 下影响 0.63% 像素、在 360 dpi 及以上归零，而 Cairo 后端在任一分辨率下均为零——差异来自光栅器的字形缓存行为，不是 PDF 内容变化，故以无此缓存的 Cairo 后端作为无损判据。合并步骤已并入构建链的 `mkpdf.sh`，后续重建自动生效。
- **撤下双态漫游五件套（回收 1,930,792 字节）**：`assets/media/dual-state-walk.mp4`、`dual-state-walk.md`、`dual-state-walk.vtt`、`dual-state-walk-en.vtt` 与 `dual-state-walk-poster.webp` 整体移出本包。撤下判据为重叠度：该短片的论点「关掉之后城市还剩什么」已由四条更硬的通道各自独立承担——OP-04 断点的可听化以静默实测复算、断面折叠纸模与三维触觉地图以指尖可读的高差交付、双态平面图版以图面呈现同一对切、交互场景的 AI 开关以可复算读数呈现同一结论；短片是其中唯一以 AI 生成影像为载体、且不产生任何可复算读数的一件。
- **撤下的十处连锁一次改完**：`manifest.json` 删除五条目，文件条目由 141 降至 136（role 计数 video 2 → 1、media_poster 3 → 2、caption_track 9 → 7、transcript 5 → 4，该文件不设 role 计数字段，计数由条目直接派生）；`report/copyright_statement.md` §G 媒体台账删一行；正文双语「动起来的双态」整段同删，其前后的「听得见的断点」「摸得到的断面」「诚实边界」三段完好；正文双语资产类别表媒体一行由 11 降至 10；`visual/assets/qa-proofing-record.json` 删除 R4-05「双态漫游成片完整性」一条并同步 R4 组标题；`visual/index.html` 与 `visual/index.en.html` 各删短片指引一处（本轮审计清单原判展示页无引用，实测为两处，已一并收口）；全包文件条目印值由 141 改为 136，涉及正文双语各一处与权利台账三处。`sources.json` 的 OpenAI 服务条款条目不动——该条目是包内六张场景概念图的权利依据，六张图版仍在包内。
- **正文四项收束（英文版回收 8,839 字节）**：其一，两处控制台输出块按包内既有惯例单语化——第二块自登记以来即为双语标签加单语内容，本轮把仍为全双语的第一块拉齐为同一形态，中文版留中文内容、英文版留英文内容，`判定 / verdict`、`理由 / reason`、`汇总 / Summary` 等工具实际打印的双语标签一律保留，反向引用断言所核的 `· 60 条 / items` 与 `matching expectation : 60 / 60` 两行原样保留。其二，服务等价基准的四段版本治理改写为一句主张加一张五行表，五行对应治理回路的五次转动，版本、驱动方、判据变更、失败样本登记与变更回执分列；全部事实锚点入表——变更回执 CR-2026-08-12-002、CR-2026-08-15-004、CR-2026-08-15-005、CR-2026-08-16-007 与 CR-2026-08-18-009，失败样本 FS-059 至 FS-062 与 FS-063 至 FS-067，两例外部采用登记，样例条数二十、四十与六十的三次扩充；六个引用令牌分散于主张句与五个不同单元格，逐行不触发引用密度门禁。其三，风险章「版权与授权」与「许可标识读法」两节向 `report/copyright_statement.md` 收口，资产类别表与许可结论句保留，其余压为指向台账的概括。其四，「输出质量与可及性自查」段压缩，全部结论读数保留。
- **收束附带的一处去重**：五行表建成后，「产品化声明」段中逐版驱动方与对应回执的枚举、以及「变更须附失败样本登记与复算说明」一句，与表内内容重复，双语同撤；主张句尾与其后加粗免责声明重复的一句同撤，免责声明本身保留在原位不动。
- **计数终值（双语两侧全等）**：905 行、639 非空行、299 表格行（含四十张表的分隔行，数据行 219）、四类证据锚点 257（metric 79、data 77、source 87、standard 14），另有 depth 深度锚点 40、assumption 前提锚点 12 与 self_check 自检锚点 4 双语持平且不计入四类抽检。正文自查段、`visual/assets/qa-proofing-record.json` 的 R2 组与复测备注、A3 两册的页数与字节读数均按上述终值同步。英文版正文由 261,814 字节降至 252,975 字节，距仓库 256 KiB 单文件上限的余量由 330 字节增至 9,169 字节。
- **本轮未执行项的如实登记**：其一，`changelog.md` 的历史条目分卷未做。`scripts/validate_submission.py` 对方案目录设有文件白名单，根目录只接受 `proposal.md`、`proposal.zh.md`、`proposal.en.md`、`changelog.md`、`FEEDBACK.md` 与九个既定 JSON，`report/` 只接受五个既定文件，`visual/assets/` 不接受 `.md` 扩展名，因此 `changelog-archive.md` 在包内没有合法落位，强行加入会直接触发校验错误。本文件现为 155,183 字节，距同一单文件上限尚余 106,961 字节（40.8%），按近几版每版约五至六千字节的增速尚有充裕余量，非临近上限项，故本轮不分卷。其二，英文版正文未达本轮设定的 243,500 字节目标：上述四项实测合计回收 8,839 字节，低于审计阶段的估算，差额主要来自控制台块的双语标签与自查段的结论读数两处不可压缩内容；继续压缩须动四项之外的正文实体内容，本轮不作此扩权。

## v8.0 - 2026-08-18

**五条中心线按包内 OSM 快照的真实走向加密，全链读数重算；服务等价基准补出第六组件并升至 v0.5.0；正文补齐七处深化，新增两张图版**

- **五条提交中心线由两点直线加密为真实走向折线（顶点 10 → 114）**：`geometry/roads.geojson` 自 v1.0 起五条中心线全部是两点直线、总计十个顶点，线位既不避让铁路走廊与封闭大院，也不对应任何一条现实道路，长度读数只是两点间的直线距离；而包内自 v6.9 起就随附的 OpenStreetMap 精简快照（`osm-fabric-snapshot-part1/2.json`，3,878 条 way、24,447 个顶点）始终可作走向依据，此前从未被用于线位本身。本版按快照真实走向加密：以原线位为基线按 40 米间距布站，每站作法线在 ±260 米内取与线位走向夹角不超过 45 度的 way 交点为候选，用 Viterbi 动态规划（节点代价贴合原线位、转移代价惩罚横向跳变与换街道）求全局最优走廊，再以 Douglas-Peucker 简化至 20—24 顶点。端点坐标一律不变，以免 `constraints.geojson` 节点挂接因端点迁移而连锁变化；安静替代线在主脊求出后再解，对距主脊 130 米内的候选加惩罚，避免两线塌陷为同一条路（最小间距 62 米）。方法、来源 way、简化容差与前后长度逐要素登记在 `densification` 属性组内。
- **加密质量以既有偏差读数自证**：`visual/assets/osm-fabric/osm-fabric-readings.json` 的中心线偏差读数按新几何重算——主脊对可步行网络的距离中位由 17.806 m 降至 2.94 m、p90 由 73.418 m 降至 8.957 m、最大偏差由 141.276 m 降至 33.462 m；低刺激安静替代线的中位由 12.38 m 降至 1.282 m。该组读数在加密前后使用同一份快照与同一套统计口径，中位数下降本身即是「线位是否踩在现实路网上」的可复算证据，而不是一句自评。
- **链复演器几何支持范围扩展，档案递增至 v0.4.0（CR-2026-08-18-008）**：`visual/assets/seb-op04-chain-run.js` 的几何支持由「仅两点线段」扩至「任意顶点数 LineString」，交点打断、节点挂接与备选构造一律改按折线弧长口径，非 LineString 与不足两点的线改为显式拒绝；两点线段下的读数与旧口径完全等价。`seb-op04-chain-data.json` 递增至 v0.4.0，41+2 项链读数全量回填。加密同时暴露一处此前无人登记的并列：OP-04 在旧几何下到 ROAD-001 与 ROAD-004 的距离并列为 0.0 米，其「物理接口线」实由遍历顺序决定；加密后真实最近邻为 ROAD-004（31 米，主脊 112 米），环节四结论据实改写而不作几何回调。回执 CR-2026-08-18-008 登记全过程，失败样本 FS-060 至 FS-062 一并入册；CR-2026-08-12-003、CR-2026-08-16-006 与 v0.3.1 及此前档案条目按「已发布记录不改写」纪律原样保留。
- **九项指标与可达域体系整体重算**：`metrics.json` 九项随几何重算——`reachability_audit_network_length_m` 20,140.6 → 22,858.6 m、`ai_off_declared_network_length_m` 17,322.014 → 18,882.811 m、`ai_on_reachability_union_sqm` 482,266.876 → 438,298.947 ㎡、`ai_off_reachability_union_sqm` 257,208.435 → 222,078.217 ㎡、`ai_off_reachability_difference_sqm` 225,058.441 → 216,220.730 ㎡、`ai_off_reachability_shrink_ratio` 0.466668 → 0.493318、`op04_reachability_difference_sqm` 50,920 → 44,356 ㎡。图版口径读数由 46.7% 变为 49.3%，主图 `mobility-bluegreen`、审计附图 `mobility-bluegreen-audit`、`site-overview` 三题中英六件与两张几何核验图（`verification-overlay-00-all`、`-02-roads`）随之重出，A3 第 11 页《十节点差集审计明细》整表二次重出，正文图引、展示页 `alt` 与图版主标题三处同步。
- **OP-04 章行程读数随档案 v0.4.0 订正**：正文此前仍印 v0.3.0 的读数——AI 开启态两段行程 244 m 与 471 m、接入距离 55.5 m 与 189 m、备选 B 的接入距离比 4.39 倍；同章的运行输出摘录已是 v0.4.0 的 306.210 m / 371.193 m，两处自相矛盾。本版按档案现值统一为 306 m 与 371 m、69.0 m 与 238 m、3.53 倍，中英同步。
- **服务等价基准升至 v0.5.0：新增第六组件「双联交接台账」（CR-2026-08-18-009）**：本版由维护方按版本治理清点包内既有登记触发，查出两处「条文写了而判据没有」——其一，人工接管在基准里只到「写下一个可被找到的角色」为止，接手本身在运行层留下什么痕迹无人可判，一份把未决项在交接时清空、由同一角色对自己签署的交接单在 v0.4.0 文本下完全合规；其二，服务桌四位序被开办条件档案明文登记为「已确定的设计判据」，却只存在于 PUBLIC-007 的要素属性与散文描述里，没有任何实现读它。新组件十三个字段、七类 `constraint_machine_rule`、九个违例码：交出方与接收方须为不同角色、两方各自署名、未决项随单移交不得清空、接收方在接收／拒收／暂缓三项中选一、拒收与暂缓须写明理由且上一责任方继续在岗；服务桌四位序（SD-1 人工窗口 → SD-2 多通道信息位 → SD-3 AI 开关 → SD-4 退出回执位）入条文为位序判据，三条判据以 `service_desk_present` 为适用前提，不向没有服务桌的节点摊派。判据全部由包内既有素材推出而非新写。
- **桌面推演样例由四十条扩至六十条，实现缺陷由注入样例当场抓出**：新增十条交接实例（十个 OP 节点各一条，五个必填字段逐字取自 `geometry/constraints.geojson` 并经来源对齐比对）与十条按 `risk.json` 注入的缺陷变体（每条只改一处判据输入，并逐字引用该风险的 `pause_threshold`）。起草与实测中另抓到三条自身缺陷：位序数组正确并不蕴含「人工窗口先于一切 AI 构件设立」，故拆为两码；位序判据初稿对全部十个节点必填，等于向九个没有服务桌的节点摊派一条无从满足的要求；一处键名冲突使暂缓判据被静默跳过，由注入样例 HL-N5 当场抓出，实测退出码一、五十九对六十。三条与不采纳项一并登记为失败样本 FS-063 至 FS-067。校验器升至 0.5.0 并执行六个组件；链复演器输入类型不含交接台账，九码结构性不触发，本版零改动并已复跑核实。
- **v0.4.0 规范全文按字节入包**：新增 `visual/assets/seb-spec-v0.4.0.json`（五组件，v0.5.0 发布时留存）与 `visual/assets/seb-change-receipt-v05.json`（CR-2026-08-18-009，一版一号），沿用既有的历史版本快照与一版一号回执两条纪律。
- **正文七处深化（双语同位）**：其一，摘要章补入「本方案的设计深度金字塔是倒置的」声明，把总体范围给关系、重点区给运行合同、单节点给可施工前一步的深度分布写成明示取舍而非疏漏；其二，品牌章「万感四问」由口号改为判据表，四问各自对应一条现场可逐项核对的判据与一件普通人当场可感知的结果；其三，画像表改写为「任务约束维度表」（六维度十类细分），并明示它不是人群分类表；其四，蓝绿章新增「一段真深化：众智园段线性绿带」四行深化表（种什么、地形标高、雨水去向、夜间照明），每项各自挂既有判据或标注现场核定条件；其五，实施章新增「年度公开报告的固定字段」七项表，逐项写明数据宿主，其中「能耗与维护」如实写作包内未登记；其六，OP-04 章补「三条待办与一条方法级存疑，全部登记而不静默处置」；其七，权利章补「许可标识 `COMMUNITY-DISPLAY-ONLY` 的读法」。
- **新增两张图版（中英双语同图单件）**：`assets/figures/green-section-principle.png`（众智园段线性绿带 · 原则性断面，S-01）把蓝绿章四行深化表画成一张自西侧建筑界面到东侧建筑界面的断面，图下四栏与表内四行逐项对应，图中不标尺寸数值；`assets/figures/tactile-corridor-map.png`（全带触觉走廊图 · T-01）把主脊、低刺激安静替代线与十个完成点泛化为可直接制版的触觉印刷原稿，泛化三步（图面容差 2.5 mm 的 Douglas-Peucker 简化、一轮 Chaikin 转角圆化、安静线沿主脊法向外推至平行凸线净距 6.0 mm）、最大位移 27 米与全部印制规格印在图面上。两张分别挂在蓝绿章深化表之后与品牌章图版之后，中英各加一行图引与一段说明；权利台账新增逐件登记：触觉图因线位取自 OSM 快照而为 ODbL 1.0 衍生，署名已印图面，盲文点位为按 GB/T 15720 尺寸自绘的几何图形、不使用盲文字体也不复制标准数值表，且未经盲文使用者或持证校对员校对；断面图不含 OSM 背景、不由 OSM 数据派生，故不适用 ODbL 条件。
- **两张新图如实排除在「接收分辨率可读性 0%」声称之外**：按同一重采样口径量测，两图页面源最小字号分别为 10.0 与 9.5 CSS 像素（画布 1200 × 750 CSS 像素，2 倍导出为 2400 × 1500 像素），折算到短边 768 像素的接收分辨率为 10.2 与 9.7 像素，低于本包 12 像素的判读阈值。两张均为参考性图版（触觉印刷原稿与原则性断面），不属于五张评审可见图，其细注记以全分辨率阅读为准；该结论连同判据一并登记于 `visual/assets/qa-proofing-record.json` R1-05，而不是靠扩大声称范围掩盖。
- **交互场景数据集三条横向接口折线化**：`visual/assets/scene-data.js` 的主脊与安静替代线已在 v6.4 与几何同源，三条横向接口 ROAD-002／003／004 却仍是两点直线，与加密后的 23／20／23 顶点几何不符。本版按同一等距圆柱投影（lon0 116.3475 / lat0 39.9827，比例常数由既有主脊与安静线四十八个点位反解，残差在 0.1 米取整精度内）逐点重算三条接口，顶点数与 `roads.geojson` 完全一致；ROAD-002 东端点由 537.4 变为 537.3，为同一投影常数下的取整订正。双语静态后备图 `scene-fallback{,.en}.png` 由第 16 章交互场景以两次真实点击（播放引导流光、切换 AI 关闭）重新截取合成，A0 板 01 的场景派生件与四册 PDF 随之重出。
- **数据可听化按档案 v0.4.0 重制并复测**：`assets/media/audible-break{,-en}.m4a` 与字幕、映射说明按新读数重合成——一秒等于二十米，AI 开启段行进音 306.210 ÷ 20 = 15.31 秒、实测 30 响且间隔全部为 0.500 秒，末响之后为上行三音到达音；AI 关闭段按接口固定口径没有一米可走，开场即静默，解码后静默区实测 RMS 为零。中文版全长 66.656 秒、英文版 73.886 秒。正文「听得见的断点」段、`manifest.json` 两条媒体描述与 QA 记录 R4-04 同步为新读数，历次读数版本（v0.2 的 30.29 秒、v0.3.0 的 12.18 秒）在映射说明中保留可追。
- **口径统一三处**：其一，可达域主图的图引与展示页 `alt` 由长式改为与图版主标题逐字一致的短式《按本图自设口径：AI 关闭后可达域收缩 49.3%》（英文 “Self-set convention: AI off shrinks the reachable domain by 49.3%”），A3 第 11 页的引题同步改短；其二，三处「按基准 v0.4 四选一」去掉版本绑定改为「按基准四选一」，避免基准版本每次递增都要回改与该判据无关的正文；其三，`assets/media/journey.md` 中「按现行图版重摄」的表述改为「按当时现行的图版重摄」，并如实登记该图在 v5.9 之后又经三次重绘与改题、成片未随之重摄，读者应以 `assets/figures/` 下现行图版为准。
- **权利台账件数订正**：资产类别总表 C 类由 33 件改为 35 件；逐资产登记原文在同一段内既漏列 `dual-state-plan` 中英两版、又把已单列为 J 类的两版折叠纸模 SVG 计入本类，两处误差互相抵消使件数仍显示为 33，本版按实际文件逐项更正为「十三题各中英两版共二十六件 + 七张几何核验图 + 两张新增单件」。
- **计数与登记终值**：正文 907 行、非空 638 行、表格 292 行（数据行 214）、四类证据锚点 257 对 257，depth 40、assumption 12、self_check 4 双语持平（行数为 `wc -l` 口径；此前本条印作 924 行，与当时实测的 923 行差一，本次一并订正，终值按本条目末尾登记的英文版收束后实测）；`manifest.json` 文件条目由 137 增至 141；四册终值为 A3 中文册 2,658,093 字节、英文册 2,131,939 字节，A0 中文板 4,353,372 字节、英文板 4,404,793 字节，页数 16／16／6／6 与页面尺寸未变。QA 记录 R1-02、R1-05、R2-01、R2-03、R2-04、R2-05、R2-06、R4-04 八条按本轮实测更新，未重跑的量测项逐条如实写明。
- **英文版体积收束（为压至仓库 256 KiB 单文件上限）**：`proposal.en.md` 在 v8.0 定稿时为 267,618 字节，超出 `scripts/validate_submission.py` 的 `MAX_MARKDOWN_BYTES`（262,144 字节）5,474 字节。本轮做四处收束，一律只压表达或删可查证的重述，不删事实、不删证据锚点。其一，实施章「年度公开报告的固定字段」由一段引言、七行表与一段收尾降格为单段字段枚举，七个数据宿主锚点按字段就近保留（中英同做，双侧各减 12 行、10 个非空行、9 个表格行，表数由 41 降至 40）。其二，蓝绿章「一段真深化」四行深化表只压英文列措辞，中文列与表结构一字未动（四行由 3,695 降至 3,182 字节）。其三，执行摘要区及其后若干英文长段逐段收紧措辞，覆盖设计依据章、三级范围章、服务等价基准章、OP-04 章、蓝绿章、实施章与权利章。前三处合计使英文版由 267,618 降至 263,813 字节，仍高于 262,144 字节上限 1,669 字节：英文正文经六轮实测，纯措辞压缩的单位回收率已由首轮的 3.8% 降至 0.7%—0.8%，按此法压满全文剩余部分也补不齐缺口，故定稿时再做第四处结构性收束。其四，国土空间章「国土空间规划创新方向」的三行五列表降格为单段字段枚举，表内两个锚点按字段就近分挂进保留句（中英同做，双侧各减 4 行、4 个非空行、5 个表格行、3 个数据行，表数由 40 降至 39）；同批删去实施章年度公开报告段「未登记项须在 L4a 补建后方可报告」一句重述，该义务已由同文 L4a 就绪证据表逐项登记，删句不减事实。四处合计使英文版由 267,618 降至 261,814 字节，低于 262,144 字节上限 330 字节，收束达标。中文版 `proposal.md` 为 213,992 字节，距同一上限尚有余量；上述改动全部中英同做，行数、非空行、表格行、表格数据行与七类证据锚点计数双语逐项持平。

## 待复核事项

- 官方 `SITE_BOUNDARY` 与三处 `KEY_AREA` 精确多边形尚未取得；发布后需统一重算全部面积类图层与指标，当前面积仅为公告近似值。
- 容积率、建筑高度、建筑密度、退线、道路红线、市政容量与文保控制线保持未知，待官方控规条件确认。
- 全部包容性绩效指标缺少现场基线与目标值，须在取得伦理与隐私协议后由真实参与者测试获得。
- 文中空间动作、场景、品牌、活动与区域协同均为概念建议，须由相应专业团队在正式审批框架下深化。

## 公开资料与合规说明

各版本仅使用已公开发布、由组织方提供或已进入来源登记的材料，不包含个人隐私信息、受控地图资料、机构内部文件或未审定的规划控制指标。OSM 快照仅作背景识别线索，不用于推定或替换项目及重点区边界。
