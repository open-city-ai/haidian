# 评审阅读顺序

先看 `assets/figures/site-overview.png` 理解两线、三场、五窗；再看 `proposal.md` 的三处重点区和十二张场景卡；最后用 `metrics.json`、九个 GeoJSON、矩阵与 `self_check.json` 复核。所有精确空间对象均为临时边界内的概念建议。

本方案的区别不是另一个‘唯一标准’，而是把基线、建议、替代方案和不同人群经验并列成公共设计界面。
