# Copyright and rights statement

This package uses participant-authored diagrams and text. Global cases are public mechanism references only; no third-party image, logo, dataset or brand is copied. `sources.json` records URL, license summary, allowed uses, prohibited uses and non-partnership boundaries. CELL·JZ is a concept identity mark; cultural source labels remain separate. If a later name/mark search finds conflict, use `JZ Civic Weave / 京张智链` and remove the conflicting graphic.

All geometry and metrics are provisional. Public displays use rounded estimates; exact machine values and formulas remain in `metrics.json` and must be recalculated with official data.
