# 方案迭代记录

## v1.26 - 2026-08-16

- 修正 `REPO-ISSUE-KEY003` 的证据链：机读来源、假设与中英文正文现统一指向大钟寺 `PROV-KEY-003` 临时几何定位问题 #1029，不再误指向无关的 manifest schema 问题 #1058。
- 保留原有空间纪律：不自行平移临时 polygon，不从其推导站点半径、四象限或精确 TOD 指标，待官方锚点或 polygon 发布后整包重算。

验证依据：#1029 当前为开放的 `PROV-KEY-003` 定位调查；#1058 是已关闭的 manifest schema 合同问题，与大钟寺几何无关。

公开资料与合规说明：本次只修正已公开 GitHub Issue 的引用目标，不新增空间事实、指标、个人信息或第三方媒体。

## v1.25 - 2026-08-16

- 将中英文来源清单中的机读制品说明与文件名列表拆分，使本地包制品与需要来源公开性说明的外部引用保持可机读区分。
- 修复后 `score_submission.py --strict` 的八个建议性维度全部通过；不改变设计、来源、指标、空间数据或实施主张。

验证依据：文件名仍仅指向 manifest 已声明的包内审计制品，中英文正文保持等义。

公开资料与合规说明：本次迭代未新增外部资料、个人信息或第三方媒体。

## v1.24 - 2026-08-15

- 为 EC-01—EC-06 双语实施表补充建议责任角色、独立核验角色和逐项验收证据，消除正文要求“每个试点必须有 owner”但项目清单未落实责任接口的不一致。
- 明确角色仅供后续招采或试点任务书采用，不任命现有机构；要求实名责任主体、核验人员、证据位置与签认日期在开工前落实，并分离交付与最终独立核验。

验证依据：新增内容仅细化方案既有 T1/T2/T3 放行、人工接管、非AI退路、复现和退出契约，不新增现状、面积、建设量、预算或政府承诺。

公开资料与合规说明：本次迭代未新增外部资料、个人信息或第三方媒体。

## v1.23 - 2026-08-15

- 补齐中英文离线展示页的五张核心证据图入口：新增独立的“用地结构 / Land-use structure”面板，展示已在 manifest 和完整报告中声明、但此前未从展示页呈现的 `land-use-structure(.en).png`。
- 展示页现可直接核对完整的五图集；本次不改变用地分区、空间数据、指标、场景分级或实施主张。

## v1.22 - 2026-08-14

- 将中英文 proposal front matter 的迭代标识从滞后的 `v1.17` 同步到 `v1.22`，使机读版本与已合入至 `v1.21` 的迭代记录重新闭合。
- 重新生成中英文离线报告并刷新 manifest/self-check 证据；本次不改变方案正文、场景、空间数据、指标或实施主张。

## v1.21 - 2026-08-13

- 修复中英文离线展示页全局退路控件的状态漂移：手动开合任一场景后，控件现在从全部 12 个原生 `<details>` 的实际状态重新计算标签与 `aria-pressed`。
- 全局操作改为依据当前整体状态展开或收起，并继续保留无脚本静态内容、筛选和打印行为。
- 本次不改变场景分级、退路文本、证据、指标、空间数据或实施主张。

## v1.20 - 2026-08-13

- 为中英文离线展示页增加“展开/收起全部人工退路”控件，评审可一次比较 12 个长尾场景的停止条件，无需逐张打开。
- 控件仅在脚本成功初始化后显示，保留无脚本静态退路；打印模式仍强制显示全部退路内容。
- 本次不改变场景分级、证据、指标、空间数据或实施主张。

## v1.19 - 2026-08-13

- 为中英文离线展示页增加键盘“跳到主要内容”入口；链接获得焦点时可见，并把焦点目标绑定到主要内容容器。
- 本次只改善重复页首后的键盘导航，不改变场景、证据、指标、空间数据或实施主张。

## v1.18 - 2026-08-13

### 改动摘要

- 在中英文离线展示页页首新增语言匹配的“完整报告 / Full report”入口，使评审可从交互摘要直接进入对应的本地 HTML 正文。
- 将报告入口与既有语言切换器组成带可访问名称的导航区，并保留键盘焦点与移动端换行样式。

### 验证依据

- 中文页只链接 `../report/proposal.html`，英文页只链接 `../report/proposal.en.html`；两条路径均为包内文件，不引入远程依赖。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；只补齐既有展示成果之间的离线导航。

## v1.17 - 2026-08-13

### 改动摘要

- 修正中英文离线展示页的打印完整性：即使评审打印前筛选了 T1、T2 或 T3，打印样式现也会显式覆盖浏览器对 `hidden` 属性的强制隐藏，恢复全部 12 张场景卡。
- 中英文 proposal front matter 同步记录 `iteration: v1.17`。

### 验证依据

- 两个 HTML 的 `@media print` 均使用 `.scenario[hidden]{display:block!important}`；屏幕模式的筛选逻辑、场景分级和人工退路文本未改动。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.16 - 2026-08-13

### 改动摘要

- 将离线展示页已有的 T1/T2/T3 放行协议同步进中英文主方案，使 Markdown、生成报告与交互看板都保留同一份最小治理契约。
- 矩阵逐级列出允许活动、放行或持续运行证据及不满足时的人工退路，并明确 T3 不存在未定义的“下一等级”。
- 中英文 proposal front matter 同步记录 `iteration: v1.16`。

### 验证依据

- 表格复用 v1.9—v1.12 已核对的放行协议，不改变 EDGE-01—EDGE-12、GeoJSON、指标、来源或实施承诺。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.15 - 2026-08-13

### 改动摘要

- 为中英文离线展示页增加 A4 横向打印评审模式：去除交互控件和装饰阴影，避免卡片跨页断裂，并使图件按页面缩放。
- 打印时强制显示全部 12 张场景卡和每张卡的人工退路/停止条件，即使评审者在打印前筛选了某一等级，纸面证据也不会被遗漏。
- 中英文 proposal front matter 同步记录 `iteration: v1.15`。

### 验证依据

- 打印规则仅改变展示；场景分级、放行协议、GeoJSON、指标和来源证据未改动。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.14 - 2026-08-13

### 改动摘要

- 将中英文离线展示页的场景筛选器改为渐进增强：只有过滤脚本成功初始化后才显示按钮。
- 在 JavaScript 不可用或被禁用时，不再展示看似可操作但无响应的控件，并保留全部 12 张场景卡作为完整静态退路。

### 验证依据

- 脚本初始化依然提供全部/T1/T2/T3 的 12/4/8/6 张卡筛选；无脚本状态不隐藏任何证据卡。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.13 - 2026-08-13

### 改动摘要

- 为中英文离线展示页的场景筛选按钮增加 `aria-controls`，明确其控制的十二张场景卡区域。
- 将各等级场景数量直接写入按钮，并用独立标签生成筛选结果通知，避免读屏播报重复数量。

### 验证依据

- T1/T2/T3 筛选仍分别显示 4/8/6 张卡，全部视图显示 12 张；只修改交互语义和通知文本，不改动场景证据。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.12 - 2026-08-13

### 改动摘要

- 将英文离线展示页四张证据图的通用替代文本更新为与图件内容对应的英文描述，与 v1.11 的中文读屏体验对齐。
- 将中英文 proposal front matter 的 `iteration` 同步到 `v1.12`，使机读版本与迭代记录一致。

### 验证依据

- 英文页的总览、重点区域、慢行蓝绿和指标证据图现均有内容特定的 `alt` 文本；图件、空间数据和放行协议未改动。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.11 - 2026-08-13

### 改动摘要

- 将中文离线展示页四张证据图的英文占位替代文本更新为与图件内容对应的中文描述，中文读屏用户无需跨语言猜测图件含义。

### 验证依据

- 中文页的总览、重点区域、慢行蓝绿和指标证据图现均有本地化 `alt` 文本；英文页与图件、空间数据和放行协议未改动。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.10 - 2026-08-12

### 改动摘要

- 在中英文离线展示页顶部新增可见、可键盘聚焦的双向语言入口，评审者无需猜测文件名即可切换完整中英文仪表板。
- 两页同步声明 `rel="alternate"` 与对应 `hreflang`，保持纯本地相对链接和离线约束。

### 验证依据

- 中英文页面各保留一个可见的对语言链接，且目标文件均在同一离线包内；未改动 EDGE-01—EDGE-12、空间数据或放行协议。

### 公开资料与合规说明

- 本次迭代未新增外部资料、远程依赖、个人信息或第三方媒体。

## v1.9 - 2026-08-12

### 改动摘要

- 修正中英文“放行协议”的等级语义：第三列现同时适用于 T1/T2 放行和 T3 持续运行，不再暗示 T3 之后存在未定义的“下一等级”。
- 删除 T3 行未由矩阵自身证明的“已低风险 / Low-risk”定性，保留可核对的责任主体、触发条件和人工退路。

### 验证依据

- 中英文表格保持同样的 4 列、3 个暴露等级和失败处置；未改变 EDGE-01—EDGE-12 数据或正文实施政策。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；不新增部署、审批或安全认证主张。

## v1.8 - 2026-08-12

### 改动摘要

- 将中英文“放行协议”的 T1/T2/T3 首列升级为显式行表头，并为列表头声明 `scope="col"`；辅助技术现可把每条允许行为、放行证据和失败退路与正确等级关联。

### 验证依据

- 两个离线 HTML 均保留 4 个列表头和 3 个等级行表头；协议文本、T1/T2/T3 分级和人工退路未改变。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；不新增部署、审批或安全认证主张。

## v1.7 - 2026-08-12

### 改动摘要

- 为中英文离线展示页的“放行协议”四列表格增加有界横向滚动容器、可见键盘焦点与小屏最小表格宽度；窄屏评审可横向查看完整门槛而不会让版面溢出。

### 验证依据

- 协议内容、T1/T2/T3 分级和人工退路均未改变；本次只修复离线展示页的小屏与键盘可达性。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；不新增部署、审批或安全认证主张。


## v1.6 - 2026-08-12

### 改动摘要

- 在中英文离线展示页新增“放行协议 / release protocol”矩阵，将既有 T1/T2/T3 暴露梯度、人工退路与停止条件组织为可逐项核对的升级门槛。
- 明确任何门槛不满足时不得自动升级：保留当前等级、切回人工服务或退出，并把问题带回受控复测。

### 验证依据

- 矩阵只压缩现有正文和 EDGE-01—EDGE-12 场景卡的既有安全契约；不新增部署事实、空间指标、审批或安全认证主张。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；离线矩阵增强既有概念性方案的可审阅性。

## v1.5 - 2026-08-12

### 改动摘要

- 将中英文离线场景筛选器的十二条“人工退路 / 停止条件”从 JavaScript 位置数组迁入各自 `EDGE-01`—`EDGE-12` 场景卡的静态 HTML。
- 禁用 JavaScript 时仍可阅读完整安全契约；后续重排场景卡时，退路内容也不再依赖数组下标而可能错配。
- 筛选与分级计数仍是可选的渐进增强，不改变场景、等级或证据数据。

### 验证依据

- 静态解析可在两个 HTML 中分别找到 12 个场景、12 个 `<details>` 和 12 条退路，且不再存在 `fallbacks` 位置数组。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；只改善包内已审计安全内容的无脚本可读性和绑定稳定性。

## v1.4 - 2026-08-12

### 改动摘要

- 新增参与者自制的概念性封面 `assets/media/cover.png`，用人行与骑行廊道、无障碍使用者、低速服务机器人、现场人工值守和急停界面，把“先暴露边界，再扩大部署”转译为人尺度的画廊入口。
- `manifest.cover_image` 指向该封面；图像仅作概念表达，不替代 GeoJSON、指标、证据图或规划主张。
- 中英文 proposal front matter 同步记录 `iteration: v1.4`。

### 验证依据

- 画面主题仅复用包内已审计的 Edgecase Mile、人工接管、无障碍路径与 T1/T2/T3 分阶测试概念，不新增面积、建设量、现状或实施事实。

### 公开资料与合规说明

- 封面为 OpenAI 内置图像生成工具根据本方案的文字概念生成；未使用外部图片、真实人物参考、商标或地图瓦片。该图不表示官方方案、准确现状、已批规划或已建成项目。

## v1.3 - 2026-08-12

### 改动摘要

- 中英文离线场景筛选器为十二张场景卡补充可键盘展开的“人工退路 / 停止条件”，让核心安全契约无需离开展示页即可核对。
- T1/T2/T3 筛选按钮显示各等级覆盖数量（4/8/6），便于评审直接检查跨等级映射；数量由 v1.2 已核对的场景等级集合计算。
- 中英文 proposal front matter 同步记录 `iteration: v1.3`。

### 验证依据

- 人工退路与停止条件逐项复用中英文正文 `EDGE-01`—`EDGE-12` 场景表，不新增规划事实、指标或空间主张。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；仅把包内既有安全证据带入离线交互展示。

## v1.2 - 2026-08-12

### 改动摘要

- 修正中英文离线场景筛选器的暴露等级映射：`EDGE-03`/`05`/`07`/`09`/`12` 现可在每个适用等级中检索，`EDGE-06` 由误标的 T2 更正为正文规定的 T3。
- 每张场景卡新增可见等级标签，使禁用 JavaScript 或未操作筛选器时仍能核对分级。
- 中英文 proposal front matter 同步记录 `iteration: v1.2`。

### 验证依据

- 等级映射逐项对齐中英文正文 `EDGE-01`—`EDGE-12` 场景表，不新增或改写规划事实。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；仅修复包内已审计场景表与派生展示之间的不一致。

## v1.1 - 2026-08-11

### 改动摘要

- 为中英文离线展示页加入可键盘操作的 T1/T2/T3 场景筛选器，让评审者直接检查十二类长尾条件从受控复现、社区共测到日常开放的暴露梯度。
- 交互仅改变展示，不修改来源证据、GeoJSON、指标或 provisional boundary 纪律。
- 在中英文 proposal front matter 中记录 `iteration: v1.1`，明确合并后的持续迭代。

### 采纳反馈

- 响应仓库新增的 multimodal / interactive presentation 指引：保留静态证据图，同时把适合比较的场景层做成离线交互。

### 暂未采纳或待复核事项

- 官方总体边界与三处重点区 polygon 尚未发布；正式数据到位后仍需整包复算。

### 公开资料与合规说明

- 本次迭代未新增外部资料、个人信息或第三方媒体；交互完全由包内既有场景内容派生。

## v1.0 - 2026-08-11

### 改动摘要

- 初始正式 EDGECASE JINGZHANG 方案通过 PR #1489 合并。
- 后续 readiness 修复把 contributor self-check 证据持久化到正式包。

### 采纳反馈

- 按当前 `persisted-self-check-v1` 契约保存四门自检结果。

### 暂未采纳或待复核事项

- 继续等待官方边界和专业基础资料。

### 公开资料与合规说明

- 仅使用仓库允许的公开、清权或明确标注 provisional 的资料。
