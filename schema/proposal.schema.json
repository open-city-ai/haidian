{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/haidian-ai/proposal.schema.json",
  "title": "Haidian AI Proposal Markdown Contract",
  "type": "object",
  "required": [
    "metadata",
    "sections"
  ],
  "properties": {
    "metadata": {
      "type": "object",
      "required": [
        "title",
        "author_github",
        "language",
        "license",
        "summary"
      ],
      "properties": {
        "title": {
          "type": "string",
          "minLength": 2,
          "maxLength": 120
        },
        "author_github": {
          "type": "string",
          "pattern": "^[A-Za-z0-9-]{1,39}$"
        },
        "language": {
          "type": "string",
          "enum": [
            "zh",
            "en"
          ]
        },
        "translation_file": {
          "type": "string",
          "pattern": "^proposal\\.(zh|en)\\.md$",
          "description": "Recommended non-blocking companion file containing the complete translation of proposal.md."
        },
        "translation_of": {
          "type": "string",
          "const": "proposal.md",
          "description": "Used by proposal.zh.md or proposal.en.md to identify the primary proposal."
        },
        "title_zh": {
          "type": "string",
          "minLength": 2,
          "maxLength": 120,
          "description": "Legacy compatibility field for older inline bilingual proposals; standalone translation metadata is preferred."
        },
        "summary_zh": {
          "type": "string",
          "minLength": 10,
          "maxLength": 500,
          "description": "Legacy compatibility field for older inline bilingual proposals; standalone translation metadata is preferred."
        },
        "license": {
          "type": "string",
          "enum": [
            "COMMUNITY-DISPLAY-ONLY",
            "CC-BY-4.0",
            "CC-BY-SA-4.0"
          ]
        },
        "summary": {
          "type": "string",
          "minLength": 10,
          "maxLength": 500
        },
        "tracks": {
          "type": "array",
          "minItems": 1,
          "maxItems": 3,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]{2,63}$"
          }
        },
        "scenarios": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]{2,63}$"
          }
        },
        "iteration": {
          "type": "string",
          "pattern": "^v?\\d+(\\.\\d+){0,2}([-+][A-Za-z0-9.-]+)?$"
        },
        "version": {
          "type": "string",
          "pattern": "^v?\\d+(\\.\\d+){0,2}([-+][A-Za-z0-9.-]+)?$"
        }
      },
      "additionalProperties": true
    },
    "sections": {
      "type": "array",
      "minItems": 13
    },
    "assets": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^assets/.+\\.(svg|png|jpg|jpeg|webp|gif)$"
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "metadata": {
            "properties": {"language": {"const": "zh"}},
            "required": ["language"]
          }
        }
      },
      "then": {
        "properties": {
          "sections": {"contains": {"const": "设计依据与资料清单"}}
        }
      }
    },
    {
      "if": {
        "properties": {
          "metadata": {
            "properties": {"language": {"const": "en"}},
            "required": ["language"]
          }
        }
      },
      "then": {
        "properties": {
          "sections": {"contains": {"const": "Design Basis and Source List"}}
        }
      }
    }
  ]
}
